# 프로토타입 정렬 핸드오프 — 2026-07-22 (새 컨텍스트 이어받기용)

> 한 줄: R2 이미지 완주 + 통합 QA(버그1 픽스) 후 **프로토타입(`test-web-design`) 기준 프론트/백 정렬** 진행 중. **P1(우정공간 메인·방목록) 완료**, RoomList "완전 똑같이" 리파인 PR이 **리더 비주얼 확인 대기**. 다음 = 그거 머지 → **P2(피드·인생4컷)**.

## ★ 업데이트 (2026-07-22 밤 2회차) — 이식 방식 전환 + 배경 테마
리더가 스샷으로 "눈에 보이는 차이" 지적 → **"styled-components 재번역 말고 프로토타입 CSS를 그대로 가져와라"** 승인. PR #37(`feat/roomlist-exact-prototype`)에 3커밋 추가(모두 리더 재확인 대기):
1. `8066d71` 티켓 정합성(목적지 헤드라인·D-DAY 셀·별 골드).
2. `66f2aed` **RoomList를 프로토타입 CSS 그대로 이식** — `roomlist.proto.css`(`.proto-roomlist` 스코프) 신설, `RoomList.style.js` 삭제, 마크업/클래스명 프로토타입 동일, 데이터만 바인딩. **앞으로 모든 화면 이 방식.**
3. `3f9809e` **앱 배경 테마 시스템**(`lib/appBackground.js` + Settings 피커 + `--clov-app-bg`). 기본=우드&클로버 그라디언트(즉시 동작). 선택형 사진 5종=R2 호스팅.
4. `0054f09` **방 미리보기 모달**(`RoomPreviewModal.jsx`) — 리더 요청, 카드 클릭 시 프로토타입 방 상세(소식 피드·멤버·레벨·입장). 소식=실제 알림(`getNotifications` FRIEND/JOIN/NOTICE 매핑). 카드 클릭→모달(직행 X), 입장하기→`/rooms/:id`, 친구초대→`/rooms/:id/invite`. **방 프로필 편집(⚙️)·초대 서브뷰는 후속.**
5. `638ab3c` 티켓 하단 "입장 ›" 스텁 → **바로 입장**(전파 차단, 본문은 미리보기).
6. `fc13cf2` **헤더 아바타 드롭다운**(clov-header home 이식) — 우측 초대코드·기어·로그아웃 → 아바타(getMe 이니셜/프로필사진)+드롭다운(⚙️ 사용자설정/로그아웃). 초대코드는 툴바에 있음.
7. `931a29c` **사용자설정 모달 2-패널 이식**(clov-profile-modal) — `settings.proto.css`+`Settings.jsx` 재작성, `Settings.style.js` 삭제. 계정(개인정보)/화면(테마=라이트다크·바탕화면 배경피커·편지/추억/마스코트) 레일+패널, 푸터(계정탈퇴/취소/저장). 커스텀물감·대시보드배경은 제외.

**핵심**: 이제 이식 방식이 화면마다 `<screen>.proto.css`(.proto-* 스코프)+프로토타입 마크업으로 굳음. RoomList·Settings 완료. 다음 P2(피드·인생4컷)도 동일 방식.

## 추가 (2회차 후속 — 리더 반복 리파인)
- clov-web `feat/roomlist-exact-prototype` 커밋 다수 추가(모두 리더 재확인 대기, ~948c939까지):
  - 스텁 직행 입장 / 헤더 아바타 드롭다운 / **설정 2-패널→index.html 풍부버전**(테마 스와치·커스텀 물감색·바탕화면·버튼그룹, 패널별 푸터) / 설정 레일 높이상한+브랜드푸터(빈공간 정리).
  - **방 미리보기 모달 3-뷰 완성**: main + **친구 초대**(getInvites/createInvite, 링크·코드 복사/공유/재발급) + **방 프로필 편집**(getRoom 프리필 → 테마색·이동수단·이름·소개글 → **updateRoom = PATCH /rooms/{id}**, api/room.js에 추가). 카드 클릭→모달, 하단 스텁→직행 입장.
- **검증 팁**: 로그인 게이트 화면은 셀프 스샷 불가(이 환경 screenshot 툴 타임아웃). CSS/레이아웃은 `src/.../_preview.html` 하니스+`javascript_tool` 수치측정으로 진단함(설정 레일 때 사용, 후 삭제).
- **여전히 대기**: R2 배경 5장 업로드 / 리더 로컬 재확인 / (선택)라이트-다크 실제 전환·대시보드배경·대표사진 갤러리.

## ★ 업데이트 (2026-07-22 밤 3회차 — RoomList 리파인 마무리) — 새 컨텍스트는 여기부터
clov-web `feat/roomlist-exact-prototype` **20커밋(~`79dfed6`)**, 전부 리더가 스샷으로 확인·승인. 리더가 "잘되네"로 라이트/다크까지 OK. **머지는 아직 안 함**(리더 로컬 전체 재확인 후).

이번 라운드 추가분(커밋):
- `5736878` 이동수단 프로토타입 일치(자동차→**시외버스**, ti-plane/bus/ship/train, 레거시 값 아이콘 별칭).
- `8eef483` **아바타 원형 버그 수정**(grid/flex center가 img height:100% 무력화 → `position:absolute;inset:0`로 컨테이너 꽉 채움). 설정 레일·기본정보·헤더·rp-ico·대시보드 멤버 전부.
- `e70f197`+`5727598` **편집모드 드래그 앤 드롭**(데스크톱 HTML5 + 모바일 롱프레스 터치, splice로 놓은 자리에 꽂음) + 빨간 🗑️=나가기(leaveRoom).
- `fc81db6` **전체 레이아웃 full-width**: index.css `#root`의 템플릿 잔재(width:1126px·text-align:center·border-inline) 제거 → 앱 full-width. **배경을 body(글로벌 --clov-app-bg)로 이관** → 헤더·배경 양 사이드까지, `.main`은 860px 중앙.
- `fbd7251` **배경 사진 번들**(R2 폐기): sharp로 5장 1920px WebP(`public/backgrounds/`, ~1.1MB).
- `17025f3` 커스텀 물감색 버그(solid색을 background-image에 → 무효) → 단색 그라디언트로 감쌈.
- `7a1b52d` 기본 배경 = **심플 그라디언트**(리더 결정). 사진6+물감색은 선택.
- `79dfed6` **라이트/다크 실제 적용**(`lib/theme.js`, `body.light-mode`, main.jsx initTheme, 설정 스와치). 기본=다크, 라이트는 토글. 적용범위=이식화면(RoomList·설정·미리보기)만.

**바로 할 일(새 컨텍스트):** ① 리더 **로컬 전체 재확인**(full-width가 Dashboard/피드/편지 등 안 깨는지 포함) → OK면 **PR #37 스쿼시 머지** → ② **P2(추억피드·인생4컷)를 같은 그대로-이식 방식**으로. Dashboard 등 다른 화면 이식 시 라이트/다크도 같이(현재 토글은 이식화면만 먹음).
**환경 팁:** 이 세션 브라우저 screenshot 툴은 타임아웃, 프리뷰 하니스는 CSS 캐시가 잘 안 갱신됨 → CSS/레이아웃 검증은 `javascript_tool` 수치측정 위주. 로그인 게이트 화면은 리더 눈 확인 필수. clov-api main=`09c493e`(백 완료, 열린 PR 없음).

**남은 것(R2 업로드 — 리더 필요):** 사진 배경 5장을 R2 `clov-media/app-backgrounds/`에 `<id>.webp`로 업로드해야 활성화(id: lp-wood-desk·clover-coast·neon-city·minimal-clover·botanical). 공개베이스 `https://pub-809fa22c1b844b338e5eb73dd0a10c90.r2.dev`. 로컬에 최적화/업로드 도구 없어 Claude가 못 올림 → 리더가 대시보드로 올리거나(권장 WebP ~1920px) 프로덕션 R2 쓰기 허락+도구. 원본=`test-web-design/02-main/background-concepts/`. 업로드 후 코드가 .webp 참조하니 확장자 맞추기(아니면 코드 1줄 수정).

## 지금 상태 (커밋 앵커)
- **clov-api main = `09c493e`** — 백엔드 전부 완료. 열린 PR·이슈 0.
- **clov-web main = `38ccbbc`** (#35 Dashboard·#36 RoomList v1 반영).
- **web-design main = 이 문서 커밋** (갭 감사·핸드오프).

## ✅ #37 머지 완료 (2026-07-22) — 다음은 P2
clov-web **PR #37 스쿼시 머지 완료**(리더 "머지하자" 승인). **clov-web main = `1a7f005`**, feat 브랜치 삭제, 열린 PR 0. RoomList·설정·미리보기·배경테마·라이트/다크·full-width 전부 본선 반영. **바로 다음 = P2(추억피드 월별아카이브·폴라로이드·"무사진→🍀" / 인생4컷 스트립)를 같은 `<screen>.proto.css` 그대로-이식 방식으로.** Dashboard 등 이식 시 라이트/다크도 함께.

## ★ P2 추억피드 이식 완료 (2026-07-22) — 리더 비주얼 확인 대기
clov-web 브랜치 **`feat/feed-prototype-archive`** (커밋 `9271588`, 아직 머지 안 함). 04-feed/feed.html + 02-main/desktop.css를 **`src/pages/feed/Feed/feed.proto.css`**(`.proto-feed` 스코프)로 그대로 이식, **`Feed.jsx`** 마크업을 프로토타입 구조로 재작성해 실제 API에 바인딩. **`Feed.style.js`(styled-components) 삭제.** lint·build 초록.
- **폴라로이드 카드**(회전·크림 종이 두 테마 공통·작성자 배지 "내 기록/OO의 기록"·"무사진→🍀 클로버 보관"·태그·날짜·댓글수).
- **월별 아카이브 팝오버**(❮연도❯ 네비 + 12개월 그리드 + N개 카운트 + 전체보기) — 방 추억 전체를 한 번에 받아 **클라이언트 월 그룹핑**.
- **필터 탭 전체/내 기록/친구 기록** — `writer.id` 기준 클라 필터(백 param 안 씀).
- **추억 상세 시트**(memory-detail-sheet): 사진 열(대표+썸네일 스트립+작성자 추가/삭제/◀▶순서) + 본문/태그 + 한 줄 메시지(=댓글) + 수정/삭제(확인).
- **글쓰기 모달**(wm-*): 사진 다중 선택 → 본문 생성 후 순차 업로드. **개별 사진 실패는 삼켜 본문 1회 생성 보장(중복 방지)**. 함께한 친구 칩=멤버(나 제외, 기본 전체 선택), 해시태그 텍스트 파싱.
- **테마**: 기본 다크(폴라로이드=크림 종이), `body.light-mode .proto-feed`로 라이트 오버라이드.
- ⚠️ **백엔드 갭(코덱스 담당)**: 피드 요약 `MemorySummaryResponse.thumbnailUrl`이 **항상 null 하드코딩** → 사진 있는 추억도 카드 대표 썸네일이 안 나오고 전부 🍀로만 보임. 내 이식의 회귀 아님(원래 그랬음). **커버 이미지 URL 채우기 백엔드 수정 필요**(태스크로 남김). 상세 시트는 `/memories/{id}` images로 실제 사진 정상 표시.
- **검증 제약**: 로그인·방ID 게이트라 Claude 셀프 비주얼 검증 불가 → **리더 로컬(5173)에서 방 진입 후 `/rooms/:id/feed` 눈 확인** 필요. OK면 머지.
- **다음 = P2 나머지**: 일정/인생4컷 스트립(`Schedule.jsx`) 같은 그대로-이식 방식.

## ★ P1 Dashboard 재이식(그대로-이식 방식) (2026-07-22) — 리더 비주얼 확인 대기
리더가 본선 vs 프로토타입 좌우 스샷으로 "우정공간도 rooms처럼" 지시. #35 Dashboard는 styled-components 재번역이라 프로토타입 "살아있는 대시보드"와 드리프트 → **RoomList/Feed와 동일한 `<screen>.proto.css` 그대로-이식으로 재작성.** clov-web 브랜치 **`feat/dashboard-prototype`**(main 기반, 커밋 `40d9490`, **feed 브랜치와 분리**·독립 머지 가능). 정본=`02-main/index.html`+`pages/space-page.js`+`desktop.css`. **`dashboard.proto.css`**(`.proto-dashboard` 스코프)+**`Dashboard.jsx`** 재작성, **`Dashboard.style.js` 삭제.** lint·build 초록.
- **헤더(clov-hdr)**: 로고 + nav 탭(우정공간·추억피드·행운편지·일정계획→방 서브라우트) + 알림 + 아바타 드롭다운(⚙️설정=Settings 모달 재사용/로그아웃).
- **성장 배너(v5-scene LP 턴테이블)**: 우드+바이닐은 **단일 이미지 에셋**(`public/banners/lp-<계절>.png`, 원본 970×215 PNG 4장, `scene-sky` background-cover) + HUD(우리 함께한 지·D+N 일째·계절 트랙 칩·Lv 필 진행도). 계절=현재 월로 자동.
- **보딩패스 커버 카드(main-photo-card)**: 대표사진(회전 폴라로이드, 없으면 🍀) + 상태 메시지 편집(`title-input-box`, 가중 40자 카운트=한글2/영1, 저장 버튼은 변경 시 노출) + 멤버 미니 아바타 스택·"N명 함께하는 중"·멤버 보기(→모달).
- **다가오는 D-day**: `+새 D-day 만들기` + 3열 `schedule-banner`(📅 제목·날짜·D-badge, 예정 plan 상위 3, →schedule). **참여자별 추억 증거 카드**: 글쓰기·전체 피드 보기 + `space-memory-grid` 미니 폴라로이드 가로 스크롤(최근 6, →feed). 참여 멤버 모달.
- **라이트/다크**: 기본 다크, `body.light-mode .proto-dashboard` 오버라이드(보딩패스 흰 카드 복원). 배너 HUD는 이미지 위 흰 텍스트라 테마 무관.
- **데이터 바인딩**: getRoom·getRoomLevel(티어명 프론트 매핑 1~777)·getRoomMembers·getPlans(SCHEDULED)·getMemories·updateStatusMessage 전부 기존 유지. `writer.id` isMine.
- **보류(문서화)**: 배너 데코 애니메이션(계절 파티클·풍선·XP 클릭 레벨업)·추억 evidence-viewer **팬+드래그 필름스트립 타임라인**(미니 폴라로이드 행으로 대체)·**배너 테마 테스트**(P2 횡단). 대표사진 업로드=방 커버 업로드 API 없음→표시만. 배너 PNG WebP 최적화=sharp 미설치·신규 패키지 팀 승인 필요로 후속.
- **검증 제약**: 로그인·방ID 게이트라 셀프 비주얼 검증 불가 → **리더 로컬(5173) 방 진입 `/rooms/:id` 눈 확인 후 머지**. 배너 이미지 4장(≈2MB) public 번들.

### 지금 열린 clov-web 이슈·PR (둘 다 CI 초록·MERGEABLE/CLEAN, 리더 비주얼 확인 후 머지, 서로 독립)
- **이슈 #38 → PR #40** (`feat/38-dashboard-prototype`, `40d9490`) = P1 우정공간 대시보드 재이식.
- **이슈 #39 → PR #41** (`feat/39-feed-prototype`, `3dd36fe`) = P2 추억피드.
- 라벨 `프로토타입-정렬`+`frontend`. 머지는 리더가 로컬(5173) `/rooms/:id`·`/rooms/:id/feed` 눈 확인 후 직접(분류기 차단). 머지 순서 무관(파일 겹침 없음).

## (참고) #37 착수 당시 메모 — 완료됨
1. **clov-web PR #37** (`feat/roomlist-exact-prototype`) = RoomList "완전 프로토타입" 리파인. **리더 비주얼 확인 후 머지.** 내용: 라이트 우드 배경(global-theme.css)·tabler 아이콘(이모지 대체)·**방 만들기 모달**·페이지네이션·**편집 모드 ◀▶ 순서(localStorage "내 순서")**·요청한 방 액션(요청취소=`DELETE /join-requests/{id}`·재요청→/join·지우기). 빌드·lint·CI 초록. 리더가 "맞다" 하면 머지, 아니면 색·간격 조정.
2. 머지 후 **P2 착수**: 추억피드(월별 아카이브 접기·폴라로이드 카드·"무사진→🍀") / 인생4컷 스트립. 그다음 P3(편지·알림·설정·로그인 폴리시) + 횡단(배너 테마=계절·시간·이벤트·레벨).

## 정렬 로드맵 = 갭 감사 문서
**`work-logs/2026-07-22-프로토타입-갭-감사.md`** 가 화면별 차이·우선순위 SSOT. P1 두 개 ✅ 표시됨. P2/P3/횡단이 남음.

## 이번 라운드에 한 백엔드 보강 (프로토타입 데이터용)
- `RoomSummary`에 **`nextPlan{title,planDate}`** (clov-api #49) — 티켓 다음약속 D-day.
- **`GET /join-requests/mine`** (#49) — 요청한 방(내 PENDING/REJECTED + roomName·roomStatus).
- **`DELETE /join-requests/{id}`** (#50) — 요청 취소(본인 PENDING만).
- 티어명은 백 없이 프론트 매핑(씨앗의 우정🌱…전설💎, 레벨 1~7). 상태메시지=`myStatusMessage`+`PATCH /members/me/status-message`(기존).

## ★ 작업/검증 제약 (중요 — 안 그러면 헤맴)
- **Claude는 로그인 필요한 화면을 셀프 비주얼 검증 못 함**: 로그인은 비번 입력이라 Claude가 안 하고, 백엔드 CORS가 `localhost:5173` 고정이라 다른 포트 프리뷰도 막힘. → **리더가 로컬(clov-web dev 5173 + clov-api bootRun)에서 눈으로 확인.** Claude는 빌드/lint/CI + API 스모크(토큰 받으면)까지.
- **프로토타입 프리뷰**: 루트 `.claude/launch.json`의 `clov-preview`(static-server 3400). `localhost:3400/03-rooms/makerooms.html` 등으로 대조. [[clov-canonical-tree-and-preview]] 참고.
- **프로토타입 정본 = `web-design-repository/test-web-design`**. makerooms는 **라이트 우드**(마지막 로드되는 `02-main/css/global-theme.css`가 정본, 인라인 다크 팔레트 아님 — 초기에 다크로 잘못 만들었다가 정정).
- **tabler-icons webfont** = clov-web `index.html`에 jsdelivr CDN 추가됨(`ti ti-*`).
- **머지 위임**: 리더가 "머지할 거 있으면 알아서 머지"라고 위임함 → CI 초록이면 Claude가 셀프코멘트+스쿼시 머지 진행. 단 **비주얼-피델리티 PR(#37 류)은 리더 확인 후** 머지.

## 큰 그림(프로젝트 전체)
- 전 도메인 백+프론트 완료 · 이미지 R2 E2E 완료 · 통합 QA 완료(약속삭제 FK 버그 #48 픽스). 배포는 리더가 "아직" — 보류.
- 지금은 **프로토타입 룩&필 정렬 페이즈**(리더 요청: "완전 똑같이 가져와"). 화면 하나씩.
