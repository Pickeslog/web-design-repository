# 핸드오프 — 2026-07-31 (다음 세션 이어받기용)

> 자체완결 요약. 오늘은 **리더 시간이 거의 전부 감사·리뷰로 갔고, 배포 전에 사고 하나를 막고 프로덕션 버그 하나를 재현해냈다.**
> 앞선 맥락: `2026-07-30-핸드오프.md`(오전 사고 2건·계약 §15) · `2026-07-30-핸드오프-CICD-자동배포.md`(CD 구축).
>
> ⚠️ **배포는 오늘 안 했다.** main에는 다 들어갔지만 **실행 중인 서버는 아직 옛 코드다.** §11에 정리했다.

---

## 1. 현재 상태

| 저장소 | main | 오늘 머지 |
|---|---|---|
| `clov-web` | `bde7dae` | 4건 (#185·#188·#189·#191) |
| `clov-api` | `b82b507` | 2건 (#96·#97) |
| `web-design` | `190c7bc` | 3건 (#23·#24·#25) |

**열린 PR 8건 · 열린 이슈 다수.**

| PR | 저장소 | 상태 |
|---|---|---|
| **#182** | clov-web | **프론트 CD 워크플로 — 내 것, 머지 대기** |
| **#180** | clov-web | `CHANGES_REQUESTED` · **main과 충돌 중**(§7) |
| **#178** | clov-web | 골드 배지(#177) — **아직 안 봤다. 이틀째다** |
| **#176** | clov-web | `CHANGES_REQUESTED` · 한 줄 수정 대기 |
| **#165** | clov-web | `CHANGES_REQUESTED` · 문구 수정 대기(계정 삭제 안내가 계약과 반대) |
| **#92** | clov-api | 골드 지급(#89) — **아직 안 봤다.** #17 판단과 묶여 있다 |
| **#88** | clov-api | `CHANGES_REQUESTED` · `List.of(...)` 한 줄 |
| **#17** | web-design | `CHANGES_REQUESTED` · 골드 획득 캡 판단 대기 |

> ⚠️ **공유 워킹트리 주의.** 오늘도 `clov-web`이 `ci/web-deploy-workflow` 브랜치에 남의 미커밋 작업과 함께 있었다. **작업 시작 전 `git branch --show-current`·`git status` 필수.**

---

## 2. ★ 배포 전에 막은 것 — 제목 40자가 400을 낼 뻔했다

lami2342의 #185(추억 글자수 목업 정합)가 **정확히 이슈대로 구현됐는데**, 그 이슈가 틀렸다.

| | 목업 `space.js:169` | 계약 §487 | 백엔드 |
|---|---|---|---|
| `title` | **40** | ≤25 | `@Size(max = 25)` |

**내가 이슈 #148을 쓸 때 백엔드를 확인하지 않았다.** 목업만 보고 "40으로 맞춘다"고 적었고, 팀원은 그대로 정확히 구현했다. 그대로 머지했으면 **사용자가 26~40자 제목을 입력할 수 있게 되고 저장 시 400이 났다.**

### 추측이 아니라 재현했다

DTO를 25로 되돌려 테스트를 돌렸다.

```
java.lang.AssertionError: Status expected:<201> but was:<400>
  at MemoryIntegrationTest.memoryTitleAcceptsFortyCharactersAndRejectsMore(...:100)
```

**이 400이 사용자가 받았을 바로 그 응답이다.** 테스트가 실제로 가드가 되는지 역방향으로도 확인한 것이다.

### 해결 순서 — 뒤집으면 안 된다

```
1. 계약 §487 ≤25 → ≤40      (web-design #23)
2. clov-api DTO 2곳 → 40    (clov-api #96)
3. 1·2 머지                  ✅ 완료
4. clov-web #185 머지        ✅ 완료
```

**`memories.title`이 `VARCHAR(100)`이라 마이그레이션은 필요 없었다.** 제약은 애노테이션뿐이었다.

**25에는 설계 근거가 없었다** — 주석에도 계약 논의에도 없고, DTO에 박힌 값이 그대로 계약으로 옮겨진 것이다. **어제 시작 골드 20,000과 완전히 같은 구조다.**

---

## 3. ★★ 재현해낸 프로덕션 버그 — 추억을 지우면 그 약속이 영구히 막힌다

`clov-web` #181 착수 검토 중 발견. **실제로 재현했다.**

```
1. 약속(memory_status=CANDIDATE)에 추억 작성  → 201, plan.memory_status=WRITTEN
2. 그 추억을 삭제                              → 200
3. 삭제 후 plan.memory_status                 → WRITTEN  ← 되돌아가지 않는다
4. 같은 약속에 다시 작성                        → HTTP 500 INTERNAL_ERROR
```

**409가 아니라 500이다.** 사용자는 "서버 오류가 발생했습니다"만 보고 이유를 알 수 없다.

### 원인 — 세 곳이 어긋나 있다

| | |
|---|---|
| **① 유니크 키가 soft delete를 모른다** | `uk_memories_plan_writer (plan_id, writer_id)` — `deleted_at`이 없다. 삭제된 행이 자리를 계속 차지 |
| **② 서비스 체크는 삭제분을 못 본다** | `findByPlanIdAndWriterId`가 `deleted_at IS NULL`로 거른다 → **체크는 통과하고 INSERT에서 DB가 막는다** |
| **③ 제약 위반 핸들러가 없다** | `GlobalExceptionHandler`에 `DataIntegrityViolationException` 핸들러 부재 → 500 |

그리고 `delete()`가 `memory_status`를 안 되돌린다 — `createFromPlan`은 `updatePlanMemoryStatusWritten`을 부르는데(`MemoryService.java:85`) **삭제에는 짝이 없다**(`:247`).

→ **clov-api #98**에 약속 연결 변경·해제와 함께 담았다. **같은 로직이라 따로 내면 세 번 고치게 된다.**

> **되돌릴 때 주의** — 그 약속에 **다른 멤버의 추억이 남아 있으면 `WRITTEN`을 유지**해야 한다. 우정공간은 한 약속에 여러 명이 각자 기록을 남기는 구조다.

---

## 4. 🔴 목업 숫자를 옮기는 자리에서 이번 주 세 번 터졌다

| 건 | 목업 | 실제 백엔드 | 결과 |
|---|---|---|---|
| 추억 제목 (#148/#185) | 40 | **25** | 400 — 오늘 막음 ✅ |
| **추억 사진 (#181)** | 30 | **10** | **아직 살아 있다** ⚠️ |
| 친구 메시지 | 80 | 255 | 안전(좁히는 방향) |

### ⚠️ 사진 개수는 지금도 깨져 있다 — 네 곳이 다 다르다

| 곳 | 값 |
|---|---|
| 목업 `utils.js:231` | 30 |
| **화면명세 `03-memory-feed-screen.md`(리더 결정 07-30)** | **8** ← 정본 |
| `clov-web` `Feed.jsx:16` `WRITE_PHOTO_LIMIT` | **15** |
| `clov-api` `MemoryService.java:48` `MAX_IMAGES_PER_MEMORY` | **10** |

**프론트가 15장을 허용하는데 백엔드는 10장에서 `507 STORAGE_QUOTA_EXCEEDED`를 던진다 — 11번째 업로드가 실패한다.** #181이 8로 맞추면 같이 없어진다(8 < 10이라 안전).

**★ 이슈를 쓸 때 목업 숫자를 옮기면 반드시 계약·DTO를 같이 대조할 것.** 세 번 다 출처가 리더가 쓴 이슈였다.

---

## 5. 팀원 판단이 좋았던 건 — #181 착수 전 차단

kimgyubi1234가 **착수 전에** "약속 연결은 서버에 저장할 방법이 없어서 화면만 만들면 새로고침 시 되돌아가는 반쪽짜리가 된다"고 알려왔다. 확인해보니 맞았다.

- `UpdateMemoryRequest`에 `planId` **없음**
- `MemoryMapper.xml`에 `plan_id`를 UPDATE하는 SQL **아예 없음**(INSERT·SELECT만)

**#185가 못 잡아서 리뷰까지 갔던 것과 같은 종류를, 착수 전에 잡았다.** 프로토타입이 브라우저 메모리만 바꾸는 목업이라는 걸 근거로 든 것도 정확했다.

→ #181을 **사진(8장)·버튼 스타일만**으로 축소하고 제목도 바꿨다. 약속 연결은 #98로.

---

## 6. 마스코트 '버거노인' — 3개 레포 미커밋 정리

07-30 심야 세션 결과물이 **세 레포에 미커밋으로 흩어져 있었다.** PR 3건으로 나눠 올려 전부 머지했다(clov-web #191 · web-design #25 · clov-api #97).

### ★ 스프라이트가 14배 과대였다

| | 크기 | 용량 |
|---|---|---|
| `rob.png` | 900×510 | 28 KB |
| `crobi.png` | 366×640 | 326 KB |
| ~~버거노인(원본)~~ | ~~1024×1536~~ | ~~**1,170 KB**~~ |
| **버거노인(수정)** | **427×640** | **256 KB** |

화면에는 **110px**로 그린다(`Mascot.css:27`). **대시보드는 진입할 때마다 이 이미지를 받는 자리다.** 레이어 분리본 2장(2.5MB)은 앱이 import하지 않아 web-design으로 보냈다.

### ★ 미커밋 작업이 남의 머지를 되돌릴 뻔했다

작업이 #188 이전 main에서 시작돼 `Settings.jsx`의 `label: '로봇'`을 붙들고 있었다. 그대로 커밋했으면 **chacha의 #188('로봇'→'롭')이 조용히 되돌아갔다.** 최신 main 위에서 다시 올리고 화면 전체 '로봇' 문자열 0건을 확인했다.

### 곁가지 — 계약이 틀렸다

| 위치 | 값 |
|---|---|
| 계약(수정 전) | `robot` |
| 프로덕션·DB·프론트 | **`rob`** |
| 프로토타입 위젯 `croby-mascot.js` | `robot` |

**프로토타입에서만 쓰는 이름이 계약으로 흘러 들어갔다.** #25에서 정정했다.

그리고 **서버가 `mascotType`을 검증하지 않는다** — `UpdatePreferencesRequest.mascotType`이 제약 없는 `String`이라 `"aaaa"`도 저장된다(`VARCHAR(20)`만 제약). **에러가 안 나고 화면에서 마스코트만 조용히 사라진다.** 계약에 명시했고 검증 추가는 미결(§11).

---

## 7. 🔴 프로세스 — 자체 머지가 4건이 됐다

| PR | author | mergedBy | 리뷰 |
|---|---|---|---|
| #168 (07-30) | chacha1650a | chacha1650a | 0건 |
| #175 (07-30) | chacha1650a | chacha1650a | 0건 |
| **#188 (오늘)** | chacha1650a | **chacha1650a** | **0건** |
| **#189 (오늘)** | chacha1650a | **chacha1650a** | **0건** |

어제 "두 번이면 패턴"이라고 적었는데 **오늘 두 건이 더 나왔다.**

그리고 **속도가 문제다** — 이슈 #186·#187을 올린 지 **1시간 만에** 본인이 작업하고 본인이 머지했다. 내가 그 이슈에 주의사항을 단 게 **머지 4분 뒤**였다. 경고가 이미 끝난 일에 도착했다.

> **다행히 결과는 괜찮았다** — #188은 `label`만 바꾸고 `value: 'rob'`은 안 건드렸다. 내가 제일 걱정한 사용자 설정 파괴는 안 일어났다.

**부작용은 이미 나오고 있다** — #189가 `Header.css`를 또 건드려서 lami2342의 #180이 **여전히 충돌 중**이다. #184 → #189로 같은 파일이 두 번 더 바뀌었다. **rebase가 늦어질수록 비싸진다.**

### ★ 브랜치 보호 규칙이 필요하다

`main`에 **PR 필수 + 승인 1건 필수**만 켜면 구조적으로 막힌다. **Settings → Branches에서 리더가 직접 켜야 한다.** 사람 기억에 의존하는 규칙은 네 번째부터도 안 지켜진다.

---

## 8. 오늘 처리한 리뷰·이슈

| 대상 | 결론 |
|---|---|
| clov-web **#176** | `CHANGES_REQUESTED` — **통일이 절반만 됐다**(아래) |
| clov-web **#180** | `CHANGES_REQUESTED` — Esc는 정확, **main과 충돌** |
| clov-web **#185** | 코멘트만 — 팀원 잘못이 아니라 이슈가 틀렸다 |
| clov-web **#181** | 범위 축소 + 제목 변경 |
| clov-web #186·#187 | 범위 확정 + 본문 요청(빈 템플릿) |
| clov-web **#155** | **착수 승인** + 함정 발견(아래) |
| clov-api **#91** | `MEMBER_JOINED` 신설 확정 → 계약 §13 반영(#24) |

### #176 — 색은 같아졌는데 셋이 어긋난다

"전체 피드 보기"를 "글쓰기"와 통일하기로 했는데 **CSS 선택자만 묶어서** 색·테두리만 같아졌다.

| | 글쓰기 `dashed` | 전체 피드 보기 `action` |
|---|---|---|
| 폰트 | Space Mono 고정폭 | 상속(산세리프) |
| 좌우 패딩 | 12px | 6px |
| 호버 | `translateY(-1px)` | 없음 |

**JSX에서 `variant="dashed"`로 바꾸고 특례 CSS 두 줄을 지우면 넷이 자동으로 일치**하고 코드가 오히려 줄어든다. 지금은 JSX가 `action`인데 화면은 dashed라 다음 사람이 되돌릴 여지가 있다.

### ★ #155 — 이스터에그가 하루 교감 한도를 태운다

마스코트가 `<button onClick={() => interactMutation.mutate()}>`라 **클릭 1회 = API 1회**인데, 백엔드에 `DAILY_INTERACTION_LIMIT = 3`이 있다.

| 이스터에그 | 벌어지는 일 |
|---|---|
| **3연타 → dizzy** | 클릭 3번 = **하루 한도 전부 소진** |
| 드래그 → scared | 버튼 위에서 놓으면 click 발화 → 1회 소진 |
| 롱홀드 → angry | 위와 같음 |

게다가 `disabled={isPending}` 때문에 요청 도는 동안 버튼이 죽어 **연타 판정(900ms) 자체가 안 될 수 있다.** "교감(API)"과 "이스터에그(로컬 연출)"를 분리해야 한다고 적고, 수용 기준에 **"이스터에그를 봐도 교감 3회가 남아 있는지"** 를 넣어달라고 요청했다.

### #91 — `MEMBER_JOINED` 신설 확정

수락 시점에 **성격이 다른 알림 두 개**가 나간다. 수신자도 actor도 달라서 한 subType으로 겸할 수 없다.

| subType | 수신자 | actor |
|---|---|---|
| `JOIN_REQUEST` | 기존 멤버 | 신청자 |
| `JOIN_ACCEPTED` | **신청자 본인** | 수락자 |
| **`MEMBER_JOINED`**(신설) | **기존 멤버 전원**(합류자 제외) | **합류자** |

`JOIN_REQUEST`를 재사용하면 "가입 신청 N건"을 세는 순간 **이미 수락된 합류까지 신청으로 센다.**

> **덤 — `JOIN_ACCEPTED`는 구현이 0건이다.** `src/` 전체에 참조가 없다. **신청자 본인에게 "수락됐어요"가 나가는 알림이 아예 없다.**

---

## 9. ★ 검증 기법 — 다음에 또 쓸 것

- **역방향 검증** — 테스트를 추가했으면 **고친 코드를 되돌려 실제로 깨지는지** 확인한다. `expected:<201> but was:<400>`이 나와야 그 테스트가 진짜 가드다. 통과만 보면 아무것도 안 지키는 테스트를 넣을 수 있다
- **프로브 테스트** — 버그를 이슈로 올리기 전에 **일회용 테스트를 써서 실제 응답을 받아본다**(§3의 500). 다 쓰고 `git checkout --`로 걷어낸다. "500이 날 것이다"와 "500이 났다"는 다른 문장이다
- **`git log A..B`는 "앞선 커밋"이지 "같다"가 아니다** — 오늘 이걸 "main과 동일"로 잘못 읽어서 백포트가 필요하다고 오판했다. **squash 머지 때문에 SHA가 갈라진 브랜치는 커밋 로그가 아니라 `git diff A B --stat`으로 판단할 것**
- **로그인 게이트 화면 검증** — 백엔드 없이 5173에서 `api.defaults.adapter` 스텁으로 대시보드까지 띄웠다. 어댑터가 `client.js`의 **named export `api`**다(`default` 아님). 함정과 절차는 메모리 `clov-web-browser-verify-pitfalls` 참조
- **Browser pane이 안 보이면 스크린샷이 실패한다** — `not compositing frames`. DOM 읽기로 대체 가능하다(오늘 마스코트 렌더·설정 옵션 전부 DOM으로 확인)
- **PowerShell `Set-Content`는 한글 파일을 깨뜨린다** — 오늘 Java 소스 주석을 깨먹고 `git checkout --`로 복구했다. **한글이 든 파일은 Edit 도구로만 고칠 것**

---

## 10. 다음에 할 것

### 리더 판단 대기 (팀원이 막혀 있다)

1. **#17 골드 획득 캡** — 유저 단위 하루 총 상한(500 권장). **#92(구현)가 이미 올라와 있어 이제 병목이다**
2. **#178** — 이틀째 미확인
3. **#92** — clov-api 골드 지급 구현. #17과 묶어서

### 리더 무소유 레인

- **#98**(오늘 신규) — 삭제 500 + 약속 연결. **500이라 우선순위가 높다**
- **`mascotType` 서버 검증** — 지금은 `"aaaa"`도 저장된다
- **#87 → `team-guides/21`** — 문서는 완성돼 있고 옮기기만 하면 된다(이틀째 이월)
- **#127**(약속 여정) — kimgyubi 착수 승인 상태

### 팀원 대기

- **#176** 한 줄 · **#180** rebase (둘 다 lami2342, **#180은 늦을수록 비싸진다**)
- **#181** 사진 8장 + 버튼 danger (kimgyubi)
- **#165** 계정 삭제 문구 (chacha)

---

## 11. ⚠️ 미결·주의

### ★ 배포를 안 했다 — main ≠ 서버

**`clov-api`의 CD 트리거는 `deploy-*` 브랜치다. main 머지는 배포가 아니다.**

```
main  b82b507  ← 제목 40 + burgerOldman 테스트
  ├ deploy-myeongjun   FF 가능 (뒤처짐)
  ├ deploy-lami        FF 가능
  ├ deploy-kimgyubi    FF 가능
  └ deploy-chacha      ⚠️ 갈라짐 (squash 전 원본 커밋 3건)
```

**어느 서버에도 제목 40이 안 올라갔다.** `clov-web`은 CD가 아직 없어서(#182 미머지) main 머지가 서버를 안 바꾼다 — 그래서 #185를 먼저 머지해도 안전했다. **다만 프론트를 빌드·배포하기 전에 API 배포가 먼저 나가야 한다.**

`deploy-chacha`용 머지 커밋은 로컬 브랜치 **`deploy-chacha-sync`**에 만들어 뒀다(main과 내용 동일, force 아님).

```bash
git -C C:/gov/project/clov-api push origin origin/main:deploy-myeongjun
git -C C:/gov/project/clov-api push origin origin/main:deploy-lami
git -C C:/gov/project/clov-api push origin origin/main:deploy-kimgyubi
git -C C:/gov/project/clov-api push origin deploy-chacha-sync:deploy-chacha
```

> **★ `deploy-*` 브랜치는 절대 지우지 않는다.** 학원 프로젝트가 끝나면 **각자 그 브랜치로 개인 프로젝트를 이어간다**(사용자 확인 2026-07-31). 개인 작업이 쌓이기 시작하면 FF 대신 `git merge origin/main`으로 간다.

### 권한 — Claude가 못 하는 것

- **`gh pr merge`** · **`deploy-*` push** 둘 다 자동 승인 분류기에 차단된다 → **사용자가 직접 실행**
- 권한을 푸는 `update-config` 스킬 호출 **자체도 차단**된다. 풀려면 `.claude/settings.local.json`의 `permissions.allow`에 직접 추가(예: `"Bash(gh pr merge:*)"`)

### 코드 곁가지 (별건 후보)

- **`Header.jsx:108`** — `{wallet.data && wallet.data.balance.toLocaleString()}`. `wallet.data`만 확인하고 `balance`는 안 본다. **없으면 헤더가 앱 전체를 죽인다**(ErrorBoundary 없음). 오늘 검증용 스텁이 우연히 밟아 확인됐다
- **`clov_logo.png`가 1,043 KB** — 오늘 줄인 마스코트(256KB)보다 4배 크고 **모든 화면에 뜬다**
- **`.claude/launch.json`에 `clov-test-web-design-root`(8899)를 추가**했다. 디자인 레포 프로토타입 검증용

### 이월

- **`work-logs/2026-07-28-개발-배포-환경분리-검토.md`가 아직 untracked** (사흘째)
- **`dckr_pat_` 디렉터리** — 프로젝트 루트에 토큰 값이 이름인 빈 디렉터리. **삭제 + 토큰 로테이트 필요**(GHCR로 가면 아예 안 쓴다)
- **8080 방화벽** — SOCKS5 스캐너 프로브가 이미 들어왔다. 다운타임 0
- **certbot 갱신** — 인증서 10/26 만료
- **피넛 마스코트** — 초안·HTML이 프로젝트 루트에 untracked. 형태 확정 후 버거노인과 같은 방식으로 정리

---

## 12. 집에서 시작할 때 첫 명령

```bash
git -C clov-api checkout main && git -C clov-api pull
git -C clov-web checkout main && git -C clov-web pull
git -C web-design-repository checkout main && git -C web-design-repository pull
```

레포 커밋·열린 PR은 계속 바뀌므로 이 문서의 번호를 믿지 말고 `gh pr list`·`gh issue list`로 확인할 것. **오늘도 이슈 3건·PR 5건이 늘었다.**
