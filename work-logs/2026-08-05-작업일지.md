# 2026-08-05 작업일지 — 골드가 안 돌던 이유를 끝까지 따라간 날

> **한 줄 요약**: 어제 핸드오프가 *"유일한 실질 블로커"*라고 적어둔 `clov-api#92`를 풀었다. **6일째 멈춰 있던 이유는 제가 남긴 `CHANGES_REQUESTED`였고**, 어제 핸드오프의 *"GitHub은 이걸 막은 적이 없다"*는 07-31 06:34 이전까지만 맞는 말이었다.
>
> 요청 6건을 리더가 직접 반영하다가 **계약을 전수 검토하게 됐고, 여덟 군데가 걸렸다.** 그중 넷을 개정했다 — 특히 **`#92`가 그대로 머지됐어도 추억피드 글 작성은 여전히 0골드**였다는 것을 구현 직전에 알았다.
>
> 반영 커밋에서 **회귀를 하나 냈다.** `ESCAPE '\'`가 MySQL 구문 오류라 골드 경로 전체가 500이 됐고, 로컬 컴파일 3회 통과·CI 3회 실패로 끝났다.
>
> **다음에 뭘 할지**는 [`2026-08-05-핸드오프.md`](2026-08-05-핸드오프.md)에 있다.
> 앞선 맥락: [`2026-08-04-핸드오프.md`](2026-08-04-핸드오프.md) · [`2026-08-04-작업일지.md`](2026-08-04-작업일지.md)

---

## 1. 아침에 확인한 것 — 핸드오프 5건 중 3건이 이미 끝나 있었다

어제 핸드오프 §0의 "내일 아침 이것부터" 목록을 실제 git 상태와 대조했다.

| 핸드오프 항목 | 실제 |
|---|---|
| clov-web 미배포 4커밋 배포 | ✅ **이미 배포됨** — `deploy-myeongjun`이 `7d51b6b`까지 나가 있었다 |
| PR 셋 머지(`clov-web#280`·`clov-api#134`·`web-design#61`) | ✅ **셋 다 머지** — 08-04 17:36~17:37, **핸드오프를 쓴 직후**에 들어갔다 |
| `clov-api#92` | ⬜ 여전히 OPEN. 07-30 생성, **6일째** |
| 동기 피드백 수거 | ⬜ `피드백` 라벨 이슈 **0건** |
| `personal_invite_code` step2 | ⬜ 남음. 다만 **사전조건 충족** — `#133`이 `deploy-myeongjun`에 들어가 있다 |

> **★ 핸드오프는 쓰는 순간 낡기 시작한다.** 세 건이 문서를 쓴 뒤 몇 분 안에 처리됐다. **문서의 번호를 믿지 말고 `gh pr list`·`git log origin/deploy-*..origin/main`으로 확인하라**는 그 문서 자신의 마지막 문장이 맞았다.

그 사이 chacha1650a가 오늘 아침 08:57~09:28에 clov-web PR 3건을 머지했고(`#284`·`#287`·`#290`) `#289`를 새로 올렸다. **어제 핸드오프의 *"멈춰 있는 건 사람이 아니라 오래된 PR이다"*가 그대로 이어졌다.**

---

## 2. ★★ `#92`가 6일간 멈춘 진짜 이유 — 어제의 진단도 반만 맞았다

어제 작업일지 §8은 이 PR의 정체 원인을 두 번 틀리게 진단한 것을 기록했다(머지 충돌 → 승인 대기). 그리고 룰셋을 열어 **"GitHub은 이걸 한 번도 막은 적이 없다"**로 결론냈다.

오늘 다시 열어보니 **그것도 반만 맞았다.**

```
mergeable = MERGEABLE      (충돌 없음)
build     = SUCCESS        (07-30부터 통과)
리뷰 스레드 = 0개           (미해결 없음)
상태       = BLOCKED        ← 그런데 BLOCKED
```

```json
"required_approving_review_count": 0,
"required_review_thread_resolution": true,
"dismiss_stale_reviews_on_push": false     ← 어제 안 본 줄
```

체크도 스레드도 깨끗한데 `BLOCKED`이므로, 남은 원인은 **`CHANGES_REQUESTED` 리뷰 2건**이다(07-31 06:34 · 08-04 02:18).

> **★ 어제는 `required_approving_review_count: 0`만 보고 "리뷰는 게이트가 아니다"로 읽었다.** 승인 **개수** 요구가 0인 것과, **변경 요청이 머지를 막는 것**은 다른 규칙이다. 룰셋을 열긴 했는데 **필요한 줄을 안 봤다.**
>
> 정확히 하면 — **07-31 06:34 이전까지는 실제로 아무것도 막지 않았다.** 첫 `CHANGES_REQUESTED`가 그 시각에 들어갔다. **어제 진단은 시점을 안 붙여서 틀린 문장이 됐다.**

**그리고 `dismiss_stale_reviews_on_push: false`가 결정적이었다.** 작성자가 6건을 다 고쳐 푸시해도 리뷰는 자동으로 안 풀린다. **리더가 새 리뷰를 올려 덮어야만 열린다.**

> **★ "요청만 처리되면 바로 머지된다"가 아니었다.** 어제 핸드오프에 그렇게 적혀 있었는데, 실제로는 **작성자가 할 수 있는 일이 끝나도 리더가 한 번 더 움직여야** 하는 구조였다. 그걸 모르면 양쪽이 서로를 기다린다.

---

## 3. ★★★ `#92`가 그대로 머지됐어도 증상은 안 없어졌다

리더 질문은 이것이었다.

> 마스코트를 눌렀는데도 골드가 안 들어왔고 글 작성도 했는데 골드 지급이 안 됐다.

`main`에 `EARN_*`이 0건이라 **모든 경로가 0**인 건 어제 확인한 그대로다. 그런데 구현을 이어받으려고 `#92`의 diff를 읽다가 하나 더 나왔다.

```java
// #92 MemoryService.java
// 약속 연결 추억(createFromPlan) 등록 1건당 지급 골드(계약 §15-4). FREE MEMORY(createFree,
// plan_id NULL)는 지급 대상이 아니다 — 약속·완료·다른 참여자 없이 혼자 무한히 쓸 수 있어서
// 캡이 없는 지급을 걸면 그대로 무한 골드가 된다.
private static final long MEMORY_WRITE_GOLD = 50;
```

**`#92`는 `createFromPlan`에만 지급을 걸고 `createFree`를 명시적으로 제외한다.**

프론트를 따라가면 갈리는 지점이 보인다.

```
useCreateMemory.js:12   planId ? createPlanMemory : createMemory

약속 선택함  → POST /plans/{id}/memories   → createFromPlan()  → 50골드
약속 없음    → POST /rooms/{id}/memories   → createFree()      → 0골드   ← 추억피드 자유 글
```

> **★ "블로커를 머지하면 풀린다"가 성립하지 않았다.** `#92`를 원안대로 머지했으면 마스코트는 돌기 시작하고 **추억피드 글은 여전히 0**이라, 증상이 절반만 사라진다. **그러면 다음 진단은 훨씬 어려워진다** — "골드가 아예 안 된다"보다 "어떤 글은 되고 어떤 글은 안 된다"가 재현이 까다롭다.
>
> **막힌 PR을 풀 때 그 PR이 원래 무엇을 고치기로 했는지 다시 읽어야 한다.** 요청 6건은 값과 캡에 관한 것이었고, **지급 범위가 증상과 안 맞는다는 건 그 목록에 없었다.**

`#92`의 판단이 틀린 것도 아니었다 — *"혼자 무한히 쓸 수 있다"*는 정확한 진단이다. 다만 계약은 그 위험을 **"제외"가 아니라 "사유 분리 + 횟수 캡"**으로 막는 쪽으로 결론나 있었다(어제 `web-design#53`). **구현이 계약보다 앞서 만들어졌고, 계약이 바뀐 뒤 따라가지 않았다.**

---

## 4. ★★★ 계약 전수 검토 — 여덟 군데

리더가 *"계약을 전체 검토하고 수정해야겠다"*고 해서 골드 관련 절을 전부 다시 읽었다. 코드·DB 실측과 대조했다.

### A. 노력 순서 원칙이 총량에서 깨져 있었다

§15-4가 스스로 이렇게 썼다 — *"금액이 노력 순서를 어기면 사용자가 가장 성의 없는 경로만 반복하게 된다."*

**건당 금액만 고쳤고 하루 총량은 안 고쳤다.**

| 행동 | 건당 | 하루 최대 | 상한 3,000 대비 |
|---|---:|---:|---:|
| 마스코트 교감(클릭 한 번) | 200 | **2,000** | **67%** |
| 자유 추억(글 작성) | 200 | 600 | 20% |
| 약속 연결 추억 | 300 | 캡 없음 | — |

**클릭 10번이 하루 예산의 3분의 2였다.** 개정 전(100 × 3회 = 300 / 상한 500)에는 60%였으니 **개정 후 오히려 늘었다.**

> **★ 순서를 정렬할 때는 단가만 정렬하면 안 된다.** 사용자가 마주하는 것은 총량이다. 08-04 개정은 표의 세 줄을 정렬해놓고 **그 옆 칸(하루 최대)을 안 봤다.**

### B. 추억 생성 응답에 `earnedGold`가 없었다

§12-1 마스코트 응답에는 있다.

```jsonc
{ "expDelta": 2, "earnedGold": 200, "remainingToday": 8, ... }
```
> **`0`이 정상 응답이다.** 화면은 이 값으로 판단한다(`earnedGold > 0`일 때만 골드 획득 연출).

**추억 생성 응답에는 계약에도 없고 `#92`도 안 넣는다.** `#92`가 바꾼 DTO는 `MascotInteractionResponse` 하나뿐이었다.

> **★ 이게 리더가 겪은 증상의 나머지 절반이다.** 지급이 0인 이유가 넷인데(상한 초과 · 횟수 초과 · 본문 미달 · **기능이 없음**) **화면에 전부 똑같이 보인다.** 원장 조회 API도 없어서 확인할 다른 경로가 없다.
>
> **지급 코드를 넣어도 이 침묵은 그대로 남는다.** 그래서 계약 §10에 신설했다.

### C. `EARN_MEMORY`의 "자연 상한"은 상한이 아니었다

계약 표는 자체 제한을 *"약속에 연결된 추억만(약속당 1개라 자연 상한)"*, 하루 최대를 **`—`**로 적었다.

**약속은 무제한으로 만들 수 있다.**

```
PlanService     일일 캡 상수 없음
complete()      날짜 전제조건 없음
```

만들기 → 완료 → 작성 한 바퀴가 300골드다. **총 상한이 없으면 카탈로그 52,000이 약 170바퀴, 십몇 분에 비워진다.** 표기가 *"여긴 캡이 필요 없다"*로 읽혔다.

### D~H. 나머지

| | |
|---|---|
| **D** 골드 지점이 XP 지점의 5분의 3 | 약속 생성·완료·이미지 보너스에 대응 골드가 없다. **핵심 흐름의 마지막 한 칸에만 걸려 있다** |
| **E** 원장 조회 API 없음 | 오늘 이 진단에 `git grep`이 필요했던 이유 |
| **F** `ADMIN_GRANT` 지급액 가이드 없음 | 60,000 > 카탈로그 52,000 |
| **G** `SKIN`·`EVENT` 0종 | 상점 탭 3개 중 2개가 빈 화면 |
| **H** `discountRate` 미정 | 07-30부터 6일째 |

**A·B·C를 개정했고**(`web-design#63`) **D·E·F·G·H는 이슈로 뗐다**(§8).

---

## 5. 개정한 값과 근거

리더 확정으로 이렇게 정했다.

| | 개정 전 | 개정 후 |
|---|---|---|
| `EARN_MEMORY_FREE` | 200 × 하루 **3회** | 200 × 하루 **10회** + **본문 20자 이상** |
| 하루 총 상한 | **3,000** (완주 17일) | **6,000** (완주 9일) |
| `EARN_MEMORY` 자체 제한 | "자연 상한" · 하루 최대 `—` | "횟수 캡 없음" · **총 상한까지** |
| 추억 생성 201 응답 | — | **`earnedGold`** 신설 |
| 마스코트 건당 | 200 | **200 유지**(리더 판단) |

### ★ 본문 20자 조건이 왜 필요했나

리더가 *"하루 3번은 너무 적으니 10번으로"*라고 했을 때, 올리기 전에 확인한 것이 있다.

```java
// CreateMemoryRequest
@NotBlank @Size(max = 40) String title,   // 제목만 필수
@Size(max = 100) String content,          // 최소도 @NotBlank도 없다 — 비어도 통과
```

**제목 한 글자짜리 글도 정상 저장된다.** 3회(600골드)일 땐 감수할 만했는데 **10회면 `"ㅇ"` 열 번에 2,000골드**다.

> **★ 캡을 올릴 때는 그 캡이 무엇을 세고 있는지 다시 본다.** 횟수 캡은 "몇 번 했나"만 세지 "그게 무엇이었나"는 안 본다. **자유 추억을 사유로 분리한 근거 자체가 *"혼자 무한히 쓸 수 있다"*였는데, 횟수만으로는 그 걱정이 안 막힌다.**

**XP는 조건 없이 그대로 주고 골드만 조건부로 걸었다.** 20자 미만이어도 글은 정상 생성되고 `201`이 나간다 — 계약이 이미 못박은 *"글쓰기를 막는 건 골드 캡이 할 일이 아니다"*와 같은 원칙이라 **`400`을 만들지 않았다.**

### ★ 마스코트 200 유지가 20자 조건과 맞물렸다

A에서 클릭 200 = 글 200이 문제라고 짚었고 100으로 내리는 안을 냈는데, **리더가 200 유지를 택했다.**

> 그 선택이 성립하는 이유가 20자 조건이다 — **노력 차이를 금액이 아니라 게이트가 만든다.** 자유 추억에는 20자 관문이 붙고 마스코트에는 안 붙는다. **이 게이트를 없애면 두 값을 다시 갈라야 한다**고 계약에 적어뒀다.

### ★ 상한 6,000의 근거는 "경로별 캡의 합"이었다

```
마스코트   200 × 10 = 2,000  (자체 캡)
자유 추억  200 × 10 = 2,000  (자체 캡)
                    ───────
                      4,000  ← 상한이 3,000이면 여기서 이미 초과
약속 완주  300 × N        0  ← 예산이 안 남는다
```

**상한이 4,000 이하면 약속 완주가 예산에서 통째로 밀려난다.** 08-04 개정이 바로잡은 노력 순서가 **반대 방향으로 다시 깨진다.**

⚠️ **이 개정은 §15-4의 "카탈로그를 먼저 본다" 가드레일에 걸린다** — 카탈로그가 52,000에 멈춘 채 획득량만 2배로 올렸다. **알고 넘겼고 근거를 계약에 적었다**: 지금은 상한을 조율하는 국면이 아니라 **골드가 아예 안 도는 상태를 푸는 국면**이다.

> **★ 어제와 오늘의 일수 변동은 성격이 다르다.** 08-04의 24 → 12 → 17은 **상한을 안 건드리고 카탈로그만 움직여서** 나왔고, 오늘의 17 → 9는 **카탈로그가 멈춘 채 상한을 올려서** 나왔다. 계약의 일수 절 제목을 그렇게 고쳤다 — **같은 숫자 변화라도 무엇이 움직였는지가 다음 판단을 가른다.**

---

## 6. ★★★ 회귀를 하나 냈다 — 로컬 컴파일 3회 통과, CI 3회 실패

리뷰 요청 ①(`EARN_` 접두사 합산)을 반영하며 이렇게 썼다.

```sql
WHERE reason LIKE 'EARN\_%' ESCAPE '\'
```

**MySQL 구문 오류다.** 문자열 리터럴 안의 백슬래시를 이스케이프 문자로 **먼저** 해석해서 `\'`가 닫는 따옴표를 이스케이프한다.

### 증상이 컸다

`sumEarnedToday`는 `earnGold`의 **첫 쿼리**라, 이게 깨지면서 **추억 생성·마스코트 교감이 전부 `500`**이 됐다.

```
473bbba   10건 실패   ← 그중 7건은 골드와 무관한 기존 테스트(ExpIntegrationTest 3건 포함)
884e8ad    4건 실패   ← SQL은 고쳐졌고 남은 건 파급
04340a1    success
```

### ★ 로컬 통과가 무증거인 범위가 정확히 드러났다

`compileJava`·`compileTestJava`는 **세 번 다 통과했다.** CI는 세 번 다 다른 것을 잡았다.

| 라운드 | CI가 잡은 것 | 왜 로컬이 못 봤나 |
|---|---|---|
| 1 | MyBatis XML의 SQL 구문 | **컴파일러가 XML 안의 SQL을 안 본다** |
| 2 | 테스트 DB의 FK 위반 | 실제 DB가 있어야 난다 |
| 2 | 테스트 기댓값 산술 | 실행해야 난다 |

> **★ 이건 08-04 §10의 "에러가 안 나는 결함"과 반대 종류다.** 조용히 틀리는 게 아니라 **요란하게 죽는다.** 그래서 **통합 테스트가 있으면 반드시 잡힌다** — 문제는 로컬에 Docker가 없어 Testcontainers를 못 돌린다는 것이고, **그동안 CI가 유일한 방어선**이다.
>
> **"로컬에서 됩니다"가 무증거인 종류가 하나 더 늘었다**(08-04 §15의 R2 CORS에 이어). 이번엔 환경이 아니라 **도구가 보는 범위**였다.

### 계약에도 같이 박았다

§15-4가 `reason LIKE 'EARN\_%'`를 **그대로 처방하고 있었다.** 그 형태로 구현하면 누구든 같은 함정에 빠진다. `'!'`로 못박고 이유를 적었다.

```sql
WHERE reason LIKE 'EARN!_%' ESCAPE '!'
```

> **★ 계약이 SQL 조각을 처방할 때는 그게 실제로 도는 형태여야 한다.** 08-04 §15의 *"계약의 경고는 검증 가능한 형태로 적는다"*를 지켜서 `reason LIKE ...`를 박아넣었는데, **박아넣은 그 코드가 안 돌았다.** 검증 가능한 형태로 적는 것과 검증된 형태로 적는 것은 다르다.

---

## 7. ★★ 남은 4건은 전부 "자유 추억이 지급 대상이 됐다"의 파급이었다

### `ExpIntegrationTest` 3건 — `users` DELETE가 FK에 걸렸다

본문 20자 이상인 테스트가 이제 `user_wallets` 행을 남기는데, 그 클래스의 `cleanUp`은 `refresh_tokens`만 지우고 `users`를 지운다.

**`MemoryIntegrationTest`에는 이미 같은 정리가 들어 있었다** — 약속 연결 추억이 지급을 시작했을 때 chacha가 겪은 자리다.

> **★ 같은 파급이 한 겹 더 번졌다.** "추억 등록이 골드를 준다"가 처음 들어올 때 한 클래스를 고쳤는데, **"자유 추억도 준다"가 들어오자 다른 클래스가 걸렸다.** 지급 경로를 넓힐 때는 **그 경로를 타는 테스트를 전수로 세야 한다.**

### `RoomIntegrationTest` 1건 — 내 수정 누락

교감 캡을 3 → 10으로 올리며 루프 횟수와 `exp-logs` 개수는 고쳤는데 **`expPoint` 단언(6)을 안 고쳤다.** 10회 × 2 XP = 20이다.

### ★ 우연히 통과하던 두 곳

전수 확인을 돌렸다.

```
users DELETE 하는 테스트 14개 중 골드 경로를 타는 것 5개
  Memory · Exp · Room · Comment · NotificationFanOut
```

`CommentIntegrationTest`(본문 `"Body"`)와 `NotificationFanOutIntegrationTest`(본문 `"짧은 추억"`)는 **본문이 20자 미만이라 지급이 안 일어나서 통과하고 있었다.** 본문을 한 글자만 늘려도 조용히 깨진다.

> **★ 통과가 우연인 테스트는 통과가 아니다.** 다섯 다 지갑 정리를 넣었다. 08-04 §15의 *"제거 작업의 완료 판정은 레포에 0건"*과 같은 계열 — **끝났다고 판정할 때는 조건 없는 전수 검색을 다시 돌린다.**

---

## 8. 이슈 정리 — 7건 중 죽은 건 1건

세 레포 열린 이슈를 전수로 읽고 실제 상태와 대조했다.

### 닫음 — `clov-api#94`(팀원별 CI/CD)

요청했던 세 명의 VM 준비가 **전부 끝나 있었다.**

```
             clov-api                    clov-web
chacha       3회 · 최근 08-03 success     4회 · 최근 08-03 success
kimgyubi     4회 · 최근 08-03 success     1회 · 최근 08-03 success
lami         2회 · 최근 07-30 success     5회 · 최근 08-05 success
myeongjun   10회 · 최근 08-04 success    18회 · 최근 08-04 success
```

**실패로 끝난 실행이 하나도 없다.** `authorized_keys` 주의사항은 참조용으로 남는다고 적고 닫았다.

### 살아 있음 — 5건, 그중 하나는 범위가 늘었다

| | 판정 |
|---|---|
| **`web-design#42`** | ✅ 유효하고 **범위가 늘었다** — DDL 주석에 `MEMBER_JOINED`뿐 아니라 어제 신설된 **`MEMBER_LEFT`도 빠져 있다.** 계약·백엔드·프론트 세 겹이 맞았는데 주석만 안 따라왔다 |
| **`clov-web#271`** | ✅ 유효. `#274` 리팩터가 안 고쳤다 — `signup-message` div가 `step === 0` 블록에만 있고 STEP 2엔 없다 |
| **`clov-api#108`** | ✅ 유효. 배포 헬스체크 부재 |
| **`clov-api#89` · `clov-web#177`** | ✅ 유효하나 **본문이 빈 템플릿**이다. 각각 `#92`·`#178`의 원 이슈라 내용은 PR에 있다 |
| **`clov-api#72`** | ✅ **리더 판단으로 닫았다** — 아래 |

### ★ 같이 닫음 — `clov-api#72`(개인 최종 배포)

처음엔 안 닫았다. **07-27 로그에 *"Docker 통합 테스트와 스모크 테스트가 남아 `OPEN`으로 유지"*라고 의도적으로 열어둔 기록**이 있어서, 그걸 뒤집는 건 리더 판단이라고 봤다. 리더가 닫기로 했다.

9일 지나 보니 **남은 둘 다 이 이슈에서 처리할 수 있는 성격이 아니었다.**

| 미체크 항목 | 판단 |
|---|---|
| Docker/Testcontainers 로컬 실행 | **이 이슈에서 못 푼다.** 로컬에 Docker가 없고, **그 역할은 CI `build`가 이미 하고 있다** — 오늘 §6에서 로컬 컴파일 3회 통과를 CI가 3회 다 잡은 게 그 증거다 |
| 핵심 기능 스모크 테스트 | **일회성 항목이 아니게 됐다.** CI/CD로 넘어가면서 매 배포 확인 사항이 됐고 방법도 정착했다(`curl` → 401, 번들 문자열 검색) |

> **★ "체크박스가 남았으니 열어둔다"가 성립하지 않는 경우가 있다.** 이 둘은 **달성해야 할 일이 아니라 이 이슈의 방식이 대체됐다는 신호**였다. 수동 배포 절차 자체가 `#93`·`#94`로 넘어갔고, `#94`도 오늘 닫혔다.

⚠️ **본문 인코딩이 깨져 있다**(한글이 전부 `?`). 실제 기록은 `2026-07-27-myeongjundev-개인-GCP-최종배포.md`(커밋 `418a0ae`)에 온전히 있고, 닫는 코멘트에 그 포인터를 남겼다 — **새 VM을 붙일 때 이슈를 다시 열지 않게 하려는 것이다.**

### 새로 뗀 5건

계약 검토에서 이번에 안 고친 것들이다.

| | |
|---|---|
| `web-design#64` | 약속 생성·완료에 골드 사유가 없다 — `EARN_PLAN` 확정 |
| `web-design#65` | `ADMIN_GRANT` 지급액 가이드 |
| `web-design#66` | `discountRate` 운영 주체·주기 |
| `clov-api#135` | 원장 조회 API(`GET /shop/transactions`) |
| `clov-web#303` | 상점 빈 탭(`SKIN`·`EVENT`) |

각 이슈에 **왜 지금 그 상태가 됐는지**와 **이미 계약에 결론이 있는 부분**을 구분해 적었다. 예를 들어 빈 탭은 *"`RETIRED` 11종을 내리고 `BACKGROUND`를 새로 만든 결과"*이고, `ADMIN_GRANT`가 상한 합산에서 빠지는 것은 이미 확정된 설계다.

---

## 9. `clov-web#289` 사후 리뷰 — 근거가 자기를 반박했다

lami2342가 오늘 00:27에 열고 **7분 만에 본인이 셀프 머지**했다(룰셋상 승인 0건이라 GitHub은 허용).

**CSS 수정 자체는 정확했다.** 캐스케이드를 대조했다.

```
@scope 내부  :207  .letter-write-card                 (0,1,0)
@scope 밖    :938  .proto-letters .letter-write-card  (0,2,0)   ← 특이도로 이긴다
```

특이도는 스코프 근접성보다 **먼저** 비교되므로 안전하다. `box-sizing: border-box`도 필수가 맞고(`padding 32+32` + `border 1+1` = 66px), `margin: 24px 0`을 2-value로 쓴 것도 맞다.

### ★ 문제는 소스에 박힌 12줄짜리 정책 주석이었다

주장은 *"이 파일의 `@scope` 내부 규칙은 사용자 환경에서 반영이 안 된다"*이고, 근거로 `Letters.jsx:136` 주석을 달았다. **그 주석도 같은 주장을 근거 없이 단언하고 있을 뿐이다** — 순환 참조다.

**그리고 그 수정 자체가 반증이다.**

```css
@scope (.proto-letters) {            /* 1~926줄 */
  .letter-tab-container { max-width: 900px; padding: 0 20px; }   /* 정렬 기준선 */
  .letter-write-card { padding: 28px 32px; border: 1px; background: ...; }
}
```

`width: 100%`가 `.letter-stage`와 같은 오른쪽 끝을 만드는 것은 **`.letter-tab-container`가 먹기 때문이다.**

> **★ `@scope`가 정말 안 먹는다면 그 카드는 배경도 테두리도 패딩도 없는 맨 텍스트여야 하고, 이번 수정도 성립하지 않는다. 화면이 멀쩡하다는 사실이 곧 `@scope`가 적용된다는 증거다.**

관측된 현상(*파일·CSSOM은 최신인데 `getComputedStyle`은 옛 값*)은 **HMR 스타일시트 이중화**의 전형적인 증상이다.

세 가지를 더 짚었다 — **원인 수정과 파일 전역 정책이 한 PR에 있는 것**(어제 `#243`에서 막은 것과 같은 형태), **이슈가 명시적으로 배제한 도구로 검증한 것**(`#286`이 *"자동화 브라우저의 `getComputedStyle`만 믿지 말 것"*이라고 적었는데 다시 그걸로 검증), **정책이 반만 적용된 것**(`:910`·`:911`은 여전히 `@scope` 안).

---

## 10. 오늘 배운 것

- **★★★ 핸드오프의 "블로커를 풀면 된다"를 그대로 믿지 않는다** — `#92`를 원안대로 머지했으면 마스코트만 돌고 추억피드 글은 0골드로 남아, **증상이 절반만 사라져 다음 진단이 훨씬 어려워졌다**(§3). 막힌 PR을 풀 때는 **그 PR이 무엇을 고치기로 했는지와 지금 증상이 맞는지**를 다시 본다
- **★★★ 계약이 SQL 조각을 처방하면 그게 실제로 도는 형태여야 한다** — §15-4가 `reason LIKE 'EARN\_%'`를 박아뒀는데 **MySQL에서 구문 오류였다**(§6). "검증 가능한 형태로 적는다"와 "검증된 형태로 적는다"는 다르다
- **★★★ 로컬 컴파일이 무증거인 범위** — MyBatis XML의 SQL·테스트 DB의 FK·기댓값 산술. **컴파일러가 XML 안을 안 본다.** Docker가 없어 Testcontainers를 못 도는 동안 CI가 유일한 방어선이다(§6)
- **★★ 순서를 정렬할 때 단가만 정렬하지 않는다** — 08-04 개정이 노력 순서표 세 줄을 맞춰놓고 **옆 칸(하루 최대)을 안 봐서**, 클릭이 하루 예산의 67%를 차지했다(§4-A)
- **★★ 캡을 올릴 때는 그 캡이 무엇을 세는지 다시 본다** — 횟수 캡은 "몇 번 했나"만 세고 "그게 무엇이었나"는 안 본다. 3 → 10으로 올리는 순간 **제목 한 글자짜리 글 열 개가 2,000골드**가 됐다(§5)
- **★★ 지급 경로를 넓히면 그 경로를 타는 테스트를 전수로 센다** — "자유 추억도 골드를 준다" 하나가 **테스트 클래스 다섯에 지갑 정리를 요구했고, 그중 둘은 본문이 우연히 짧아서 통과하고 있었다**(§7)
- **★★ 룰셋을 열었어도 필요한 줄을 봐야 한다** — 어제 `required_approving_review_count: 0`만 보고 "리뷰는 게이트가 아니다"로 읽었는데, **`CHANGES_REQUESTED`가 막는 것과 승인 개수 요구는 다른 규칙**이고 `dismiss_stale_reviews_on_push: false`가 결정적이었다(§2)
- **★★ 진단에는 시점을 붙인다** — *"GitHub은 이걸 막은 적이 없다"*는 **07-31 06:34 이전까지만** 맞았다. 시점 없는 단언은 다음 날 틀린 문장이 된다(§2)
- **★ 근거가 순환하면 근거가 아니다** — `#289`의 `@scope` 주장이 `Letters.jsx` 주석을 가리키고 그 주석도 같은 주장을 단언할 뿐이었다(§9). **그리고 그 수정 자체가 반증이었다**
- **★ 핸드오프는 쓰는 순간 낡는다** — 어제 §0의 5건 중 3건이 문서를 쓴 뒤 몇 분 안에 처리됐다(§1)
- **★ 통과가 우연인 테스트는 통과가 아니다** — 본문이 20자 미만이라 지급이 안 일어나서 통과하던 테스트 둘(§7)
- **★ 같은 숫자 변화라도 무엇이 움직였는지가 다르다** — 완주 일수가 24 → 12 → 17일 때는 카탈로그가 움직였고, 17 → 9는 상한이 움직였다. **계약 절 제목을 그렇게 갈랐다**(§5)

---

## 11. 상태

```
clov-api#92        04340a1   build=success · mergeable=CLEAN   ← 머지 대기
  dd50f2d  chacha 원본 (보존)
  473bbba  리뷰 6건 + 계약 개정값
  884e8ad  LIKE 이스케이프 수정 + 회귀 테스트
  04340a1  지갑 정리 누락 · 교감 XP 기댓값

web-design#63      7b4e65f   계약 개정 (열림)
```

**아직 골드는 안 돈다.** `#92` 머지 → clov-api 배포가 남았고, 그 뒤에도 **프론트가 `earnedGold`를 안 읽어서 화면은 조용하다**(`clov-web#178` 자리).

다음 순서는 [`2026-08-05-핸드오프.md`](2026-08-05-핸드오프.md)에 있다.
