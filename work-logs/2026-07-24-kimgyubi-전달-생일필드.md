# kimgyubi 전달 프롬프트 — 멤버 생일 필드 (clov-api #61 + 계약 §6)

> 대상: **kimgyubi1234** (Claude)
> 관련 이슈: clov-api [#61](https://github.com/Pickeslog/clov-api/issues/61) · clov-web [#85](https://github.com/Pickeslog/clov-web/issues/85)
> 리더 승인 완료(2026-07-24) — 승인 코멘트에서 결정된 형태를 그대로 담았다.

---

## 붙여넣을 프롬프트

아래 블록을 그대로 Claude에 붙여넣으면 된다.

````
멤버 생일 필드를 추가하는 작업이야. clov-api 이슈 #61이고 리더 승인이 끝났어.

## 작업 순서 — 이 순서를 반드시 지켜줘

1단계: web-design-repository (계약 문서) — PR 올리고 멈춤
2단계: 리더가 계약 PR을 머지할 때까지 대기
3단계: clov-api (구현) — PR

계약이 먼저 확정돼야 리뷰어가 그걸 기준으로 코드를 볼 수 있고,
내가 나중에 할 프론트 작업(clov-web #85)도 같은 필드명을 써야 해서 순서가 중요해.

---

## 1단계 — 계약 갱신 (web-design-repository)

작업 폴더: C:\gov\project\web-design-repository

⚠️ 이 저장소는 브랜치 보호가 없어서 main에 바로 push가 돼. 하지만 계약 문서는
팀 4명과 AI가 전부 이걸 기준으로 코드를 짜는 단일 기준점이라 리뷰 없이 바꾸면 안 돼.
반드시 브랜치를 파고 PR로 올려줘. main에 직접 커밋하지 마.

```
git checkout main
git pull origin main
git checkout -b docs/61-member-birthday
```

고칠 파일: docs/API-CONTRACT.md 의 §6 `GET /rooms/{roomId}/members` 응답 예시

현재:
```jsonc
{ "membershipId": "88", "userId": "1024", "nickname": "클로버", "profileImageUrl": null,
  "status": "ACTIVE", "statusMessage": "다이어트 중",
  "joinedAt": "2026-06-30T10:00:00", "leftAt": null }
```

여기에 `birthMonthDay` 한 필드를 추가한다:
```jsonc
{ "membershipId": "88", "userId": "1024", "nickname": "클로버", "profileImageUrl": null,
  "status": "ACTIVE", "statusMessage": "다이어트 중",
  "joinedAt": "2026-06-30T10:00:00", "leftAt": null,
  "birthMonthDay": "03-21" }
```

그리고 예시 아래에 이 설명을 함께 적어줘:

- `birthMonthDay` — 생일의 **월·일만** (`"MM-DD"`, 제로 패딩). 생일 미입력이거나 익명화(탈퇴) 계정이면 `null`.
- **연도를 빼는 이유**: 방 멤버 전원에게 생년(나이 추정 가능 정보)을 노출하지 않기 위해서.
- **서버가 "오늘 생일인가"를 계산해 주지 않는 이유**: 백엔드는 UTC 기준으로 동작해서
  한국 시각 00:00~09:00 사이엔 서버의 "오늘"이 아직 어제다. 월·일만 내려주고
  **브라우저가 사용자 로컬 날짜로 판단**하게 해서 이 문제를 원천 차단한다.

DB 스키마는 바꾸지 않아 (users.birthdate DATE NULL 이미 존재). 문서만 고치면 돼.

PR 제목: `docs: 멤버 응답에 birthMonthDay 추가 (계약 §6)`
PR 본문에 `관련: Pickeslog/clov-api#61` 링크를 넣어줘.

여기까지 하고 멈춰. 리더가 머지하면 알려줄게.

---

## 3단계 — 구현 (clov-api) ※ 계약 PR 머지된 뒤에 시작

작업 폴더: C:\gov\project\clov-api

```
git checkout main
git pull origin main
git checkout -b feat/61-member-birthday
```

이 저장소엔 `.claude/skills/clov-task/SKILL.md`가 있어서 작업 규칙(브랜치·PR·테스트·
INSERT 후 재조회 같은 것)이 자동으로 적용돼. pull 먼저 받아줘.

할 일:
- `GET /rooms/{roomId}/members` 응답에 `birthMonthDay` 추가
- **DB 스키마 변경 없음.** `room_members` 조회 쿼리에 `users` join만 추가하면 돼
- SQL에서 `DATE_FORMAT(u.birthdate, '%m-%d')`로 뽑으면 깔끔해.
  `birthdate`가 NULL이면 결과도 자동으로 NULL이라 따로 분기할 필요 없어
- RoomMember 응답 DTO에 필드 추가
- 통합테스트 추가

테스트에 **반드시 넣어야 할 케이스**:
- 생일이 있는 멤버 → `"03-21"` 형태로 나오는지 (제로 패딩 확인: 1월 5일이면 `"01-05"`)
- **생일 미입력 멤버 → `null`이 나오는지** ← 이게 이 필드의 핵심 리스크야.
  users.birthdate가 nullable이라 실제로 자주 발생해
- 두 멤버가 섞인 방에서 각각 맞게 나오는지

로컬 검증은 `./gradlew compileJava compileTestJava`까지만 해줘.
(로컬에 Docker가 없어서 통합테스트는 CI에서 돌아)

PR 본문 첫 줄에 `Closes #61` 꼭 넣어줘.

---

## 하지 말 것

- 계약에 없는 필드를 추가로 넣기 (예: `birthdate` 전체, `isBirthdayToday` 같은 것)
- 프론트(clov-web) 작업 — 그건 별도 이슈 #85, 이 API가 머지된 뒤에
- 마스코트 관련 (다른 팀원이 작업 중)
````

---

## 참고 — 왜 이 형태로 정했나

리더 승인 코멘트(clov-api #61)에 상세히 적혀 있다. 요약:

| 후보 | 채택 여부 | 이유 |
|---|---|---|
| `birthdate` 전체(`YYYY-MM-DD`) | ❌ | 생년 노출 = 나이 추정 가능 |
| `isBirthdayToday: boolean` | ❌ | 서버가 UTC라 KST 00~09시에 하루 어긋남 |
| **`birthMonthDay`(`MM-DD`)** | ✅ | 프라이버시 + 타임존 안전 + 기존 본인 생일 처리와 일관 |

프론트는 이미 본인 생일을 `getMe.birthdate`의 월·일 비교로 처리하고 있어서(`Dashboard.jsx`의 `isTodayBirthday`), 친구 생일도 같은 방식이 되어 코드가 한 갈래로 유지된다.

**주의**: 기존 `isTodayBirthday`는 `YYYY-MM-DD`를 파싱하므로 `MM-DD`에는 그대로 못 쓴다. 프론트(#85) 때 별도 헬퍼가 필요하다 — 이슈 코멘트에 코드까지 남겨뒀다.
