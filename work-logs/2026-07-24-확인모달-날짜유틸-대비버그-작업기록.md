# 작업기록 — 2026-07-24 (lami2342 / Claude 세션)

> 자체완결 요약. 담당: memory 도메인(lami2342) 세션이지만, 오늘은 리더 공지로 clov-web 공통 작업(날짜유틸·확인모달·대비버그)에 참여.

---

## 1. 현재 상태 (한 줄)

- **clov-web main = `5b4f0bd`** (오늘 작업분 전부 머지 완료) · **clov-api main = 배포 준비 단계**(`#56` 이후 안정)
- **오늘 만든 이슈 4개(`#88`·`#95`·`#98` + 팀 공지 `#79`) 전부 PR 머지 완료.** 열린 PR 없음.

## 2. 오늘 한 것

1. **프로젝트 현황 재확인 + 메모리 정정** — 기억해둔 이슈 #22(memory §10)는 이미 CLOSED, main은 그새 배포 준비(`#56`)까지 진행된 걸 확인. 2일 전 스냅샷 메모리가 크게 낡아 있어서 갱신.
2. **로컬 백엔드/프론트 직접 기동해 확인** — `clov-api`(8080)·`clov-web`(5173) 둘 다 preview로 띄워 정상 동작(로그인 화면, `/api/v1/rooms` 401) 확인.
3. **목업 vs 실제 구현 갭 분석 (6개 영역 병렬)** — 인증/프로필, 디자인시스템/모달, 대시보드/방/초대, 추억피드, 편지/알림, 일정/데이터레이어. 핵심 발견:
   - 공유 모달/디자인 토큰 시스템 부재, 파괴적 액션이 전부 `window.confirm`
   - 초대 딥링크(`/join/:code`) 라우트 미등록으로 깨짐
   - 알림 `payload` 필드가 백엔드에 없어서 프론트가 죽은 경로를 읽는 중
   - 마스코트(크로비/롭) 캐릭터가 실제 화면에 전혀 없음(이후 팀에서 `#90`으로 해결됨)
4. **`clov-web #88` — 공유 ConfirmDialog 신설 (PR #91, 머지)**
   - `src/components/ConfirmDialog/`(Context·useConfirm 훅·Provider·UI) 신설, `App.jsx`에 마운트, 계정 탈퇴(`Settings.jsx`)에 적용
   - **작업 중 실수 발견·수정**: `useConfirm()`을 `Settings`가 아니라 실제 하위 컴포넌트 `SettingsBody`에 넣어야 했는데 처음엔 상위에 넣어서 전역 `window.confirm`으로 조용히 fallback될 뻔함 — `eslint no-unused-vars`가 잡아줌
   - 이후 팀원이 `#93`(PR #94)으로 나머지 3곳(Dashboard/RoomList/Schedule)까지 롤아웃 완료 — 중복 작업 피하려고 main 먼저 확인하고 스킵함
5. **팀 공지 — 날짜·시각 공통화 (`#78`~`#81`)** — `#78`(kimgyubi, `lib/datetime.js` 신설) 머지 대기(백그라운드 폴링) 후 **`#79`(lami, 추억피드 D-day 중복 제거) 처리 → PR #84 머지**. `ddayLabel`/`ddayCaption`의 중복 계산부를 `ddayDiff()` 호출로 교체, 화면 문구는 원본과 동일 유지(리팩터링 원칙 준수).
6. **`clov-task` 스킬 디스커버리 확인** — `clov-web`/`clov-api` 양쪽에 팀이 추가한 작업절차 스킬(`#82`/`#60`)이 이번 세션 시작 시점엔 인식 안 됐던 걸 실제 `Skill()` 호출로 확인(`Unknown skill` 에러) → 세션 중간에 재확인했더니 이후 인식됨(스킬 목록은 세션 초기에 고정되는 것으로 추정, 원인 확정은 못 함).
7. **로그인해서 실제 화면 vs 목업 라이브 비교 (Dashboard)** — 사용자가 직접 로그인(자격증명은 제가 입력하지 않음, 정책상 금지)한 세션으로 대시보드를 열어 computed style까지 비교:
   - LP 배너·레벨바·커버카드·멤버카드·D-day 리스트·증거카드 구조는 목업과 거의 1:1 이식 확인(이전에 "없다"고 잘못 판단했던 LP 트랙 문구도 재확인해서 정정)
   - **진짜 버그 발견**: 섹션 제목·D-day 카드·"전체 피드 보기" 버튼이 카드 배경 없이 페이지 전체 배경(우드텍스처)에 바로 노출돼 다크모드 밝은 글자색의 대비가 부족. D-day 카드 배경도 `rgba(255,255,255,.05)`로 사실상 투명. D-day 뱃지는 임박도 무관하게 항상 같은 초록색.
8. **`clov-web #95` — 대시보드 D-day 섹션 대비·레이아웃 버그 수정 (PR #99, 머지)**
   - `.section-title-label`/action 버튼에 배경 칩 추가, `.schedule-banner`를 `var(--card-bg)`로 채움, `ddayUrgency()` 추가해 뱃지 색상(주황/초록/파랑) 구분
   - 사용자 지시로 D-day 그리드는 `auto-fit` 대신 **3열 고정 유지**
   - 다크/라이트 둘 다 실제 로그인 세션 + `getComputedStyle`로 검증(단, 첫 검증 때 `document.body.classList.add`로 수동 토글해서 오검증 — 실제 앱 경로(localStorage `clov_darkMode` → 새로고침)로 다시 검증해 확정)
9. **`clov-web #98` — 추억피드·일정계획 헤더 텍스트 대비 보정 (PR #100, 머지)**
   - `#95`와 동일 패턴이 `Feed.jsx`("월별 추억 아카이브")·`Schedule.jsx`("PROMISE JOURNEY 약속 여정")에도 있어서 배경 박스 추가
   - `Schedule.jsx`는 코드 주석상 원래부터 **다크모드 미지원(라이트 팔레트 고정)** 화면이라 라이트 배경만 추가 — 사용자가 "다크모드에서 안 바뀐다"고 지적했으나 확인 결과 이 화면 자체가 원래 다크모드를 안 타는 의도된 설계(다크모드 지원을 새로 넣으려면 색상 값 전체를 변수화해야 해서 별도 이슈 필요, 아직 미결정)

## 3. ★ 대기·주의

- `Schedule.jsx`(일정계획) 화면의 다크모드 지원 여부 — 사용자에게 "새로 넣을지" 물어본 상태, 답 대기.
- 팀이 병렬로 작업 중이라 이슈/PR 번호가 빠르게 소진됨(오늘 중 `#78`→`#103`까지 진행) — 새 작업 시작 전 **반드시 main pull + 관련 이슈 상태 재확인**(오늘 실제로 `#93`/`#90` 중복 작업을 이렇게 피함).

## 4. 환경 / 참조

- 로컬 dev: `preview_start name="clov-web-dev"`(5173) / `name="clov-api-dev"`(8080). 포트 충돌(고아 프로세스) 종종 발생 — `PowerShell Stop-Process`로 정리 후 재시작.
- 검증: `npm --prefix clov-web run lint && run build` 필수. 로그인 필요한 화면은 사용자가 직접 로그인한 세션에 이어서 `javascript_tool`로 computed style 확인(자격증명 입력은 절대 대신 하지 않음).
- 테마 토글 검증 시 `document.body.classList.add` 같은 수동 DOM 조작은 실제 앱 반영과 다를 수 있음 — **`localStorage.setItem('clov_darkMode', ...)` + `location.reload()`로 정식 경로 검증할 것.**
- SSOT: `docs/API-CONTRACT.md`, `screen-spec-source/`. 이슈 스타일은 `#78`(리더 작성) 참고 — 배경/할 일/범위 밖/검증/작업방법 구조.
