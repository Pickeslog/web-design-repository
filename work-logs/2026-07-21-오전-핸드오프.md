# 2026-07-21 오전 정리 — M2 R1 백엔드 팬아웃 · 프론트 착수 · main hotfix (핸드오프)

> **한 줄**: M2 도메인 배정 확정 → **R1 백엔드 5개 머지**(room·invite·letter·memory·notification) → **room 프론트 뼈대 머지·팀원 프론트 착수** → 도중 **main broken(별칭 충돌) hotfix**·notification 3연속 디버깅 돌파. 다음 = 팀원 프론트 PR 감사 + Codex plan/GET rooms.
> 상세 배경: [`2026-07-20-day-summary.md`](2026-07-20-day-summary.md) · 착수 프롬프트 [`2026-07-21-M2-착수-프롬프트.md`](2026-07-21-M2-착수-프롬프트.md) · 결정요약 [`2026-07-21-morning-m2-prep.md`](2026-07-21-morning-m2-prep.md)

## 1. 오전 완료

**M2 배정 확정 (리더 결정)**
- 슬라이스 형태 = **도메인별 혼합**(쉬운 도메인 세로 통째=비전공 / 어려운 도메인 백=리더 선행). lami2342=**Claude** 확정.
- FREE MEMORY 승인(`POST /rooms/{roomId}/memories`), lami=**memory 축소**(글+피드, 이미지/댓글 R2).
- 레지스트리 §2 담당표 **실명 매핑**, 계약 §10 FREE MEMORY·**§6 GET /rooms** 추가.

**R1 백엔드 이슈 7개(#15·#19~#24) → 5개 머지**
| 도메인 | PR | 담당 | 비고 |
|---|---|---|---|
| room §6 | #25 | 리더/Codex | 부모, 최우선 |
| invite §7 | #26 | 리더/Codex | 동시성. 버그: insert 후 재조회 안 함→status null→Claude fallback 수정 |
| letter §11 | #28 | chacha(Claude) | 모범적 |
| memory §10(축소) | #29 | lami(Claude) | 모범적, RoomService 재사용 |
| notification §13 | #30 | kimgyubi(Gemini) | 삽질 후 완주(아래 교훈) |

**프론트**
- **room 프론트 뼈대 PR#22 머지**(clov-web): RoomList(생성 랜딩)·Dashboard(`/rooms/:roomId`, letter/memory/plan 진입점)·router·api/room.js. 버그: react-query `isLoading`→**`isPending`**(retry 딜레이 크래시).
- 프론트 이슈(clov-web): #19 room·#20 letter(chacha)·#21 memory(lami)·**#24 notification(kimgyubi, 오후 착수)**.

**도구/문서**
- PR 감사 체크리스트 `team-guides/17-backend-domain-pr-review.md`.
- 코덱스 전달 프롬프트 `work-logs/2026-07-21-코덱스-전달-{room,invite,plan,rooms-list}.md` + `코덱스-재작업-plan.md`.

## 2. 현재 위치 (커밋 앵커)
- **clov-api main = `8f8e8d7`** (notification #30 머지 후). 별칭 hotfix `#33` 반영됨.
- **clov-web main** = room 프론트 뼈대(#22) 머지.
- **web-design main** = 계약 §6 GET /rooms·프롬프트 반영(push 완료).

## 3. 다음 할 일
1. **팀원 프론트 PR 감사** — chacha(letter #20)·lami(memory #21)·kimgyubi(notification #24). Dashboard 진입점(feed/letters/notifications 라우트)에 연결.
2. **Codex plan #27 재작업 마무리**(압축포맷→room 스타일 표준화 + 테스트 보강) → 재감사 → 머지. **재작업 전 머지 금지**.
3. **Codex GET /rooms**(브랜치 `feat/rooms-list`) PR → 감사 → 머지 → **Claude가 RoomList 목록 연동**(`getRooms` + 방 카드, 현재 "목록 준비 중" 자리).
4. **user #15**(백=Codex/프론트=Claude 세로) — 남은 리더 도메인, 후순위.

## 4. 열린 항목 / 교훈
- **★ main hotfix(#33) 교훈**: letter/memory가 각자 `domain/*/dto/UserSummaryResponse` 생성 → `type-aliases-package: com.korit.clovapi.domain` 전체 스캔 별칭 충돌 → 전 컨텍스트 붕괴. **stacked+독립 CI라 각 PR은 통과, 둘 다 머지된 main에서만 터짐**(main test 안 돌려 몰랐음). **공유 DTO는 `global/dto`(domain 밖)에** — 레지스트리/CONVENTION 명문화 권장(미반영).
- **notification 삽질사**: ① build.gradle `spring-boot-starter-test`=오진(SB4는 모듈식 test 스타터) ② 진짜=별칭(main) ③ `reference_id`(Long 컬럼)에 테스트가 문자열 삽입→SQLException(→`(Long) null`). CI 초록까지 봐야 확정(로컬 Docker 없음→Testcontainers는 CI만).
- **리뷰 blocked**: `CHANGES_REQUESTED`가 남으면 CI 초록이어도 머지 불가 → **approve로 갱신** 필요.
- 팀 품질: chacha·lami(Claude) 모범적(insert 후 재조회·공통 재사용·충실 테스트). Gemini(kimgyubi)·Codex(plan)는 재작업 필요했음.
- 로컬 clov-api 워킹트리는 Codex가 `feat/rooms-list` 점유 중 — 리더 작업은 **worktree 격리** 권장.
