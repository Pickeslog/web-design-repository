# 핸드오프 — 2026-07-24 (오후, 새 컨텍스트 이어받기용)

> 자체완결 요약. 상세 진행·교훈은 로컬 메모리 `clov-production-build-workflow`(새 세션 자동 로드)에 누적돼 있다.
> 오전 요약은 `work-logs/2026-07-24-오전-작업정리.md`, 알림 설계는 `2026-07-24-알림-구조-설계안.md`.

---

## 1. 현재 상태 (한 줄)

- **clov-web main = `2912f54`**(#101까지) · **clov-api main = `76dd685`** · **web-design main = 이 커밋**
- **세 레포 열린 PR 0.** 새 세션은 각 레포 `git checkout main && git pull` 부터.
- 실행 중: **8080(clov-api #65 빌드)** · **5173(clov-web)** — 단 세션 종료 시 Claude preview가 내려갈 수 있음. 새 세션에서 필요하면 리더 IntelliJ로 8080 기동(또는 Claude preview). clov-api는 HMR 없어 재기동 필요.

## 2. 오늘 완료된 것 (머지됨)

**clov-api**
- **#57** 테스트 데이터 정리 SQL · **#59** 경험치 MVP(적립5종+연속레벨업) · **#60** 작업 스킬 · **#63** 멤버 `birthMonthDay` · **#65 알림 구조**(subType/payload + FRIEND 생산자 4종)

**clov-web**
- **#77** 가입수락 멤버 즉시갱신 · **#82** 작업 스킬 · **#83/#84/#87/#89** 날짜유틸 공통화(lib/datetime) · **#91/#94** ConfirmDialog(신설+3곳 적용) · **#92** 마스코트 교감 · **#97** 친구 생일 배너 · **#99/#100** CSS 대비 보정

**web-design**: 계약 §6(birthMonthDay)·§12(경험치)·§13(알림 subType/payload) 갱신, 전달 프롬프트·설계안 다수.

## 3. ★ clov-api #65 실서버 반영 — 완료 확인됨

- 마이그레이션 `db/manual-migrations/2026-07-24-notification-subtype-payload.sql`를 **리더가 MySQL에 실행 완료**. 8080 #65 새 빌드로 기동.
- **Claude 검증 통과**(2026-07-24 오후): ① 기존 JOIN 알림이 `subType=JOIN_REQUEST`로 백필됨 ② 추억 작성 시 알림 INSERT 201(=sub_type NOT NULL 정상) ③ 수신자 FRIEND 탭에 `MEMORY_WRITE`(actor=작성자) 생성.
- → **알림 구조가 실서버에서 살아 있음.** kimgyubi의 web #86이 이제 라이브 검증 가능.

## 4. 진행 중 / 대기

> **2026-07-24 오후 최신**: web #86 알림 렌더 = **PR #101 머지 완료**(라이브 검증: MEMORY_WRITE·PLAN·LEVEL_UP 문구·가짜공지 제거·NOTICE 빈상태 전부 확인, LEVEL_UP actor=null 크래시 없음). 알림 파이프라인(계약§13→백#65→프론트#101) **완결**.

> **재검증 (같은 날 늦은 오후, 새 세션)**: 세 레포 `origin/main` = clov-web `2912f54`(#101) · clov-api `76dd685`(#65) · web-design `bdc0cb6`(이 핸드오프) — §1과 일치, **세 레포 열린 PR 0** 재확인. web-design 열린 이슈 0.

### ⚠️ clov-api #62 — 닫아야 할 좀비 이슈
- **#62 "레벨업 알림 — type 추가 + 레벨업 시 알림 생성"이 아직 OPEN**이지만, 실제로는 **#64/#65에 완전 흡수돼 이미 완료·라이브 검증됨.** LEVEL_UP 생산자가 `ExpService.grant()`([96–97줄](../../clov-api/src/main/java/com/korit/clovapi/domain/room/service/ExpService.java#L96))에 구현돼 있음 — 레벨업 시 `fanOut(roomId, actor=null, TYPE_FRIEND, SUB_LEVEL_UP, payload={"level":N})`.
- #62 코멘트에도 "레벨업 알림은 #64 안에 들어간다"고 리더가 남겨둠. **남은 작업 없음 → 닫으면 됨.** kimgyubi가 연 이슈라 이 세션에선 안 닫았다(보드 정리는 리더/작성자 몫).

### kimgyubi1234 (열린 이슈 1개)
- **web #102 경험치 히스토리 UI** (배정됨, 착수 대기) — 계약 변경 없음. `GET /rooms/{id}/exp-logs` 완비, 프론트만. `api/room.js`에 `getExpLogs` 추가 + actionType 한글라벨·+N·작성자·시각. ⚠️ 시각은 `lib/datetime`(createdAt UTC, 직접 new Date 시 9h 밀림). 라이브 가능(방15에 exp-log 다수). 초안=scratchpad `issue-explog-ui.md`(이슈 본문에 반영됨).

### chacha1650a (열린 이슈 1개)
- **web #96 상점 관련 UI 디자인** — chacha가 연 이슈. **아직 리더/Claude 미검토.** 상점은 계약·명세에 없던 새 영역이라 스코프·백엔드 필요 여부부터 정의해야 함.

### lami2342
- 현재 배정된 열린 이슈 없음. 다음 작업 미정.
- 후보: 마스코트 후속(`remainingToday` 안내 + 롭 말투 차별화, chacha #92 리뷰 코멘트 참조) / #99에서 뺀 D-day 카드 폭·제목 짤림(아래).

## 5. 남은 백로그 / 미결

- **편지 알림(LETTER_RECEIVE)** — #65에서 제외. broadcast가 벌크 insert라 개별 letterId를 못 받아 referenceId 설계 별도 필요. 계약 §13 카탈로그엔 목표로 남음. 후속 이슈 필요.
- **D-day 카드 폭/제목 짤림** — 이슈 #95 항목2, PR #99에서 빠짐(`schedule-grid` 데스크톱 3열 고정·`schedule-title` nowrap+ellipsis 그대로). lami에게 "후속인지" 물음(리뷰 코멘트). 후속이면 별도 이슈.
- **경험치 후속(MVP 밖)**: 가속도 §9.2 · 기억의 샘 §9.3 · 마일스톤 연출 §9.5.
- **배포/Docker** — 리더가 "시작하자" 할 때까지 착수 금지(2026-07-23 지시).

## 6. 테스트 데이터 (dev DB)

- 계정 `invA~invE-0724a@test.local`(userId 34~38, 비번 `Abcd1234!`) · 방 15 · 오늘 검증으로 추억/약속/알림 다수 잔존.
- 정리 SQL = `clov-api/db/maintenance/cleanup-test-data.sql`(email LIKE `%@test.local` + 멤버 전원 테스트계정인 방). 사용자 DB툴로 실행.
- A(34) birthdate는 검증 중 바꿨다 원복함(1998-03-21). B(35)도 원복(2000-03-21).

## 7. 환경 / 규칙 (반복 함정)

- **8080 소유권 하나로**: Claude preview와 리더 IntelliJ가 동시에 8080을 잡으면 `Port already in use`. 오늘 4번 충돌. 재기동 필요 시 한쪽이 내리고. clov-api는 HMR 없음 → 코드/마이그레이션 반영엔 `clean bootRun` 재기동 필수. clov-web은 5173 HMR.
- **PR 머지 = 리더가 직접**(분류기 차단). 생성·리뷰·CI확인·라이브검증까지 Claude. gh 전체경로 `C:\Program Files\GitHub CLI\gh.exe`.
- **공유 워킹트리**: clov-web·clov-api 각각 Codex와 공유 가능 → `git add -A` 금지, 내 파일 경로만.
- **팀원 전원 Claude**(kimgyubi 포함). 각 레포 `.claude/skills/clov-task` 자동 적용. 비전공이라 프롬프트는 파일·줄번호·함정까지 명시.
- **계약(web-design)은 브랜치 보호 없음** → 팀원도 반드시 PR로(main 직접 push 금지). 계약 변경은 리더 승인.
- **PS 5.1 API 스크립트**: `-SkipHttpErrorCheck` 없음(try/catch), 스크립트는 UTF-8 BOM, 응답 한글은 `RawContentStream`을 UTF8 디코딩.

## 8. 다음 세션 착수 순서(제안)

1. 각 레포 `git checkout main && git pull`
2. **clov-api #62 닫기** — #65에 흡수·라이브 검증 끝난 좀비 이슈(§4 참조). 리더가 정리
3. **web #96 상점 이슈 스코프 검토** — 새 영역이라 계약/명세 정합부터
4. **kimgyubi web #102(경험치 히스토리 UI) PR 올라오면 감사** — 라이브 가능(방15에 exp-log 다수). ⚠️ 시각은 `lib/datetime`(직접 `new Date` 시 9h 밀림)
5. lami·chacha 다음 작업 배정 (둘 다 손 빔 — §4 후보 참조)
6. 편지 알림(LETTER_RECEIVE)·D-day 짤림 후속 이슈화 여부 결정 (§5)
