# 핸드오프 — 2026-08-05 (다음 세션 이어받기용)

> 자체완결 요약. 오늘의 한 줄: **오전엔 골드(`clov-api#92`)를 끝냈고, 오후·저녁은 실기기 모바일에서 쏟아진 12건을 잡았다.** 저녁엔 신규 사용자 온보딩 가이드 시안을 설계까지 마쳤다(구현 전).
> 골드는 실제로 돈다(원장 확인 완료). 계약 골드 경제를 전수 검토해 넷을 개정하고 다섯을 이슈로 뗐다.
> **다만 화면은 아직 조용하다** — `clov-web#178`이 붙어야 사용자가 골드를 벌었다는 걸 안다.
> 앞선 맥락: [`2026-08-04-핸드오프.md`](2026-08-04-핸드오프.md) · [`2026-08-04-작업일지.md`](2026-08-04-작업일지.md)
> 오늘 있었던 일: [`2026-08-05-작업일지.md`](2026-08-05-작업일지.md)

---

## 0. 다음 세션 이것부터

1. **★★ 온보딩 가이드 구현 시작** — 설계·시안이 다 끝났다(§12). **남은 결정은 하나뿐이다**: *방이 0개일 때만 자동으로 띄울지, 모두에게 띄울지.* 리더가 "일단 다 뜨는데 다시는 안 보기 버튼"을 골랐으니 그대로 가되, **방이 여러 개인 사용자에게 "우정공간이 뭐예요?"가 매번 뜨는 게 괜찮은지**만 확인하면 된다
2. **★ `clov-web#178`** — 헤더 골드 배지 즉시 갱신. **골드는 도는데 화면이 말을 안 한다**(§2). chacha 응답 대기 중
   > 요청한 건 주석 값 넷뿐이다 — 교감 캡 3→10 · 하루 상한 500→6,000 · "약속 연결만"→자유 추억도 · 상한 500. **코드는 손댈 게 없다**
3. **실기기에서 오늘 배포분 확인**(§11-4) — 데스크톱에서 재현 불가능한 것들이라 눈으로 봐야 한다
4. **상점 카드 640×640 재작업** — 한정 배경 카드만 1920×1080이라 정사각 슬롯에 빈 띠가 생긴다. **기능은 정상, 그림만 작다**(§9). 이 환경에 이미지 변환 도구가 없어 못 했다
5. **동기 피드백 수거** — `피드백` 라벨 이슈가 **아직 0건**. 세 레포 다 확인
6. **`personal_invite_code` step2(DROP COLUMN)** — 사전조건 충족됨(`#133` 배포). 안 급하다

> ✅ **오늘 끝난 것** — 오전: `clov-api#92` 머지·배포(**골드가 처음으로 돈다**) · 자유 추억 3자 인하 · **한정 상품 둘** · 계약 개정 3건 / 오후·저녁: **모바일 14건 머지·배포**(§11) · **온보딩 가이드 설계 완료**(§12)

> ⚠️ **이제 안 끝난 것 중 리더 결정이 필요한 건 §0-1 하나뿐이다.** 나머지는 다 실행이다.

---

## 1. 현재 상태

| 저장소 | main | 배포본 | 열린 PR |
|---|---|---|---|
| `clov-api` | `375f26f` | **동일** ✅ | — |
| `clov-web` | `315fba6` | **동일** ✅ | **#178**(배지 갱신) |
| `web-design` | (아래 PR 참고) | — | — |

```
상점   ACTIVE 19종 · 55,920 · 완주 약 9일
       마스코트 스킨 14종 (크로비 4종으로 최다) + 배경 5종
       LEGENDARY 3종 — 아케이드 보스 9,800 · 크로비 한정 1,960 · 배경 한정 1,960
```

```
clov-api#92   머지 2d4962c (10:43) · 배포 완료 (10:44)   ← 골드가 처음으로 돈다
  dd50f2d  chacha 원본 (보존)
  473bbba  리뷰 6건 + 계약 개정값
  884e8ad  LIKE 이스케이프 수정 + 회귀 테스트
  04340a1  지갑 정리 누락 · 교감 XP 기댓값
```

> **★ `#92`는 chacha 브랜치(`feat/89-gold-earn`) 위에 리더가 얹었다.** 원 커밋과 작성자 기록이 그대로 남아 있다. PR 코멘트에 방향 전환(§2)을 명시해뒀고 **"이상하면 되돌려 달라"**고 적었다.

---

## 2. ★★★ 골드 — 돈다. 확인 끝났다. 화면만 조용하다

> ✅ **원장으로 확정했다**(2026-08-05 10:56 KST).
>
> ```
> 142  EARN_MEMORY_FREE  +200  → 49,800   01:56:59 UTC
> 141  EARN_MASCOT       +200  → 49,600
> 140  EARN_MASCOT       +200  → 49,400
> 139  EARN_MASCOT       +200  → 49,200
> ```
>
> **이 네 줄이 세 가지를 한 번에 증명했다** — 자유 추억 지급이 작동한다 · 마스코트가 200이다(옛 코드면 100) · **배포된 게 새 빌드다**(`EARN_` 사유는 옛 코드에 존재하지 않는다).

> ### ★★ 그런데 리더는 "지급이 안 된다"고 봤다
>
> **`clov-web#178`이 안 붙어서 헤더 골드 배지가 갱신되지 않는다.** 지급은 됐는데 화면이 말을 안 한 것이다.
>
> **★ 이 혼동이 개발자에게도 일어났다.** 같은 보고를 받고 처음엔 "길이 조건이 문제인가"를 의심해 20자 → 3자 인하로 갔다. 그런데 **리더가 올린 본문은 27자라 20자 기준으로도 통과**했다 — 원인이 아니었다. **화면이 침묵하면 사용자만 헷갈리는 게 아니라 진단도 헷갈린다.**
>
> **증상 보고를 받고 값을 고치기 전에 그 값이 원인인지 먼저 재라.** 원장을 안 봤으면 `#136` 배포 후 같은 증상이 또 나왔을 때 처음부터 다시 파야 했다.

> ### 원장 보는 법 (인증 뒤 상태는 이 방법뿐이다)
>
> Claude는 못 한다 — 원격 DB 자격증명을 실은 명령이라 분류기가 막는다(타당하다). **사람이 직접 실행한다.**
>
> ```bash
> cd /c/gov/project/clov-api && DBU=$(grep -m1 "username:" src/main/resources/application-secret.yaml | sed -E 's/.*username: *//' | tr -d '"'"'"' \r') && DBP=$(grep -m1 "password:" src/main/resources/application-secret.yaml | sed -E 's/.*password: *//' | tr -d '"'"'"' \r') && "/c/Program Files/MySQL/MySQL Shell 8.0/bin/mysqlsh.exe" --sql --quiet-start=2 -h 116.122.153.5 -P 3306 -u "$DBU" -p"$DBP" --schema=st4_clov -e "SELECT id, user_id, reason, amount, balance_after, created_at FROM wallet_transactions ORDER BY id DESC LIMIT 15;"
> ```
>
> - **DB는 VM이 아니라 학원 MySQL** `116.122.153.5:3306/st4_clov`다. 로컬 `application-secret.yaml`이 운영과 같은 곳을 본다
> - **`created_at`은 UTC다**(hikari `connection-init-sql`이 세션 타임존을 `+00:00`으로 고정). `01:56` UTC = **10:56 KST**
> - ⚠️ 리더 계정에는 08-04 `ADMIN_GRANT` 60,000이 있어 **잔액 변화가 눈에 안 띈다.** 원장으로 보는 게 확실하다
>
> **★ 우회법 셋(axios 스텁 · 정적 자원 `curl` · 번들 문자열)은 전부 인증 밖에서만 통한다.** 원장·잔액·지급처럼 로그인 뒤 상태는 그 셋 어디에도 안 걸린다 — **`clov-api#135`(원장 조회 API)가 그래서 필요하다.**

### 오늘 확정된 값 (계약 §15-4·§10, `web-design#63`)

| 사유 | 금액 | 자체 제한 | 하루 최대 |
|---|---:|---|---:|
| `EARN_MASCOT` | 200 | 하루 10회 | 2,000 |
| `EARN_MEMORY` | 300 | **횟수 캡 없음** | 총 상한까지 |
| `EARN_MEMORY_FREE` | 200 | 하루 10회 + **본문 3자 이상** | 2,000 |

**유저 단위 하루 총 상한 `6,000`** (완주 약 9일 · 카탈로그 52,000 실측)

### ★ `#92`가 원안대로 머지됐으면 증상이 절반만 사라졌다

원 구현은 `createFree`(자유 추억 = 추억피드에서 약속 없이 쓰는 글)를 **지급 대상에서 명시적으로 제외**했다.

```
useCreateMemory.js:12   planId ? createPlanMemory : createMemory

약속 선택함  → POST /plans/{id}/memories   → createFromPlan()  → 지급
약속 없음    → POST /rooms/{id}/memories   → createFree()      → 원안은 0골드
```

리더가 보고한 증상이 정확히 이쪽이었다. 계약은 그 위험(*"혼자 무한히 쓸 수 있다"* — **정확한 진단이다**)을 "제외"가 아니라 **"사유 분리 + 횟수 캡 + 본문 길이"**로 막는 쪽으로 결론냈다.

### ★ 본문 길이 조건 — 같은 날 20 → 5 → 3으로 두 번 내렸다

```java
// CreateMemoryRequest — 필수인 것은 title 뿐이다
@NotBlank @Size(max = 40) String title,
@Size(max = 100) String content,          // 최소도 @NotBlank도 없다
```

**본문이 통째로 비어도 추억은 정상 저장된다.** 그래서 조건 자체는 필요하다.

내린 이유는 **사진 위주 추억은 본문이 짧은 것이 정상**이기 때문이다. §12가 `MEMORY_IMAGE_BONUS`로 사진을 이미 노력으로 인정하는데 골드 쪽만 글 길이로 판정해 사진 기록을 벌하고 있었다. **사진 유무로 판정하는 게 맞지만 작성 시점에는 사진 수를 알 수 없다**(생성 → presign → PUT → 커밋 순서, `CreateMemoryRequest`에 이미지 필드 없음).

> ### ⚠️ 그 결과 이 값은 채굴 방지 장치가 아니다 — 빈 글만 거른다
>
> **채굴은 전적으로 하루 10회 캡이 막는다.** 길이 조건은 몇 자로 잡든 붙여넣기면 뚫리므로 **방어력을 여기에 기대지 말 것.**
>
> **★ 계약의 "클릭 200 = 글 200" 근거가 무너졌다.** 원래 근거가 *"노력 차이는 금액이 아니라 게이트가 만든다"*였는데 **3자는 클릭 한 번과 다르지 않다.** 계약에 "근거가 약해졌고 알고 넘긴다"로 고쳐 적었다 — **며칠 지켜보고, 손대야 하면 게이트를 올리는 것보다 금액을 가르는 쪽**(자유 글 200 → 150)이 먼저다. 한 번 내린 게이트를 올리면 쓰던 사람이 벌을 받는다.
>
> **★ 진짜 해법은 이미지 커밋 시점(`POST /memories/{id}/images`)에 지급하는 것이다.** §12가 XP에 대해 이미 그 구조를 쓴다(`MEMORY_IMAGE_BONUS`, 추억당 상한 8). **`web-design#64`(약속 골드)와 같이 검토할 것.**
>
> ⚠️ **XP는 조건 없이 그대로 준다. 골드만 조건부다.** 3자 미만이어도 글은 정상 생성되고 `201`이 나간다 — **여기에 `400`을 만들지 말 것**(계약: *"글쓰기를 막는 건 골드 캡이 할 일이 아니다"*).

### ⚠️ 배포해도 화면은 조용하다

`#92`가 생성 응답에 `earnedGold`를 붙였지만 **clov-web이 그 필드를 안 읽는다.**

```jsonc
POST /rooms/{roomId}/memories → 201
{ /* MemoryDetail 전체 */, "earnedGold": 200 }
```

- `0`이 **정상값**이다 — 상한 초과 · 횟수 초과 · **본문 3자 미만** · 삭제 후 재작성(revive)
- 조회·수정 응답에는 **안 붙는다**(`@JsonInclude(NON_NULL)`) — 그때는 지급이 없어서 `0`을 보내면 캡에 걸린 `0`과 구분이 안 된다
- 화면은 `earnedGold > 0`일 때만 획득 연출을 한다(§12 마스코트와 같은 규약)

**골드 획득 연출과 배지 갱신이 `clov-web#178` 자리다.** 그게 붙기 전까지는 *"글을 썼는데 골드가 들어왔는지 모르겠다"*가 남는다. 잔액 자체는 `GET /shop/wallet`으로 올라간다.

### 남은 미결

- **약속 생성·완료에 골드가 없다** — `web-design#64`. 지금 설계는 "만들기 → 완료 → 작성" 한 덩어리에 300을 매기는데 **확정한 기록이 없다**

---

## 3. ★★ SQL — `EARN_` 합산의 이스케이프 함정

```sql
WHERE reason LIKE 'EARN!_%' ESCAPE '!'     -- ✅ 이렇게 쓴다
WHERE reason LIKE 'EARN\_%' ESCAPE '\'     -- ❌ MySQL 구문 오류
```

**MySQL은 문자열 리터럴 안의 백슬래시를 이스케이프 문자로 먼저 해석한다** — `\'`가 닫는 따옴표를 이스케이프해 문자열이 안 닫힌다. 오늘 실제로 냈다.

**증상이 크다.** `sumEarnedToday`는 `earnGold`의 첫 쿼리라, 깨지면 **추억 생성·마스코트 교감이 전부 `500`**이 된다. CI에서 테스트 10건이 깨졌고 **그중 7건은 골드와 무관한 기존 테스트**였다.

> **`ESCAPE` 절 자체는 반드시 있어야 한다.** 빼면 `_`가 임의의 한 글자로 매칭돼 `EARNX_...` 같은 사유까지 합산에 걸린다.
>
> **`ADMIN_GRANT`는 접두사가 달라 합산에서 빠진다** — 의도된 것이다. `EARN_` 계열로 주면 받은 날 정상 획득이 통째로 막힌다.

회귀 테스트 `adminGrantIsExcludedFromTheDailyEarnCap`이 이걸 지킨다 — 상한을 넘는 `ADMIN_GRANT` 10만을 오늘 원장에 넣고도 자유 추억 200이 지급되는지 본다. **접두사 합산 · `ESCAPE` 생존 · 쿼리 실행 여부를 한 번에 잡는다.**

---

## 4. ★ 검증 제약 — 로컬 통과가 무증거인 범위

오늘 `compileJava`·`compileTestJava`가 **세 번 다 통과했는데 CI는 세 번 다 다른 걸 잡았다.**

| CI가 잡은 것 | 왜 로컬이 못 봤나 |
|---|---|
| MyBatis XML의 SQL 구문 | **컴파일러가 XML 안의 SQL을 안 본다** |
| 테스트 DB FK 위반 | 실제 DB가 있어야 난다 |
| 테스트 기댓값 산술 | 실행해야 난다 |

> **로컬에 Docker가 없어 Testcontainers를 못 도는 동안 CI가 유일한 방어선이다.** 08-04의 "에러가 안 나는 결함"과 반대로 **요란하게 죽는 종류**라, 통합 테스트만 도달하면 반드시 잡힌다.

### ★★ clov-api 배포에서 `401`은 "새 빌드가 떴다"를 뜻하지 않는다

오늘 `#92` 배포에서 응답이 이렇게 움직였다.

```
푸시 직후        401   ← 아직 옛 컨테이너다
워크플로 success  -
                 502   ← 옛 것이 내려가고 새 것이 뜨는 중
                 401   ← 새 컨테이너
```

**푸시 직후의 401을 "배포 완료"로 읽을 뻔했다.** 워크플로가 빌드·이미지 push·SSH·`compose pull`·재기동까지 도는 동안 **옛 컨테이너가 계속 401을 준다.**

> **★ `401` 하나는 "어떤 clov-api가 살아 있다"까지다.** `401 → 502 → 401` **순서**가 교체를 증명한다. **어제 핸드오프의 "초록불은 컨테이너가 떴다까지다"에서 한 칸 더 들어간 자리다** — 초록불도 못 믿고, 401도 그것만으로는 못 믿는다.
>
> 확인 순서는 이렇게 한다.
>
> ```bash
> gh run list -R Pickeslog/clov-api --branch deploy-myeongjun --limit 1   # success 확인
> curl -s -o /dev/null -w "%{http_code}\n" https://clovlabcalss.store/api/v1/rooms
> ```
>
> **502를 한 번 보고 401로 돌아오는 것까지 봐야 한다.** 502 지속은 편차가 크다(08-04 실측 17초~4분 30초, 오늘은 수 초). **고정 `sleep`이 아니라 폴링으로 기다린다.**

### ⚠️ clov-api는 내용까지 증명할 수단이 없다

clov-web은 인증 없이 **배포 번들에서 문자열을 찾을 수 있다.** clov-api는 그 수단이 없다 — springdoc이 있는데도 외부에서 안 닿는다.

```
/api/v3/api-docs   → 401       nginx는 통과하는데 permitAll 이 /v3/api-docs/** 라 /api 접두사가 붙으면 안 걸린다
/v3/api-docs       → 200 875B  그런데 nginx SPA 폴백(index.html)이다
```

> ⚠️ **이 875B를 "받아왔다"로 읽으면 오진한다.** 찾던 문자열이 0건이길래 "옛 빌드"로 판단할 뻔했는데, **대조군까지 0건**이라 검색 자체가 틀렸다는 게 드러났다(08-04 §13-5와 같은 함정).
>
> **`clov-api#108`에 해법을 적어뒀다** — actuator를 넣을 때 `/actuator/info`에 커밋 SHA를 실으면 "떴나"와 "뜬 게 새 것인가"를 같은 요청으로 판정한다. **그때 nginx 라우팅도 같이 열어야 한다**(한쪽만 열면 오늘 `/api/v3/api-docs`가 401을 준 것과 똑같아진다).

### ★ 지급 경로를 넓히면 테스트를 전수로 센다

"자유 추억도 골드를 준다" 하나가 **테스트 클래스 다섯에 지갑 정리를 요구했다.**

```
users DELETE 하는 테스트 14개 중 골드 경로를 타는 것 5개
  Memory · Exp · Room · Comment · NotificationFanOut   ← 다섯 다 정리 완료
```

`Comment`·`NotificationFanOut`은 본문이 `"Body"`·`"짧은 추억"`이라 **20자 미만이어서 우연히 통과**하고 있었다. 본문을 한 글자만 늘려도 조용히 깨진다.

### 그 외 (이월)

- **`gh`가 PATH에 없다** — `C:\Program Files\GitHub CLI\gh.exe` 전체 경로
  > ⚠️ **`gh pr review --approve`·`gh pr merge`·`deploy-*` push는 분류기가 막는다. 사용자가 직접 실행한다.** Git Bash에서는 `&` 없이, 경로는 `/` 구분자로 쓴다(`&`는 PowerShell 전용이라 bash에서 문법 오류)
- **JDK가 PATH에 없다** — `$env:JAVA_HOME` 먼저. clov-api는 **Java 21**
- **Claude는 로그인 뒤 화면을 못 본다** — 우회 셋은 08-04 핸드오프 §7 참조

---

## 5. 계약 검토 결과 — 여덟 중 넷 개정, 다섯 이슈

### 개정함 (`web-design#63`, 열림)

| | 내용 |
|---|---|
| **A** | 노력 순서가 **총량에서** 깨져 있었다 — 클릭 10번이 하루 예산의 67%였다. 단가만 정렬하고 옆 칸(하루 최대)을 안 봤다 |
| **B** | 추억 생성 응답에 `earnedGold` 신설 — 마스코트에만 있던 비대칭 |
| **C** | `EARN_MEMORY`의 *"약속당 1개라 자연 상한"*은 **하루 상한이 아니다**를 명시 |
| — | `EARN_MEMORY_FREE` 3회 → 10회 + 길이 조건(20 → 5 → 3자) · 총 상한 3,000 → 6,000 |

> ⚠️ **상한 6,000은 §15-4의 "카탈로그를 먼저 본다" 가드레일에 걸린다** — 카탈로그가 52,000에 멈춘 채 획득량만 2배로 올렸다. **알고 넘겼고 근거를 계약에 적었다**: 지금은 상한 조율 국면이 아니라 **골드가 아예 안 도는 상태를 푸는 국면**이다. 경로가 실제로 돌면 며칠 재보고 다시 정한다.

### 이슈로 뗌

| | |
|---|---|
| `web-design#64` | 약속 생성·완료 골드(`EARN_PLAN`) 확정 |
| `web-design#65` | `ADMIN_GRANT` 지급액 가이드 — 60,000 > 카탈로그 52,000 |
| `web-design#66` | `discountRate` 운영 주체·주기 (07-30부터 미정) |
| `clov-api#135` | 원장 조회 API — **골드가 왜 0인지 확인할 방법이 없다** |
| `clov-web#303` | 상점 빈 탭 (`SKIN`·`EVENT` 0종) |

---

## 6. 이슈 현황

### 오늘 닫음 — `clov-api#94`

팀원 셋의 VM 준비가 **전부 끝나 있었다.** `deploy-chacha`·`deploy-lami`·`deploy-kimgyubi`가 두 레포에 다 있고 **실패로 끝난 실행이 하나도 없다**(lami는 오늘 09:42 clov-web 배포 성공).

> ⚠️ **`deploy-<이름>` 브랜치는 지우지 말 것.** 학원 과정 후 개인 프로젝트 배포에 그대로 쓴다.

### 오늘 닫음 — `clov-api#72`(개인 최종 배포)

07-27 로그에 *"Docker 통합 테스트와 스모크 테스트가 남아 `OPEN`으로 유지"*라고 **의도적으로 열어둔 기록**이 있었다. 9일 지나 보니 **둘 다 이 이슈에서 처리할 수 있는 성격이 아니었다.**

| 미체크 항목 | 판단 |
|---|---|
| Docker/Testcontainers 로컬 실행 | **이 이슈에서 못 푼다.** 로컬에 Docker가 없고, **그 역할은 CI `build`가 이미 하고 있다**(오늘 `#92`에서 로컬 컴파일 3회 통과를 CI가 3회 다 잡았다) |
| 핵심 기능 스모크 테스트 | **일회성 항목이 아니게 됐다.** CI/CD로 넘어가면서 매 배포 확인 사항이 됐고 방법도 정착했다 |

수동 배포 체계 자체가 `#93`·`#94`로 대체됐고, `#94`도 오늘 닫았다.

> ⚠️ **`#72` 본문은 한글이 전부 `?`로 깨져 있다.** 실제 기록은 [`2026-07-27-myeongjundev-개인-GCP-최종배포.md`](2026-07-27-myeongjundev-개인-GCP-최종배포.md)(커밋 `418a0ae`)에 온전히 있다 — **새 VM을 붙일 때는 이슈가 아니라 이 문서를 본다.**

### 이월

- **`clov-api#108`** 배포 헬스체크 부재 — 편차가 17초~4분 30초라 매번 `curl`로 확인
- **`clov-api#89`** / **`clov-web#177`** — `#92`·`#178`의 원 이슈. **본문이 빈 템플릿**이라 내용은 PR에 있다
- ~~**`clov-web#271`** Signup STEP 2 에러 미표시~~ — **오후에 `#308`로 해결·배포됐다**
- **`web-design#42`** DDL 주석 — `MEMBER_JOINED`뿐 아니라 **`MEMBER_LEFT`도 빠져 있다**(오늘 확인해 갱신)
- **`web-design#64`~`#66` · `clov-api#135` · `clov-web#303`** — 오늘 계약 검토에서 뗀 5건(§5)

### 오늘 닫은 PR — `clov-web#165` (사용자설정 카드형)

07-29부터 열려 있었다. **닫은 근거가 둘이다.**

- **이 PR이 없어진 필드에 복사 버튼을 붙인다.** `personal_invite_code`는 08-04에 제거됐고(`clov-api#133`) `main`에 흔적이 0건인데, PR은 `me.personalInviteCode`를 카드형 행으로 만든다. **머지하면 빈 칸이 뜨고 복사 버튼이 아무것도 복사하지 않는다**
- **같은 두 파일이 갈라졌다** — `Settings.jsx`·`settings.proto.css`가 merge-base(07-29) 이후 main에서 `+105/-33` 바뀌었다(배경 소유 잠금·폰트 정리). PR은 `+113/-43`이라 **rebase가 아니라 다시 짜는 수준**이다

> **브랜치(`feat/settings-ui-redesign`)는 안 지웠다.** 프로필 히어로 · **위험구역 분리(계정 탈퇴)** · 아이콘 카드형 행 · 레일 메뉴 아이콘 넷은 지금도 유효한 방향이다. **다시 할 때는 초대코드 행을 빼고, 배경 소유 잠금 UI를 안 깨는 것만 주의한다.**

---

## 7. ★ PR이 막히는 조건 — 어제 진단을 정정한다

어제 핸드오프의 *"GitHub은 `#92`를 한 번도 막은 적이 없다"*는 **07-31 06:34 이전까지만** 맞다. 그 시각에 첫 `CHANGES_REQUESTED`가 들어갔다.

```json
"required_approving_review_count": 0,
"required_review_thread_resolution": true,
"dismiss_stale_reviews_on_push": false     ← 어제 안 본 줄
```

**이 레포에서 PR이 막히는 조건은 셋이다.**

| | |
|---|---|
| `build` 체크 | 실패하거나 아직 도는 중이면 `BLOCKED` |
| 미해결 리뷰 스레드 | 인라인 코멘트 스레드를 남겨두면 막힌다 |
| **`CHANGES_REQUESTED` 리뷰** | **승인 개수 요구가 0이어도 막는다.** 그리고 `dismiss_stale_reviews_on_push: false`라 **작성자가 푸시해도 안 풀린다 — 리뷰어가 새 리뷰로 덮어야 한다** |

> **★ "요청만 처리되면 바로 머지된다"가 아니었다.** 작성자가 할 수 있는 일이 끝나도 **리뷰어가 한 번 더 움직여야 한다.** 그걸 모르면 양쪽이 서로를 기다린다.
>
> ```bash
> gh api repos/Pickeslog/clov-api/rules/branches/main    # 추측하지 말고 연다 — 그리고 필요한 줄까지 본다
> ```

---

## 8. 팀원

| | |
|---|---|
| **chacha1650a** | 오늘 아침 clov-web 3건 머지(`#284`·`#287`·`#290`) + `#289` 리뷰 대상. **`#92`는 리더가 이어받았다** — PR에 방향 전환을 설명하고 검토를 요청해뒀다. 다음은 `clov-web#178` rebase |
| **lami2342** | `clov-web#289`를 열고 **7분 만에 셀프 머지**했다(룰셋상 허용). 수정 자체는 정확했지만 **소스에 박은 12줄짜리 `@scope` 정책 주석의 근거가 순환 참조**라 사후 리뷰를 남겼다(§9 작업일지) |
| **kimgyubi1234** | `#258` 폰트 진행 중 |

---

## 9. ★★ 한정 상품 — 오후에 둘을 넣었다. 규칙이 하나 생겼다

```
크로비 스킨  '새벽의 별빛 기록자'   COSTUME    · LEGENDARY 9,800 · 할인 80% → 1,960
배경         '은하수 아래 개발자의 밤'  BACKGROUND · LEGENDARY 9,800 · 할인 80% → 1,960
```

**둘 다 배포·SQL 실행까지 끝났다.** 에셋이 운영에서 서빙되는 것까지 확인했다(바이트 일치).

> ⚠️ **화면은 아무도 못 봤다.** 인증 뒤라 Claude가 못 본다. **상점에 뜨는지 · 사면 잠금이 풀리는지 · 배경을 고르면 별이 반짝이는지** 셋을 봐야 닫힌다.

### ★★★ "한정"을 표현하는 규칙 — 앞으로 이대로 한다

```
price          등급이 말한다 (계약 §15-4 규칙 그대로)
discount_rate  "한정"이 말한다
```

**정가를 올려서 한정을 표현하지 않는다.** 계약 §15-4가 *"등급도 같고 내용도 같으면 값도 같게"*인데, 한정 스킨은 아케이드 보스 롭과 **등급도 내용(상태 9종)도 같다.** 정가를 올렸으면 규칙을 어겼고, **다음 한정 상품마다 "얼마나 올릴까"를 근거 없이 정해야 했다.**

> ### ★★ 등급을 올릴 때 "한정"을 근거로 쓰지 말 것 — 같은 이유를 두 축에 쓰는 것이다
>
> 배경 쪽에서 이게 걸렸다. 기존 배경 4종이 `RARE 2,800`인데 이것만 LEGENDARY다. **근거는 "한정"이 아니라 "이 배경에만 CSS 효과 레이어가 붙는다"로 적었다** — *"등급도 같고 **내용도 같으면**"*이라 **내용이 다르면 등급이 달라도 된다.**
>
> **★ 그래서 조건이 붙는다** — `index.css`의 `[data-app-bg="developer-gemini-night"]` 별 반짝임을 빼면 **이 등급의 근거가 사라진다.** 그때는 `RARE 2,800`으로 내리는 게 맞다. SQL 주석과 `appBackground.js` 양쪽에 적어뒀다.

### ⚠️ 할인 해제는 SQL을 직접 돌려야 한다

`discount_rate`를 **실제로 쓰는 첫 사례**다(계약 §15-3이 *"운영 주체·주기 미정"*으로 둔 필드, `web-design#66`). 관리 화면이 없어서 **해제 SQL을 등록 SQL 파일 맨 아래에 주석으로 같이 넣었다.**

```
db/manual-migrations/2026-08-05-shop-crobi-dawn-starlight-archivist-skin.sql
db/manual-migrations/2026-08-05-shop-developer-gemini-night-background.sql
```

> **등록만 남기면 "어떻게 끝내지"를 나중 사람이 처음부터 만들어야 하고, 그러면 대개 안 끝난다.** "한정"이 영구 할인이 되는 경로가 이것이다.
>
> ⚠️ **해제해도 이미 산 사람의 가격은 안 바뀐다** — `paid_price`가 구매 시점 실효가를 박아둔다(§15-1 (4)). **되돌릴 수 없다.**
>
> ⚠️ **해제하면 카탈로그 총액과 완주 일수를 계약에서 같이 고친다.** 지금은 할인 덕에 55,920 / 약 9일로 계약값이 그대로 맞는다.

### ★ 배경 에셋 규격 (한 곳에 모아둔다)

```
배경        1920 × 1080   WebP   66KB ~ 431KB    public/backgrounds/<id>.webp
썸네일       512 × 512     PNG    26KB ~ 155KB    public/bg-thumbs/<id>.png
상점 카드    640 × 640     WebP   47KB ~ 139KB    public/shop/backgrounds/<id>.webp
```

⚠️ **한정 배경의 상점 카드만 1920×1080이다.** `.shop-art`가 `aspect-ratio: 1/1` + `object-fit: contain`이라 **위아래에 빈 띠**가 생긴다(세로 약 43%). 기능은 정상이고 **카드에서만 작게 보인다.** 640×640으로 다시 뽑아 파일만 교체하면 된다 — **이 환경에 이미지 변환 도구가 없어 못 했다.**

> **★ 애니메이션 배경은 안 한다.** GIF 15.4MB(720×405)를 정지 이미지 130KB(1920×1080)로 바꿨다 — **119분의 1이고 해상도는 오히려 올라갔다.** 애니메이션 WebP도 1920×1080 × 100프레임이면 3~8MB라 답이 아니다. **"한정" 느낌은 움직임의 양이 아니라 "다른 배경엔 없는 것"에서 온다.**

### ★ CSS 효과 레이어를 만질 때

```css
:root[data-app-bg='developer-gemini-night'] body::before,
:root[data-app-bg='developer-gemini-night'] body::after {
  position: fixed; inset: 0;
  z-index: -1;          /* 배경 위 · 콘텐츠 아래 */
  pointer-events: none; /* 없으면 화면 전체 클릭이 막힌다 */
}
```

**`z-index: -1`이 되는 이유** — `html`에 `background`가 없어서 `body` 배경이 **캔버스로 승격**되고 스택 맨 아래에 칠해진다. 그래서 음수 z-index 의사요소가 그 위, `#root`가 다시 그 위다.

> **`#root`에 `position`/`z-index`를 주는 방식은 쓰지 말 것.** 전역 쌓임을 건드리면 **모달(240~9600)과의 관계까지 다시 봐야 한다.**
>
> **★ 다른 배경에 안 붙는 것을 대조군으로 확인할 것.** 새 배경에서 보이는 것만으로는 증명이 안 된다 — `getComputedStyle(document.body, '::before').content`가 다른 배경에서 `none`이어야 한다.

---

## 10. ⚠️ 이월 주의

- **R2 버킷 CORS** — 새 도메인으로 배포하면 `Cloudflare → R2 → clov-media → Settings → CORS Policy`에 오리진을 **반드시 추가**한다. `localhost:5173`은 빼지 말 것. 코드 배포 없이 즉시 반영된다
- **테스트 골드** — `grant-test-gold.sql` 잔액이 남아 있다. **운영 지급은 `ADMIN_GRANT`를 쓸 것**(`EARN_` 계열은 상한 합산에 잡힌다)
- **`_bak_user_wallets_20260802`** — 데모 끝나면 `DROP TABLE`
- **certbot 갱신** — 인증서 10/26 만료
- **`StoragePresigner`에 삭제 메서드 없음** — `deleteImage`가 DB 행만 지우고 R2 객체는 남는다
- **버거노인 8종** · **`crobi/dizzy.png` 오프모델** · **롭 나머지 5종 원본 점검** — 08-04 핸드오프 §7 참조
- **에셋 내보내기 고립 픽셀** — 6종 연속. `ALPHA_THRESHOLD = 8` 기준, `y < 12` 또는 `y >= 628` 띠에서 알파 ≤ 48만 비운다

---

## 11. ★★ 오후·저녁 — 실기기 모바일 14건

리더가 폰으로 배포본을 보기 시작하자 문제가 연달아 나왔다. **데스크톱에서는 하나도 안 보이던 것들이다.** 전부 머지·배포 완료.

```
#313 마스코트 미표시        #316 설정 모달 크기        #321 하단 탭바
#324 추억 카드 스와이프     #327 일정계획 레일 스냅     #329 마스코트 스크롤 사각지대
#334 우정공간 가로 밀림     #335 빈 상태 가독성        #338 설정 탭 3개 안 눌림
#342 달력 팝오버 위치       #344 초대 코드 발견성       #348 초대 코드 모달화
#349 배너 🗑️ → ti-trash    #350 방 목록 헤더에 상점
```

### 11-1. ★★★ "증상 셋이 한 원인"이었다

우정공간에서 **검은 띠 · 탭바 밀림 · 화면이 좌우로 흔들림**이 동시에 보고됐다. 셋 다 탭바를 의심하게 만드는 모양이었는데 —

```
clientW 375 · scrollW 534 = 159px 넘침
.cline-cards 숨김 → 375   ← 범인(코버플로우 덱)
.clov-hdr    숨김 → 534   ← 헤더는 무관
```

검은 띠는 `background-attachment: fixed`라 밀린 구역에 배경이 안 칠해진 것이고, 탭바 밀림은 문서가 가로로 넘치면 `position:fixed`가 레이아웃 뷰포트를 따라가는 것이다. **둘 다 결과였다.**

> **하나씩 숨겨가며 `scrollWidth`를 재는 것 말고는 가를 방법이 없었다.** 보고된 증상의 모양을 믿지 말 것.

### 11-2. ★★ CSS 함정 넷 (반복해서 밟았다)

| | 함정 | 어디서 |
|---|---|---|
| ① | **`1fr` ≠ `minmax(0,1fr)`** — `1fr`은 `minmax(auto,1fr)`이고 auto 바닥이 콘텐츠 최소폭 | `#338` **내 회귀** |
| ② | **`overflow-x:hidden`은 반대 축을 `auto`로 승격** — `visible`을 지키려면 `clip` | `#334` |
| ③ | **미디어쿼리는 특이도를 안 올린다** — 기본 규칙보다 **뒤에** 놓아야 한다 | `#342` |
| ④ | **`flex-shrink:0`은 자기 `flex-wrap`을 죽인다** — `max-width:100%` 필요 | `#344` |

①은 특히 조심할 것. **탭이 둘일 땐 안 드러났고 `#319`에서 5개로 쪼개지자 터졌다** — 내가 `#316`에서 심은 회귀다.

### 11-3. ★★ 검증 함정 — "멈춘 전환 시계"에 세 번 속았다

프리뷰 창이 표시되지 않으면 **CSS transition/animation 시계가 멈춘다.** 어제 핸드오프에 이미 적혀 있는데 오늘 세 번 다시 밟았다.

```
라이트 색이 다크 값으로 읽힘         transition: color
입력 테두리가 양쪽 테마에서 동일      transition: border-color
window.scrollBy({behavior:'smooth'})  0px 이동 (auto는 정상)
```

**컴퓨티드 값을 읽기 전에 `transition:none !important`를 주입할 것.** `behavior:'smooth'`는 이 환경에서 검증 불가라 **`scrollBy` 호출을 가로채 인자로** 확인했다.

### 11-4. 실기기에서 봐야 할 것 (데스크톱 재현 불가)

```
설정 모달    주소창 보이는 상태에서 저장 버튼이 화면 안에 있는지  (dvh)
마스코트     네트워크 끊었다 켠 뒤에도 남아 있는지
일정계획     천천히 밀어도 손가락을 따라오는지
초대 코드    방 만들기 모달과 같은 색·버튼·등장 연출인지
             /join/코드 링크로 들어가면 뒤에 방 목록이 남는지
```

### 11-5. ⚠️ 안 고친 것 — 구조적 한계

`#329`로 마스코트 아래 **세로 페이지 스크롤**은 살렸지만, **시각적으로만 겹친 가로 레일은 여전히 안 밀린다.**

```
after   vScroller: HTML   ← 세로는 살아남
        hScroller: null   ← 가로 레일은 못 찾음
```

`touch-action`은 **어떤 제스처를 허용할지만 정할 뿐 터치를 아래 요소로 통과시키지 않는다.** 통과시키려면 `pointer-events:none`인데 그러면 못 누른다. **떠 있는 위젯이 내용 위에 있는 한 피할 수 없다** — `clov-web#328`에 남겼다.

---

## 12. ★★★ 온보딩 가이드 — 설계 완료, 구현 대기

리더 제안: **처음 접속한 사용자에게 START → 가이드.** 레퍼런스는 마인크래프트 / Sun Haven / 소니 **스파이디 트래커**(`spideytracker.net`).

### 12-1. 확정된 사양

```
위치        방 목록(RoomList) 위 모달
구성        픽셀 창 프레임 + 모서리 클로버 + 도트 배경 + 가운데 마스코트 + START
가이드      4단계 · 이전 / 다음 / 건너뛰기
마스코트    64px · 12색 · 평균 다운샘플 → median cut 팔레트
저장        localStorage + sessionStorage (기기 단위, 백엔드 작업 없음)
되돌리기    프로필 드롭다운 "가이드 다시 보기"
```

**4단계 내용**: ① 우정공간이란(방장 없음) ② 들어오는 두 길(만들기 / 초대 코드) ③ 추억 남기면 골드 ④ 준비 끝

### 12-2. ★ 다시 뜰지 말지 — 세 갈래

```
건너뛰기 · X             이번 방문만    sessionStorage  clov-guide-skipped
다시는 안 보기            영구           localStorage    clov-guide-done
끝까지 보고 "시작하기"     영구           localStorage
프로필 → 가이드 다시 보기   둘 다 지움
```

**둘을 합치면 한쪽이 손해를 본다.** 급해서 건너뛴 사람은 다시 볼 기회가, 확실히 필요 없는 사람은 영구 차단이 필요하다. **되돌릴 길이 없으면 "다시는 안 보기"는 일방통행이다.**

### 12-3. ★★ 한글 픽셀 폰트는 답이 없다 — 분리한다

유명 16비트 폰트(Press Start 2P, Silkscreen)는 **전부 영문 전용**이다. 지금도 폰트만 4.3MB를 싣는다. **스파이디 트래커도 똑같이 했다** — 픽셀은 테두리·버튼·캐릭터가 만들고 **한글은 일반 폰트**다.

```
"START" · 로고   5×7 비트맵을 SVG rect로 직접 그림   폰트 0KB
한글 본문        기존 폰트 유지
```

### 12-4. ★★★ 마스코트는 A안(자동 변환) — 이유는 유지보수

크로비를 **손으로 찍어보기도 했다**(32×46 · 12색 · 도형+자동 외곽선). **B가 더 예쁘다.** 그런데 마스코트가 **7종**이고 스킨은 계속 는다 — 오늘만 하나 늘었다. B로 가면 새 스킨마다 픽셀을 새로 찍어야 하고 **사용자가 고른 마스코트를 못 보여준다.**

> B안 코드는 버리지 않았다(`clov-guide-v3.html`). **가이드 마지막 장면 같은 한 곳에만 손으로 찍은 크로비를 섞을 수 있다.**

### 12-5. ★★ 줄이는 순서가 결과를 갈랐다 (제일 중요)

64px·12색으로 처음 돌렸을 때 **오히려 더 지저분했다.** 색 수가 아니라 **줄이는 방식**이 문제였다.

```
니어리스트로 줄임  →  원본의 잡티 픽셀을 그대로 집어옴 → 12색으로 뭉개지며 얼룩
평균으로 줄임      →  면이 먼저 정리됨 → 색을 줄이면 딱 떨어짐
```

**줄일 때는 평균(`imageSmoothingEnabled=true`), 보여줄 때는 `image-rendering:pixelated`.** 롭·타코군은 면이 평평해 티가 안 났고 크로비에서 바로 드러났다.

### 12-6. "몇 픽셀"은 화면 배율과 같이 본다

```
32px → 220px로 띄우면 1픽셀 6.9px    64px → 3.4px  ★확정
96px → 2.3px (경계)                 128px → 1.7px  픽셀로 안 읽힘
```

**1픽셀이 화면 2px 아래면 픽셀 아트가 아니다.**

### 12-7. 시안 파일 — 레포에 넣었다

`work-logs/2026-08-05-온보딩-시안/` — **브라우저로 열면 그대로 동작한다**(스프라이트가 base64로 박혀 있어 서버가 필요 없다).

```
guide-v5.html          ★ 확정본 — 픽셀 창 + START + 4단계 + "다시는 안 보기"
pixel-tuner.html         해상도·색 수 조절기 (캐릭터 3종)
pixel-crobi-B안.html     손으로 찍은 픽셀 크로비 — 채택 안 했지만 코드를 남긴다
README.md                구현할 때 가져갈 함수 목록
```

**구현은 `guide-v5.html`에서 그대로 옮겨 심으면 된다** — `pixelize()`(평균 다운샘플 → median cut), 5×7 비트맵 폰트, 저장소 세 갈래가 전부 거기 있다.

### 12-8. 남은 결정 하나

**방이 0개일 때만 자동으로 띄울지, 모두에게 띄울지.** 리더는 "다 뜨는데 다시는 안 보기"를 골랐다. 다만 **방이 여러 개인 사용자에게 "우정공간이 뭐예요?"가 매번 뜨는 것**이 괜찮은지만 확인하면 된다 — 막으려면 `hasNoRooms` 한 줄이면 된다(`#344`에서 이미 만든 판정이다).

---

## 13. 집에서 시작할 때 첫 명령

```bash
git -C clov-api checkout main && git -C clov-api pull
git -C clov-web checkout main && git -C clov-web pull
git -C web-design-repository checkout main && git -C web-design-repository pull
```

**이 문서의 번호를 믿지 말고 `gh pr list`·`gh issue list`로 확인할 것.** 배포 상태는 `git log origin/deploy-myeongjun..origin/main`으로 본다 — **머지는 배포가 아니다.**

> ⚠️ **오늘 이 문서의 §0 항목 여러 개가 어제 그랬듯 금방 낡을 수 있다.** 어제 핸드오프 §0의 5건 중 3건이 **문서를 쓴 직후 몇 분 안에** 처리돼 있었다.
