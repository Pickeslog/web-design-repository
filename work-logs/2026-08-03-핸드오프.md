# 핸드오프 — 2026-08-03 (다음 세션 이어받기용)

> 자체완결 요약. 오늘의 한 줄: **마스코트가 5종 중 4종에서 살아 움직이고, 상점에 스킨 2종이 실제로 팔린다.**
> 앞선 맥락: [`2026-08-02-작업일지.md`](2026-08-02-작업일지.md) · [`2026-08-01-핸드오프.md`](2026-08-01-핸드오프.md)
> 오늘 있었던 일: [`2026-08-03-작업일지.md`](2026-08-03-작업일지.md)

---

## 1. 현재 상태

| 저장소 | main | 배포본 | 오늘 머지 |
|---|---|---|---|
| `clov-web` | `34d90c1` | **1커밋 뒤처짐** ⚠️ | 9건 |
| `clov-api` | `1d20d54` | 동일 ✅ | 7건 |
| `web-design` | `ecaa16a` | — | 3건 |

**⚠️ clov-web 배포가 밀려 있다** — `#226`(선물상자 테마, kimgyubi)이 오늘 배포 이후에 머지됐다. 다음 배포 때 같이 나간다.

### 열린 PR 4건 — 전부 chacha1650a 것이다

| PR | 상태 |
|---|---|
| **clov-api #92** 골드 지급 | 07-30부터 정체. **크리티컬 패스** — clov-web #178이 여기 물려 있다 |
| clov-web #224 헤더 골드 뱃지 | 마지막 확인 때 `CLEAN` · 리뷰만 하면 머지 가능 |
| clov-web #178 골드 배지 즉시 갱신 | `CHANGES_REQUESTED` · **머지 충돌** |
| clov-web #165 사용자설정 카드형 | `CHANGES_REQUESTED` · **머지 충돌** (07-30부터) |

07-31에 *"`Header.css`가 두 번 바뀌어 #180이 계속 충돌 중"* 이라고 적었는데 **#178·#165는 아직 그대로다.** rebase는 늦어질수록 비싸진다.

---

## 2. ★★ 마스코트 — 상태 머신이 캐릭터·스킨 공통이 됐다

핵심 구조 하나만 알면 된다.

```js
// Mascot.jsx
const hasFullStateSet = INTERACTIVE_STATES.every((s) => Boolean(stateSprites[s]))
```

**9개 상태 스프라이트가 다 있으면** 연타 dizzy · 무입력 sleepy · 드래그 lifted/scared/angry · 클릭 리액션(find·pencil·smile)이 **코드 수정 없이** 붙는다. 이 판정이 **캐릭터에도 장착 스킨에도 똑같이** 걸린다.

| 캐릭터 | 상태 |
|---|---|
| crobi · rob · tako-gun · kim-cheolsu · onyx | **9/9** ✅ |
| **burger-oldman** | **1/9** — 유일하게 상태 머신이 안 붙는다 |

**새 캐릭터를 추가하려면** `src/assets/mascot/<id>/`에 9종을 넣고 `MASCOT_STATE_SPRITES`·`SPRITES`·`LINES`에 등록한다. 규격은 `src/assets/mascot/_template/`.

**★ 계약·백엔드 순서를 반드시 지킬 것.**

```
1. 계약 §5 mascotType 허용값
2. clov-api @Pattern (+ UserIntegrationTest 허용값 배열)
3. clov-api 배포          ← ★ 빠지면 4번이 400 VALIDATION_FAILED
4. clov-web 렌더
```

07-31 버거노인·타코군·김철수, 08-01 선물상자, 오늘 오닉스까지 **다섯 번 같은 순서로 갔다.**

### 연타 판정을 목업에서 의도적으로 벗어났다

목업은 `900ms에 3번`인데 **`1000ms에 5번`으로 바꿨다.** 900ms에 3번은 클릭 간격 450ms — "연타"가 아니라 보통 클릭 속도라 그냥 교감하려던 사용자가 계속 어지러워졌다. 목업에서도 크로비가 같은 값이었지만 앱에서는 상태 스프라이트가 롭에만 있어 이 코드에 도달하지 않았고, **크로비 세트가 채워지면서 기본 마스코트를 쓰는 모든 사용자가 밟게 됐다.**

콤보 중간 클릭에 흔들림(nudge)도 붙였다. 그전엔 말풍선도 스프라이트도 API도 없이 **완전 무반응**이라 5번을 눌러도 체감이 2~3번이었다.

`find`·`pencil`·`smile`은 **교감 API에서 분리**했다. 순수 연출인데 `onSuccess` 안에 있어서 **하루 3회 교감에 갇혀** 특정 포즈를 하루에 볼 확률이 37%였다. 이제 클릭당 43%다.

---

## 3. ★ 스프라이트 정규화 — 도구가 있다

`clov-web/scripts/mascot/`

```powershell
.\normalize-sprites.ps1 -Character <캐릭터>       # 드라이런(기본)
.\contact-sheet.ps1 -Dir <폴더> -Out check.png    # 눈으로 확인
.\normalize-sprites.ps1 -Character <캐릭터> -Apply
```

**왜 필요한가** — 마스코트는 `height`만 고정하고 `width: auto`라, 원본 비율이 상태마다 다르면 **박스 폭이 같이 달라져 캐릭터가 좌우로 움직인다.** 크로비가 103~135px를 널뛰었다.

**배율 기준은 알파 면적의 제곱근**이다. bbox 높이로 맞추면 웅크린 `pencil`이 부풀고 줄에 매달린 `pulled`이 쪼그라든다. 머리 폭은 팔·소품에 오염돼 못 쓴다.

**★★ 이미 단일 규격인 폴더에는 돌리지 마라.** 면적 기준이 포즈별 소품의 잉크 양을 크기 차이로 오독한다 — 실측으로 `rob` 0.948~1.02, `kim-cheolsu` 0.99~1.09가 나왔다. 스크립트가 경고를 내니 **드라이런 표의 `scale` 열이 1에서 멀면 돌리지 말 것.**

Windows 전용(`System.Drawing`)이고 새 패키지를 안 쓴다. `.ps1`은 **UTF-8 BOM 필수**(PS 5.1이 ANSI로 읽어 파싱이 깨진다), `SpriteTool.cs`는 **ASCII만**(Add-Type 컴파일이 깨진다).

---

## 4. 상점 스킨 — 구조와 함정

**에셋** `clov-web/public/shop/skins/<mascot-id>/<skin-id>/` 9종 + `metadata.json` (규격은 `_template/README.md`)
**코드** `Mascot.jsx`의 `EQUIPPED_SKIN_STATE_SPRITES` 배열에 **경로 한 줄** 추가
**DB** `shop_items` 행 — 이게 없으면 상점에 아예 안 뜬다

`metadata.json`은 **런타임에서 안 읽는다.** 자산 인벤토리·DB 등록용이다.

### ★ 조용히 깨지는 값 둘

| | 틀리면 |
|---|---|
| `category`가 `SKIN` | 구매는 되는데 **장착이 막힌다** — `ShopService.equip`이 COSTUME만 허용 |
| `image_url` 오타 | `Mascot.jsx`가 `endsWith`로 매칭하는데 실패해 **상태 전환 없이 한 장**만 나온다 |

둘 다 **에러가 안 난다.** 등록 후 아래로 판정까지 확인할 것.

```sql
SELECT s.code, s.price, s.status,
       CASE WHEN s.category='COSTUME' THEN 'OK' ELSE '★ 장착 불가' END AS category_chk,
       CASE WHEN s.image_url = x.expected THEN 'OK' ELSE CONCAT('★ ', s.image_url) END AS path_chk
FROM shop_items s JOIN ( /* code, expected 쌍 */ ) x ON x.c = s.code;
```

### 현재 등록된 스킨

| code | 가격 | 등급 |
|---|---|---|
| `COSTUME_ROB_ARCADE_BOSS` | 9,800 | LEGENDARY |
| `COSTUME_KIM_CHEOLSU_SAFETY` | 1,500 | UNCOMMON |

**가격은 카탈로그 수치로 정했다** — LEGENDARY 실효가 6,860~12,000, 여름밤 12,000은 `EVENT`(기간 한정)라 **상시 판매 최고가**를 아케이드 보스가 갖는다. UNCOMMON은 별빛 1,200·포레스트 1,500 선.

**데모용으로 가격을 내리지 말 것.** 되돌리기 전에 산 계정에는 `user_inventory_items.paid_price`에 낮은 값이 그대로 남는다. 대신 `clov-api/db/maintenance/grant-test-gold.sql`로 지갑을 채운다(`reason`은 `SIGNUP_GRANT` — `EARN_` 계열을 쓰면 하루 상한 500 합산에 잡혀 그날 정상 획득이 막힌다).

---

## 5. ★★ 오늘 배운 사고 둘 — 다음에도 밟는다

### ① MERGED인데 코드가 main에 없다

`#114`가 base를 피처 브랜치로 잡은 **스택 PR**이었는데, `#112`가 스쿼시 머지된 뒤에도 그 브랜치가 **원격에 살아 있어서** GitHub이 리타깃을 안 했다. `#114`는 이미 main에 반영된 옛 브랜치로 머지됐고, **코드는 main에 없었다.**

```
$ git grep -c "SUB_JOIN_ACCEPTED" origin/main -- src/main/java
0건
```

→ **스택 PR을 쓸 때는 base 브랜치가 머지 시 삭제되게 할 것.** 그러면 GitHub이 자동 리타깃한다.
→ 이미 벌어졌으면 **델타만 떼어 main 위에 재적용**한다(08-01 `#92` 때와 같은 방법).

### ② 배포가 초록인데 서비스가 4분 30초 죽어 있었다

```
03:32:58  배포 success        03:33~35  502 (4회)        03:35:30  401 정상
```

워크플로가 `Container api Started`만 보고 초록불을 낸다. **clov-api에는 헬스체크가 없다**(이슈 `clov-api#108`) — 08-02에 열어둔 이슈가 오늘 실제 피해로 관측됐다. clov-web은 `#182`에서 넣어둬서 `웹 응답 확인 OK (1회차)`가 찍힌다.

→ **배포 후 `curl`로 직접 확인한다.** `/api/v1/rooms`가 **401**이면 정상(인증 필요), **502면 아직 안 뜬 것**이다.

---

## 6. 다음에 할 것

### 리더

1. **clov-web 배포** — `#226`이 1커밋 밀려 있다
2. **chacha PR 4건 처리** — `#224`는 `CLEAN`이라 리뷰만 하면 되고, `#178`·`#165`는 본인 rebase가 필요하다. `clov-api#92`가 풀려야 `#178`이 이어진다
3. **`crobi/dizzy.png` 오프모델** — 검정 모자·흰 셔츠·회색 바지라 **어지러울 때 크로비가 옷을 갈아입는다.** 프로토타입 원본이 그렇다. 나머지 8장에 맞춰 다시 뽑아야 한다
4. **버거노인 8종** — 유일하게 상태 머신이 안 붙는 캐릭터

### 팀원

| 담당 | |
|---|---|
| **lami2342** | `#230` JoinRoom 리팩터. **토큰 정본 판단은 이슈 코멘트에 답해뒀다** — 현재 코드(전역 `tokens.css`)가 정본이고 목업 토큰 이름을 따라가면 안 된다 |
| **chacha1650a** | `clov-api#92` → `clov-web#178` → `#165` → `#224`. **열린 PR 4건이 전부 이 사람 것** |
| **kimgyubi1234** | `#217` 완료. 다음 대기열 |

### 열린 이슈 (미배정)

- **clov-api #108** 배포 헬스체크 부재 — 오늘 실제 피해가 났으니 우선순위가 올라갔다
- **web-design #42** DB 스펙 DDL 주석에 `MEMBER_JOINED` 누락
- **clov-web #223** 메인 화면 재화 UI(chacha)
- **미등록** — `findCatalog`에 `status` 조건이 없어 **INACTIVE 아이템이 목록에 뜨고 사면 실패한다**("보이는데 못 사는" 상태). 이슈로 안 만들었다

---

## 7. ⚠️ 미결·주의

### 검증 제약 (안 지키면 헤맴)

- **Claude는 로그인 뒤 화면을 못 본다.** 마스코트·상점이 전부 대시보드 안이라 오늘도 코드 대조까지가 한계였다
  - **우회법 둘** — lami2342가 `#228`에서 **axios 어댑터를 스텁**해 실제 렌더를 확인했다(가장 좋은 방법). 그리고 **정적 자원은 로그인 없이 `curl`로 검증된다**(오늘 스킨 에셋 18개 전수 200 확인)
- **★ Browser pane이 안 보이면 이미지 디코드도 안 된다.** `new Image()`가 `onerror`로 떨어지고 `naturalWidth`가 0인데, **`fetch`는 200에 정상 바이트를 준다** — "경로는 맞는데 이미지만 깨진 것"으로 오진하기 쉽다. PNG 치수가 필요하면 헤더를 직접 읽을 것(`readUInt32BE(16)`=width, `(20)`=height, `readUInt8(25)`=컬러타입)
- **Docker 미설치 → Testcontainers 로컬 실행 불가.** `compileJava`/`compileTestJava`까지만 로컬
- **JDK가 PATH에 없다** — `C:\Users\user\.jdks\temurin-21.0.10`. `$env:JAVA_HOME` 먼저 세팅

### 이월

- **`_bak_user_wallets_20260802`** 스냅샷 테이블이 DB에 남아 있다 — 데모 끝나면 `DROP TABLE`
- **테스트 골드** — `grant-test-gold.sql`로 지급한 잔액이 남아 있다. 데모 전에 `§3` 되돌리기로 정리할 것
- **certbot 갱신** — 인증서 10/26 만료
- **`StoragePresigner`에 삭제 메서드 없음** — `deleteImage`가 DB 행만 지우고 R2 객체는 남는다
- **web-design `#16`** — `#27`로 해결됐는데 열려 있다

---

## 8. 집에서 시작할 때 첫 명령

```bash
git -C clov-api checkout main && git -C clov-api pull
git -C clov-web checkout main && git -C clov-web pull
git -C web-design-repository checkout main && git -C web-design-repository pull
```

**이 문서의 번호를 믿지 말고 `gh pr list`·`gh issue list`로 확인할 것.** 그리고 **배포 상태는 `git log origin/deploy-myeongjun..origin/main`으로 확인한다** — main 머지는 배포가 아니다.
