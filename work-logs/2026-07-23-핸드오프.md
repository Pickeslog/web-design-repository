# 핸드오프 — 2026-07-23 (새 컨텍스트 이어받기용)

> 자체완결 요약. 상세 진행·교훈은 로컬 메모리 `clov-production-build-workflow`(새 세션 자동 로드)에 누적돼 있음.

---

## 1. 현재 상태 (한 줄)
- **clov-web main = `87b9475`** · **clov-api main = `25a679e`** · **web-design main = `6149db9`**
- **세 레포 모두 열린 PR 0 · 작업트리 클린 · 오늘 작업 전부 머지 완료.**

## 2. 오늘(2026-07-23) 한 것
1. **초대 코드 A안 구현 (clov-api #55 머지)** — "방당 고정 회전 코드": `room_invites` `UNIQUE(room_id)`, `create`=upsert(제자리 회전), `accept`=다회용(USED 마킹 제거), 취소/만료는 `INVITE_EXPIRED`. **프로덕션 DB 마이그레이션 적용·검증 완료**(방당 중복 0, `uk_room_invites_room` 존재). 8080 재빌드 완료. 계약§7·DB SSOT 갱신.
2. **포트폴리오 케이스 스터디** — 초대→입장 3중버그(#72·#73·#54) 트러블슈팅 + A안 구조개선. work-log `2026-07-23-초대플로우-트러블슈팅-및-초대코드-구조개선.md` + 아티팩트(claude.ai/code/artifact/319412f0).
3. **P3 프로토타입 정렬 (사실상 종료)** — 로그인/가입(clov-web #74)·설정 native color-scheme(clov-web #75) 머지. **게이트 앞 화면은 Claude가 직접 브라우저 계산값으로 self-verify**(로그인/가입), 설정은 **격리 하니스**(임시 `/dev/settings`+mock QueryClient, 검증 후 되돌림)로 검증. 발견·수정: 다크OS 대비붕괴(항상 라이트 핀)·제목 Outfit·native date color-scheme. **측정 교훈: 테마 토글 직후 getComputedStyle은 CSS transition 중간값 → `transition:none` 후 재측정.**
4. **배포 준비 (clov-api #56 머지)** — OAuth redirect env화(`APP_OAUTH_REDIRECT_BASE`·`APP_OAUTH2_REDIRECT_URL`, dev 기본값 유지) + `server.forward-headers-strategy=framework` + `deploy/`(nginx-clovlabcalss.conf·clov-api.service·clov-api.env.example·**DEPLOY.md 런북**). **결정: VM 로컬 MySQL · 단일 오리진 nginx · certbot HTTPS.**
5. **팀원 PR 리뷰 (web-design #1 머지)** — kimgyubi 명세(씬배너 잔재 제거→LP턴테이블 정합, 마스코트/XP/레벨/생일 명세 보강). 검토=프로토타입 `js/space.js`(XP 가속·기억의 샘)·백엔드(마스코트 +2·flat 500/lv)와 대조, 정확한 문서화로 판정.
6. **org 프로필 README 최신화** — 공개 `.github`(리더 커밋 `38d42b4`) + 멤버 `.github-private`(`0dff7d5`) 둘 다 새 버전(스택·리포구성·팀&협업[AI 페어]·현재상태). 개인 로스터는 미포함.

## 3. ★★ 대기·주의 (반드시 지킬 것)
- **배포·도커는 리더가 "시작하자" 할 때까지 착수 금지**(2026-07-23 지시). Claude가 먼저 배포를 밀지 말 것.
  - 배포 방식 = **Docker 확정**(bare-metal systemd 아님). #56의 OAuth env화·nginx·forward-headers는 Docker에도 재활용, systemd 유닛·bare-metal 런북만 Dockerfile+compose로 대체 예정. **아직 Docker 파일 미작성.**
- **PR 머지는 분류기가 Claude 차단** → 리더가 직접 `gh pr merge`. (생성·리뷰·CI확인까지 Claude)
- 공유 워킹트리(clov-web·clov-api 각각 Codex와) → `git add -A` 금지, 내 파일 경로만.

## 4. 남은 것 (급하지 않음)
- **초대 라이브 2계정 확인**(선택) — 로그인+2계정이라 리더 브라우저, 또는 리더가 토큰(localStorage `clov-auth`.state.accessToken) 주면 Claude가 API로.
- 초대 후속(소규모): `InviteService.accept`는 이미 `@Transactional` 있음(스테일 아님 확인됨). 초대코드 다회용 전환 완료.
- 캐시 이니셔티브(Caffeine hot-GET) — 탐구 단계, 미착수.
- P3 남은 = 의도적 제외만(대시보드 배경·대표사진·인생4컷 극장).

## 5. 환경 / 참조
- gh 전체경로 `C:\Program Files\GitHub CLI\gh.exe`(인증 myeongjundev). Java21. 로컬 clov-api 테스트는 `./gradlew compileJava compileTestJava`(Docker 없어 통합테스트는 CI만).
- 검증: `npm --prefix clov-web run lint && build`. dev=`preview_start name=clov-web`(5173), 백 8080. **브라우저 screenshot 툴 타임아웃 → `javascript_tool` 계산값 측정으로 검증.**
- SSOT: `docs/API-CONTRACT.md` · `api-spec/05-db-unified-final.md`. 배포: `clov-api/deploy/DEPLOY.md`.
- 로컬 메모리: `clov-production-build-workflow`(진행·교훈 SSOT) · `clov-build-env-setup` · `clov-room-invite-flow-work`.
