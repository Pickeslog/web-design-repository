# 2026-08-04 작업일지 — 에러가 안 나는 결함만 넷을 잡은 날

> **한 줄 요약**: PR **29건**이 머지됐다. 스킨이 **5종 → 12종**이 되고 마스코트가 **방 안 4개 화면**으로 넓어졌으며, **알림 도메인이 계약·백엔드·프론트 세 겹 다** 맞았다.
>
> 그 사이 잡은 결함 넷이 **전부 `lint`·`build`를 통과하는 종류**였다 — 없는 아이콘, 죽은 애니메이션, 조건이 빠진 목록 쿼리, 조용히 상품을 덮어쓰는 UPSERT(§10).
>
> 하루 끝에 **골드가 한 푼도 안 들어오고 있다**는 걸 알았다. 버그가 아니라 **지급 코드가 main에 없었다**(§8). 그리고 **카탈로그 총액을 세 번 틀렸는데 전부 재지 않고 계산해서였다**(§9).
>
> 이 문서는 **오늘 무슨 일이 있었는지의 기록**이다.
> **다음에 뭘 할지**는 [`2026-08-04-핸드오프.md`](2026-08-04-핸드오프.md)에 있다.
>
> 앞선 맥락: [`2026-08-03-핸드오프.md`](2026-08-03-핸드오프.md) · [`2026-08-03-작업일지.md`](2026-08-03-작업일지.md)

---

## 1. 숫자로 본 하루

| | |
|---|---|
| 머지된 PR | **39건** — clov-web 21 · clov-api 9 · web-design 9 |
| 닫힌 이슈 | **14건** (clov-web 10 · clov-api 2 · web-design 2) |
| 새로 연 이슈 | **11건** |
| 코드 증감 | clov-web `+1,564 / −549` (92파일) · clov-api `+595 / −21` (16파일) · web-design `+1,055 / −54` |
| 배포 | clov-web **8회** · clov-api **3회** |
| DB 작업 | `shop_items` **12행 등록** + **11행 판매 종료** + **4행 수정** |

**커밋 앵커** — `clov-web 8bcbd4e` · `clov-api d24f6f2` · `web-design ec8ce4b`

### 사람별 머지

| | 건수 | 무엇 |
|---|---|---|
| **myeongjundev**(리더) | **16** | 스킨 7종(에셋·배선·SQL) · 마스코트 4화면 확산 · 아이콘 수정 · 카탈로그 정리 · 계약 7건 |
| **lami2342** | **10** | 인증 화면 4건(#241·#254·#257·#260·#261) · 알림 도메인 3겹(#242·#250·#124·#128) · 계약 이동(#52) |
| chacha1650a | 2 | 일정 상세 티켓 UI(#246) · OAuth 만료 오탐(#127) |
| kimgyubi1234 | 1 | 대시보드·헤더·피드 UI 폴리시(#243) |

리뷰는 **12건** 했다 — 승인 8(#241·#242·#250·#257·#260·#261·#124·#128), 변경 요청 후 승인 1(#52), 변경 요청 3(#243·#254는 리더 판단으로 그대로 머지 · `clov-api#92`는 대기 중).

> **lami2342가 오늘 팀에서 가장 많이 움직였다(10건).** 어제 핸드오프에 "다음 대기열"로 적혀 있던 사람인데, **알림 도메인을 계약·백엔드·프론트 세 겹 전부** 끝내고 저녁엔 **인증 화면 이슈 4건을 직접 발굴해 전부 PR까지** 냈다. 그중 `#253`은 마우스 없이는 가입이 안 되던 접근성 차단이었다.

---

## 2. 줄기 하나 — 스킨이 다섯에서 열둘로, 마스코트가 한 화면에서 네 화면으로

아침에 스킨은 5종이었고 마스코트는 **우정공간에서만** 보였다.

```
오전  오닉스 '블랙스타 공방'  ·  롭 '옐로우 크루'  ·  타코군 '수박껍질모자'
      마스코트를 추억피드·행운편지·일정계획에도
저녁  크로비 '마법학교 학생' · 크로비 '개발 경력자' · 김철수 '신입 개발자' · 롭 '탐험가'
```

| | 아침 | 저녁 |
|---|---|---|
| 스킨 | 5종 | **12종** |
| 스킨을 가진 마스코트 | 4/6 | **5/6** (버거노인만 없다) |
| 마스코트가 보이는 화면 | 1곳 | **4곳** |
| 등급 층 | COMMON 비어 있음 | **네 층 전부 채워짐** |
| 상점에서 살 수 있는 것 | 자리표시 12종 + 스킨 5종 | **전부 스킨** (자리표시 11종은 판매 종료, §9) |

**크로비가 3종으로 가장 많아졌다.** 하루에 스킨이 2.4배가 됐다.

**COMMON 층이 처음 열린 게 크다.** 900골드는 시작 골드 1,000 아래라 **가입 첫날 바로 살 수 있고**, 거기에 캐릭터가 둘(롭·타코군)이라 첫 스킨에 선택지가 생긴다.

### 등급마다 같은 값 쌍이 생겼다

```
COMMON      900  옐로우 크루 · 수박껍질모자
UNCOMMON  1,500  철골 작업반 · 블랙스타 공방
EPIC      6,000  마지막 신호병 · 오션 구조대
LEGENDARY 9,800  벚꽃 세트 · 아케이드 보스
```

어제 정한 규칙 — **등급도 같고 내용(상태 9종)도 같으면 값도 같게 둔다** — 이 그대로 네 번 적용됐다. 값을 가르는 두 축이 모두 같은데 다른 값을 붙이면 그게 근거 없는 숫자다.

COMMON만 예외적으로 **새 최고가**를 만들었다. 기존 두 줄(아이보리 500·클로버 배지 600)이 장식 하나짜리라 상태 9종 스킨과 내용이 다르기 때문이다. 선례도 있다 — ROV 조종사가 RARE 최고 2,400을, 마지막 신호병이 EPIC 최고 5,200을 넘었다.

---

## 3. ★★ 같은 결함에 반대 판정을 두 번 내렸고 둘 다 맞았다

오늘 가장 값진 것이다.

두 스킨에서 **똑같은 종류의 결함**이 나왔다. 배경 제거가 덜 끝나 캔버스 코너와 최하단 행에 고립 픽셀이 남아 있었다.

| | 알파 | bbox 하단 | 판정 |
|---|---|---|---|
| `rob/yellow-crew/angry.png` | **41** (코너), 1~8 (나머지) | **639** — 같은 폴더 다른 8장(471~539)에서 168px 벗어남 | **지웠다** |
| `tako-gun/watermelon` 9장 전부 | **3~8** | 594~626 — 정상 대역 | **안 지웠다** |

### 기준은 "잔여물이 있느냐"가 아니었다

```csharp
// scripts/mascot/SpriteTool.cs
private const int ALPHA_THRESHOLD = 8;
...
if (buf[row + x * 4 + 3] > ALPHA_THRESHOLD) {
```

**정규화 도구가 알파 8 이하를 이미 무시한다.** 그러니 판정은 이렇게 갈린다.

- 수박껍질모자의 알파 3~8 → **도구도 렌더도 못 본다.** 9장을 재인코딩하면 폴더가 300KB 넘게 커지는데 얻는 게 없다
- 옐로우 크루의 알파 41 → **임계값을 넘는다.** bbox가 캔버스 바닥까지 잡혀서, 이 상태로 정규화를 돌리면 면적 기준 배율 계산이 망가진다

> **판정 기준을 "결함이 있느냐"에 걸면 둘 다 지운다. "도구와 렌더가 그걸 보느냐"에 걸어야 둘이 갈린다.**
> 어제 정규화 판정을 `scale`이 아니라 `캔버스 균일성`에 건 것과 같은 계열이다 — **기준은 증상이 아니라 원인에 건다.**

### 순수 빨강이 단서였다

옐로우 크루의 잔여 픽셀 중 둘이 `RGB(255,0,0)`이었다. **노란 로봇·파란 멜빵·갈색 케이블에 순수 빨강이 있을 자리가 없다.** 크로마키 배경을 지운 흔적이다.

중간에 "크로마키 흔적" 자동 검출기를 만들어봤는데 **오닉스에서도 100건 넘게 잡혔다.** 반투명 아웃라인을 오탐한 것이라 버렸다. 검출기를 믿지 않고 **면별 분해(상/하/좌/우)로 돌아간 게 맞았다** — 케이블이 우측을 관통하는 것(정상)과 코너 잔여물(결함)이 그때 갈렸다.

### 이건 재발 중이다

스킨 **두 종 연속**으로 나왔고, `metadata.json`의 `states` 값이 **두 번 다 절대 경로**였다(기존 6종은 파일명만 쓴다). 매번 검사해서 잡는 것보다 **내보내기 쪽에서 끝내는 게 맞다.**

---

## 4. ★★ 알림 `type`을 무엇으로 가를지 정했다 — 그리고 첫 답을 뒤집었다

lami2342가 `clov-api#122`를 열었다. **나가기 확인창은 "남은 멤버에게 나갔다고 알림이 가요"라고 하는데 실제로는 안 간다.** 계약 §13 프로즈도 "입·퇴장"을 약속하는데 표에 행이 없었다.

`MEMBER_LEFT`를 신설하는 건 명확했다. 문제는 **어느 `type` 탭에 넣느냐**였다.

### 처음엔 JOIN이 맞아 보였다

짝인 `MEMBER_JOINED`가 JOIN에 있으니 대칭이 맞고, FRIEND에 넣으면 입장과 퇴장이 다른 탭으로 갈린다. 그렇게 정리해서 이슈에 올렸다.

### 코드를 보고 전제가 틀렸음을 알았다

```js
// Notifications.jsx
enabled: activeTab !== 'JOIN',                                       // :59  JOIN이면 알림 조회를 안 한다
const joinRequestsQuery = useQuery({ ... getJoinRequests(roomId) })  // :61  대신 가입신청 리소스
{tab.id === 'JOIN' && pendingRequests.length > 0 && ...}             // :130 뱃지도 신청 건수
{activeTab === 'JOIN' ? <JoinRequestList ... /> : ...}               // :136
```

탭 라벨도 `'가입 신청'`이다. **JOIN 탭은 알림 피드가 아니라 승인/거절하는 처리함이었다.**

그래서 두 가지가 뒤집혔다.

1. 내가 걱정한 **"JOIN_REQUEST 건수 희석"은 근거가 없었다** — 뱃지가 알림이 아니라 가입 신청 리소스를 센다
2. 대신 더 큰 문제가 있었다 — **JOIN에 넣으면 알림 목록 어디에도 안 뜬다**

### 그래서 기준을 다시 세웠다

계약 §13이 이미 **"`type` = 어느 탭에 보일지"** 라고 정의하고 있었다. 주제 분류가 아니다. 그러면 탭의 성격이 곧 기준이 된다.

> **알림은 주제축("멤버에 관한 것끼리 묶는다")이 아니라 행동축("처리할 게 있나")으로 먼저 가른다.**
> 처리함은 비워야 하는 통이라, 안 없어지는 정보성 항목이 섞이면 뱃지가 0이 되지 않고 그때부터 사용자가 뱃지를 무시한다.

| | 사용자가 할 일 | 통 |
|---|---|---|
| 가입 신청 | 승인/거절해야 함 | 처리함 |
| 입장·퇴장·추억·약속·레벨업 | 알기만 하면 됨 | 피드 |

`MEMBER_LEFT`는 **FRIEND**로 확정했다(`web-design-repository#50`).

### 원칙이 그 자리에서 확산됐다

같은 구조 때문에 **`JOIN_ACCEPTED`도 알림 화면에 안 뜬다**는 게 드러났다. 어제 `#113`으로 "승인됐는데 아무 소식이 없는" 빈자리를 채웠는데, **채운 알림이 정작 알림 화면에 없었다.** 빈자리가 반만 채워져 있던 것이다.

더 파보니 `type='JOIN'`은 알림 테이블에서 **사실상 죽은 카테고리**였다.

| subType | 성격 | 생산 | 알림 화면에 뜨나 |
|---|---|---|---|
| `JOIN_REQUEST` | 처리형 | **안 됨** | — |
| `MEMBER_JOINED` | 정보성 | 됨 | **안 뜸** |
| `JOIN_ACCEPTED` | 정보성 | 됨 | **안 뜸** |

**처리형은 안 만들어지고, 만들어지는 건 전부 정보성인데 볼 수가 없다.** `web-design#51`로 이슈를 떼고 lami2342에게 배정했고, 같은 날 `#52`로 계약이 정리됐다.

---

## 5. 리뷰 — 막은 것과 통과시킨 것

### 통과 — `#124`에서 위험한 자리 둘을 확인했다

lami2342가 `MEMBER_LEFT`를 구현하면서 **순환 의존**을 만났다(`RoomService → NotificationService → RoomService`). `NotificationService`가 `RoomService` 대신 `RoomMemberMapper`를 직접 받도록 끊었는데, 이건 **기존 API 두 개의 인가 판정이 바뀔 수 있는 자리**다.

- 복제한 `assertActiveMember`가 원본과 **같은 mapper·같은 조건·같은 에러코드**임을 대조했다
- 팬아웃이 `status='LEFT'` 갱신 **뒤에** 불리는데, `fanOutToActiveMembers`가 `status='ACTIVE'`와 `user_id != actorId` **양쪽으로 거른다** — 이중으로 안전하다

테스트가 **총 건수를 1로 못박은 게** 좋았다. 수신자 조건만 봤으면 "나간 사람에게도 한 건 더 갔다"를 못 잡는다.

### 막은 것 — `#243`의 `<ScrollRestoration />`

kimgyubi1234가 필름스트립 스크롤 버그를 고치면서 `router.jsx` 최상위에 `<ScrollRestoration />`을 "안전망으로" 넣었다.

**틀린 코드가 아니다.** 라우트 이동 시 스크롤이 남는 건 실제 문제고 이게 표준 해법이다. 다만 —

1. **원인은 이미 고쳐졌다** — 필름스트립 `scrollLeft` 수정으로 증상은 사라진다. 이건 그 위에 얹는 별개 개선이다
2. **적용 범위가 앱 전체다** — 프로젝트에 기존 사용이 0건이라 되돌아갈 기준선도 없다
3. **검증이 안 됐다** — 본인도 "로그인 화면이라 직접 확인 못 함"이라 적었다

> **한 PR에 원인 수정과 전역 정책 변경이 같이 있으면, 나중에 스크롤이 이상할 때 어느 쪽 탓인지 가릴 수 없다.**

상태 메시지 제한도 짚었다. 이슈는 "한글20/영어40 안내를 유지한다"고 했는데 가중치 로직을 걷어내 **한글 한도가 2배(20→40자)가 됐다.** 방향은 (A)가 낫다고 봤지만 — 실제로 기존은 **입력 한도(40자)와 저장 한도(가중치 40)가 어긋나** 한글 21자부터 저장 버튼이 죽던 상태였다 — **이슈가 유지하라고 한 것을 뒤집었으면 본문에 드러나야 한다.**

**리더 판단으로 그대로 머지됐다.** 두 항목은 현재 main에 있다.

### 변경 요청 후 승인 — `#52`

구조는 전부 맞았는데 본문 두 문장이 사실과 달랐다. 특히 이것 —

> 순서가 반대면 입장·수락 알림이 잠깐 "◯◯님의 행운편지"로 잘못 뜨는 회귀가 생긴다.

**`#242`가 머지되면서 `FEED_META.FRIEND`가 삭제돼 이미 틀린 말이 됐다.** 실제로는 `DEFAULT_META`("◯◯님의 새 소식")로 떨어진다 — 모호할 뿐 틀리지 않은 문구다.

계약은 SSOT라 **순서 압박이 실제보다 세게 적혀 있으면 나중 판단이 왜곡된다.** lami2342가 두 문장을 한 항목으로 합쳐서 고쳤는데, 원래 같은 이야기였던 걸 나눠 적었던 거라 **내 제안보다 나았다.**

### 저녁에 셋을 더 봤다

`#250`(프론트 문구 이동)·`#128`(백엔드 type 전환)은 승인했다. `#128`은 **`TYPE_JOIN` 생산자를 전수로 세어** 빠진 자리가 없음을 확인했고, 테스트가 **`type` 단언만 바꾸고 건수·수신자·actor는 그대로 둔 것**이 좋았다 — 같이 풀었으면 `#90`·`#113`에서 잡은 회귀를 못 잡는다.

**남은 하나가 `clov-api#92`인데, 이건 리뷰보다 그 앞의 진단이 컸다 → §8.**

---

## 6. 마스코트를 네 화면으로 — 라우터를 안 건드린 이유

캐릭터를 고르고 스킨을 사도 **볼 수 있는 자리가 우정공간 한 곳뿐**이었다.

방법이 둘이었다.

| | 얻는 것 | 치르는 것 |
|---|---|---|
| **화면별 마운트**(택함) | 3줄, 위험 없음 | 화면 이동 시 리마운트 |
| 레이아웃 라우트 | 마스코트 상태 유지 | `index` route 도입·경로 매칭 변화 |

**리마운트로 잃는 게 수면 20초·연타 1초·말풍선 1.8초짜리 짧은 타이머뿐이다.** 화면을 옮기는 순간 사용자 주의도 옮겨가서 체감이 거의 없다. 반면 라우트 계층 변경은 회귀 위험이 실재한다 — **같은 날 `#243`에서 `<ScrollRestoration />` 하나를 검증 부족으로 막았는데, 라우트 계층을 바꾸는 건 그보다 크다.**

### 함정 하나를 확인했다

`Letters`·`Schedule`은 루트에 라이트 팔레트를 `style`로 강제한다(다크 모드에서도 라이트로 렌더). 마스코트가 그 안에 들어가면 **CSS 커스텀 프로퍼티를 상속받는다** — `position: fixed`라도 변수는 시각적 박스가 아니라 DOM 트리를 타기 때문이다.

대조 결과 `Mascot.css`가 쓰는 변수는 **`--clov-mascot-size` 하나뿐**이고, 그건 `mascotSize.js`가 `:root`에 직접 세팅한다. 두 팔레트의 13개 변수와 안 겹치고 `:root` 값이라 페이지 래퍼가 덮을 수도 없다.

z-index도 안전했다 — 세 화면의 `position: fixed` 요소가 전부 모달(240~9600)이라 마스코트(40)가 뒤에 깔린다. **모달을 열면 가려지는 게 맞는 동작이다.**

---

## 7. 배포 — 일곱 번 다 인증 없이 증명했다

clov-web **5회**, clov-api **2회**. 인증이 걸린 화면을 못 보는 상태에서 **일곱 번 다 "올라갔다"를 눈이 아니라 응답으로 증명했다.**

| 회차 | 무엇 | 증명 방법 |
|---|---|---|
| web 1 | 스킨 3종 + lami·kimgyubi 수정 | `public/` 에셋 6개 GET — **바이트까지 디스크와 일치** |
| web 2 | 마스코트 4화면 + 티켓 UI | 로컬 `dist/assets/`의 **콘텐츠 해시 파일명**을 배포 도메인에 요청 → 200 |
| web 3 | `#250` 알림 문구 이동 | 배포된 번들을 받아 **바뀐 문자열을 직접 찾음** |
| web 4 | 스킨 4종 + 아이콘 수정 | 에셋 바이트 대조 + 번들에서 `ti-circle-off` 1건 · `ti-bubble` **0건** 확인 |
| web 5 | Signup Enter · OAuthRedirect 이식 | **운영 도메인을 브라우저로 직접 열었다** — 아래 참조 |
| api 1 | `#124` + `#127` + `#128` | `/api/v1/rooms` → **401**(502 아님) |
| api 2 | `#130` 카탈로그 status 필터 | 401까지 **17초** · CI에서 새 통합 테스트 통과 |

### 증명 방법을 한 단계 더 밀었다

1·2회차는 **파일이 올라갔다**까지였다. 3회차는 **그 파일 안에 오늘의 코드가 있다**를 봤다.

```
/assets/index-BOYC_Q_9.js   676,484B
  MEMBER_LEFT:{cls:`join`,icon:`ti-user-minus`,tail:`님이 나갔어요`,fallback:`멤버가 나갔어요`}
```

해시 대조는 **로컬에서 같은 빌드를 돌려야** 성립하는데, 번들에서 문자열을 찾는 건 **로컬 빌드 없이도 된다.** 배포본이 진짜인지 묻는 데는 이쪽이 더 직접적이다.

### ★★ 5회차에서 한 칸 더 갔다 — 운영 화면을 직접 봤다

`OAuthRedirect`는 **로그인 게이트 밖**이다. 그래서 배포된 도메인을 브라우저로 열어 **렌더된 결과를 직접 읽었다.**

```
https://clovlabcalss.store/oauth2/redirect?code=<아무 값>

.proto-oauthredirect   있음
emotion css-* 클래스   0개        ← 이식이 끝났다는 뜻
box-sizing             content-box  ← lami가 일부러 보존한 값 그대로
콘솔 에러              0건
화면                   "인증 시간이 만료되었어요. 다시 로그인해주세요."
```

**그 문구가 나온 게 덤으로 하나를 더 증명했다.** 오전에 `clov-web#247`을 닫으면서 *"`clov-api#127`의 401→400 변경이 문구는 그대로 두고 refresh 왕복만 없앤다"* 를 코드로만 판단했는데, **운영에서 실제로 그 문구가 나왔다.**

> **"로그인 뒤 화면을 못 본다"가 "아무것도 못 본다"는 아니다.** 인증 화면 넷(Login·Signup·JoinRoom·OAuthRedirect)은 게이트 밖이라 실제 렌더를 볼 수 있다. 오늘 `#259`(shake 애니메이션)도 그래서 재현으로 잡았다(§9).

`REACT_ENV_deploy-myeongjun` 시크릿이 없어 레포의 `.env.production`을 쓴다는 경고가 다섯 번 다 떴다 — **공용 도메인으로 나간다는 뜻이고 리더 배포에서는 의도한 동작이다.**

### ★ clov-api 배포를 하루 종일 미뤄서 502를 한 번만 겪었다

`#124`가 오전에 머지됐지만 **배포하지 않고 기다렸다.** `#127`·`#128`이 같은 날 들어올 게 보였기 때문이다.

```
01:43  #124 머지 (MEMBER_LEFT)      ← 배포 안 함
02:19  #127 머지 (OAuth 만료 오탐)   ← 배포 안 함
02:27  #128 머지 (type FRIEND)
02:29  배포 1회 — 세 개가 같이 나감
```

레포에 헬스체크가 없어 **배포마다 4분 30초쯤 502가 난다**(`clov-api#108`, 08-03 실측). 세 번 나눠 쐈으면 **13분 30초**를 죽었을 자리다.

> **머지는 배포가 아니다.** 이 간격이 있어야 묶을 수 있다. 헬스체크가 없는 레포에서는 **머지 즉시 배포가 오히려 손해다.**

배포 후 확인도 워크플로 초록불이 아니라 `curl`로 했다. **초록불은 "컨테이너가 떴다"까지고 "서비스 정상"이 아니다.** `/api/v1/rooms`가 401이면 인증 필터까지 살아 있다는 뜻이다.

---

## 8. ★★ 골드가 "안 들어오는" 게 아니라 "만든 적이 없었다"

하루 끝에 이 질문이 왔다.

> 새 D-day 만들기, 글쓰기 할 때마다 골드가 들어와야 하는데 지급이 안 되고 있는 것 같다. 돈이 들어와야 사용자가 상점을 쓸 수 있지 않겠나.

**버그로 접근했으면 `MemoryService`를 뒤졌을 것이다.** 먼저 확인한 건 지급 코드가 있느냐였다.

```
$ git grep -n "EARN_" origin/main -- src/main
(0건)
```

`WalletTransaction`에 있는 사유는 **`PURCHASE`와 `SIGNUP_GRANT` 둘뿐**이었다. 행동 지점 여섯 곳(`MemoryService` 2 · `PlanService` 2 · `MascotService` 1 · 이미지 보너스 1)이 전부 `expService.grant(...)`만 부른다 — **XP는 주는데 골드는 안 준다.**

| | main의 실제 동작 |
|---|---|
| 들어오는 길 | 가입 시 `SIGNUP_GRANT` 1,000 — **평생 한 번** |
| 나가는 길 | `PURCHASE` |
| 버는 길 | **없음** |

> **골드가 단방향이었다.** 1,000을 받아서 다 쓰면 그 계정은 상점이 영구히 닫힌다. 카탈로그는 71,660짜리인데 **평생 예산이 1,000이었다.**

### 왜 5일 동안 아무도 몰랐나 — 내 책임이다

지급 기능은 `clov-api#92`(chacha1650a)로 **07-30에 이미 올라와 있었다.** 5일 동안 열려 있었다.

어제 핸드오프에 *"`#92`가 07-30부터 정체. 크리티컬 패스"* 라고 적어놓고도 **막힌 이유를 안 봤다.** `BLOCKED`을 머지 충돌로 읽고 "작성자가 rebase할 차례"라고 미뤘다.

### ★ 그날 저녁에 그 진단도 틀렸다는 게 드러났다

리뷰에 *"승인이 없어서였다"* 라고 써서 chacha1650a에게 사과까지 했는데, 나중에 룰셋을 직접 열어보니 그것도 아니었다.

```json
// clov-api main 룰셋
"required_approving_review_count": 0,          ← 승인은 애초에 필요 없다
"required_review_thread_resolution": true,
"required_status_checks": [{ "context": "build" }]
```

`#92`는 **07-30에 이미 `build` pass**였고 미해결 리뷰 스레드도 **0개**였다.

> **GitHub은 이걸 한 번도 막은 적이 없다. 5일 내내 머지 가능했다.**

**결론은 더 나빠졌다.** 승인 게이트가 있었으면 "승인을 안 눌렀다"는 절차 누락인데, 게이트가 없었으니 **그냥 아무도 안 본 것**이다. 상태 문자열을 두 번 연속 추측으로 읽었다 — 처음엔 충돌로, 다음엔 승인 대기로. **둘 다 룰셋을 안 열어보고 낸 답이다.**

이 레포에서 PR이 실제로 막히는 조건은 둘뿐이다.

| | |
|---|---|
| `build` 체크 | 실패하거나 **아직 도는 중**이면 `BLOCKED` |
| 미해결 리뷰 스레드 | `required_review_thread_resolution: true`라 **스레드를 남겨두면 막힌다** |

두 번째가 실질적이다 — **인라인 코멘트로 스레드를 열면 고친 뒤 resolve까지 해야 머지된다.**

`#92`에 정정 코멘트를 달았다. **틀린 진단을 근거로 사과한 것을 그대로 두면, 다음 사람이 "승인 게이트가 있는 레포"로 읽는다.**

### 리뷰하면서 금액이 뒤집혀 있는 걸 봤다

구현을 맞추려고 계약 §15-4를 다시 읽다가 나왔다.

| 행동 | 필요한 것 | 07-31 값 |
|---|---|---:|
| 마스코트 교감 | **클릭 한 번** | **100** |
| 약속 연결 추억 | 약속 만들기 → 완료 → 글 작성 | **50** |

**가장 성의 없는 행동이 두 배를 벌고 있었다.** `CLAUDE.md`가 핵심 흐름을 *"약속 완료 후 추억 작성"* 이라고 못박아 두었는데, **골드가 그 루프를 밀지 않고 반대쪽을 밀고 있었다.**

07-31 값은 **획득 경로가 구현되기 전에 정한 탁상 수치**였다. 붙일 코드가 없으니 순서가 뒤집힌 걸 아무도 밟지 않았다.

### 계약 §15-4를 개정했다 (`web-design-repository#53`)

| | 개정 전(07-31) | 개정 후(08-04) |
|---|---:|---:|
| 마스코트 교감 | 100 × 3회 = 300 | **200 × 10회 = 2,000** |
| 약속 연결 추억 | 50 | **300** |
| 자유 추억 | **없음** | **200 × 3회 = 600** |
| 하루 총 상한 | 500 | **3,000** |
| 카탈로그 완주 | **143일** | **24일** |

금액을 **노력 순서**로 다시 세웠다 — 클릭 200 < 자유 글 200 < 약속 완주 300. 그리고 **표를 계약에 박아넣었다.** 새 사유를 넣을 때 자기 자리를 먼저 정하게 하려는 것이다. 금액이 노력 순서를 어기면 사용자가 가장 싼 경로만 반복한다.

### ★ 자유 추억은 사유를 따로 만들어야 했다

원래 `EARN_MEMORY` 하나로 합치려 했는데 못 합친다.

- 자유 추억은 **약속·완료·다른 참여자가 필요 없다** — 혼자 무한히 쓸 수 있다
- `MEMORY_ALREADY_WRITTEN`의 `UNIQUE(plan_id, writer_id)`도 **NULL은 중복을 허용**해서 안 막힌다
- 그래서 **하루 3회 캡**이 필요한데, **횟수는 사유별로 센다**(`countReasonToday`)

**사유를 합치면 그 3회 캡이 약속 추억까지 묶어버린다.** 하루에 약속을 다섯 개 완주해도 셋까지만 받는다. 그래서 `EARN_MEMORY_FREE`를 따로 냈다.

### 카탈로그 총액이 낡아 있었다

계약 §15-1의 `42,260 G`는 **스킨이 하나도 없던 07-30 값**이었다. 8월 초에 스킨 8종(29,400)이 들어와 실제로는 **71,660**이다 — 70% 커졌다.

이 값을 안 다시 세고 "며칠이면 다 산다"를 계산하면 그대로 틀린다. **세는 쿼리를 계약에 같이 넣었다.**

```sql
SELECT SUM(ROUND(price * (100 - discount_rate) / 100)) FROM shop_items;
```

### `#92` 리뷰 — 계약이 지목해서 금지한 실수가 그대로 있었다

```xml
<select id="sumEarnedToday" ...>
    ... WHERE reason IN ('EARN_MASCOT', 'EARN_MEMORY') ...
```

계약 §15-4에 **이 실수를 이름으로 지목한 경고**가 이미 있다 — *"합산 대상을 사유 이름으로 나열하면 새 사유가 조용히 캡 밖으로 빠진다. `EARN_` 접두사 전체를 대상으로 삼는다."*

**그리고 이 PR에서 바로 터진다.** `EARN_MEMORY_FREE`를 얹는 순간 자유 추억 골드가 **통째로 총 상한 밖에서 무한히 쌓인다.** 자바 쪽 Javadoc은 *"`EARN_*` 사유 합계만 센다"* 로 맞게 적혀 있는데 SQL이 그걸 안 지켰다.

> **주석이 맞고 쿼리가 틀리면 리뷰가 주석만 보고 통과시킨다.** 계약이 경고까지 써둬도 코드가 어기는 건 막지 못한다 — **경고를 검증 가능한 형태로 적어야 한다.** 그래서 계약의 경고 문구에 `reason LIKE 'EARN\_%'`를 같이 박았다.

캡 판정이 **행 잠금 밖**에 있는 것도 짚었다(`sumEarnedToday` → `if` → `findWalletForUpdate` 순서라 동시 요청 둘이 같은 누적치를 읽고 둘 다 통과한다). 요청 5건 중 ①②③은 필수, ④는 권장, ⑤는 테스트다.

**설계 자체는 좋았다.** `earnGold`가 실지급액을 반환해서 캡에 걸려도 화면이 거짓말을 안 하고, revive 분기가 `earnGold` 앞에서 early return해서 **삭제 후 재작성으로 다시 받는 경로가 없고**, `ClovTime.startOfTodayUtc()`가 KST 자정을 UTC로 환산한다(UTC 자정으로 잘랐으면 한국 사용자는 **오전 9시에 하루가 바뀐다**).

### 미결 — 약속(D-day) 생성에는 여전히 골드가 없다

질문에 *"새 D-day 만들기"* 가 있었는데 **계약에 `EARN_PLAN`이 없고 `#92`도 `PlanService`를 안 건드린다.**

지금 설계는 **약속 만들기 자체가 아니라 완주(약속 → 완료 → 추억 300)에 값을 매긴다.** 약속 생성에 돈을 붙이면 만들고 지우기를 반복해 벌 수 있어서 캡이 하나 더 필요해진다. **다만 이건 확정한 적이 없다 — 정하고 넘어가야 한다.**

---

## 9. ★★ 카탈로그 — 숫자를 세 번 틀리고, 상품 하나를 덮어쓰고, 열한 종을 내렸다

저녁에 스킨 4종을 등록하면서 시작됐는데, **계약에 적혀 있던 숫자를 확인하려다 세 겹이 연달아 드러났다.**

### ① 총액을 세 번 틀렸다 — 전부 "계산해서"

```
71,660   계산   시드 12종 42,260 + 스킨 8종 29,400
77,060   계산   위에 새 스킨 4종을 더함
71,860   실측   ← 앞의 둘이 얹혀 있던 시드 값이 틀렸다
34,800   실측   ← 셋째는 판매 종료분까지 세고 있었다
```

§15-1에 **"세는 쿼리는 아래와 같다"를 내가 직접 써놓고 한 번도 돌리지 않았다.** 두 번 다 앞 값에 더하기만 했다.

> **문서에 적힌 숫자는 계산으로 갱신하면 안 된다.** 앞 값이 틀렸으면 그 위에 얹은 값도 전부 틀린다. **재는 쿼리가 문서에 있으면 그건 "돌리라"는 뜻이다.**

### ② `code` 충돌로 기존 상품을 덮어썼다

실측이 예상과 어긋나서 **시드/스킨으로 쪼개 재봤다.** 그러자 원인이 바로 나왔다.

```
스킨 34,800   예상과 정확히 일치
시드 37,060   계약의 42,260보다 5,200 모자람
```

어제 가격표에 `EPIC 5,200 롭탐험가`가 있었다. 내가 새로 넣은 스킨의 `code`가 **그 시드 상품과 같았다.**

```sql
('COSTUME_ROB_EXPLORER', '롭 탐험가 코스튬', 'EPIC', 5200, ..., 100)   -- 07-29 시드
('COSTUME_ROB_EXPLORER', '탐험가 롭',        'UNCOMMON', 1500, ..., 230) -- 내 SQL
```

등록 SQL은 재실행 안전을 위해 `ON DUPLICATE KEY UPDATE`로 쓴다. **코드가 겹치면 새 행이 생기는 대신 기존 상품이 통째로 덮이고 에러는 안 난다.**

> **★ 내가 쓴 검증 쿼리로는 이걸 못 잡는다.** "4행 반환"은 **4개를 넣었을 때와 1개를 덮고 3개를 넣었을 때가 똑같다.** 전수 검사(스킨 12종)도 통과한다 — 덮인 쪽이 시드라 스킨 수는 맞기 때문이다.
>
> **넣기 전에 `code`가 이미 있는지 보는 것 말고는 막을 방법이 없다.** 계약에 그 쿼리를 박아뒀다.

**총액 하나만 쟀으면 원인을 못 짚었다.** 쪼개서 잰 덕에 "스킨은 맞는데 시드가 모자라다"가 나왔고 거기서 5,200이라는 숫자가 바로 걸렸다.

**리더 판단으로 덮어쓴 상태를 그대로 뒀다** — 덮인 시드가 SVG 한 장짜리 자리표시였고 그 자리를 상태 9종 스킨이 대신했으니 내용상 업그레이드다. 다만 **의도한 교체가 아니었다.**

### ③ 자리표시 11종을 내렸다 — `DELETE`가 아니라 `RETIRED`

시드 12종은 에셋 없이 이름과 가격만 있던 자리표시였다. 진짜 스킨이 12종 들어왔으니 비켜줄 때가 됐다.

**`DELETE`는 애초에 불가능했다.**

```
user_inventory_items.item_id        FK RESTRICT
user_preferences.equipped_item_id   FK RESTRICT
```

그리고 실제로 **6명이 갖고 있었다.** 뚫고 지웠으면 user 19가 그날 02:37에 12,000골드로 산 여름밤 스킨까지 전부 날아간다. 환불 로직도 없고 `wallet_transactions`의 구매 이력은 없는 상품을 가리킨 채 남는다.

`RETIRED`는 계약이 정의해둔 정식 수단이었다 — **목록에서 빠지고, 구매가 막히고, 산 사람은 그대로 갖고, 되돌리기가 한 줄이다.**

### ④ 그런데 `RETIRED`가 안 먹고 있었다

```xml
<!-- ShopMapper.findCatalog — status 조건이 없다 -->
WHERE (#{category} IS NULL OR i.category = #{category})
```

`ShopItem.isPurchasable()`은 `ACTIVE`만 허용하는데 **목록 쿼리만 그 기준을 안 봤다.** 내려도 화면에 그대로 뜨고 누르면 `ITEM_NOT_PURCHASABLE`로 떨어지는 **"보이는데 못 사는"** 상태다. 어제 핸드오프에 *"이슈로 안 만들었다"* 로 적혀 있던 항목이다.

**테스트가 이 버그를 통과시켜 온 이유가 있었다.** 셋업에 `TEST_RETIRED_SKIN`이 **이미 있었는데 구매 차단만 검증**하고 목록 쪽은 비어 있었다. 그 단언을 채웠다.

한 줄 고치고 배포한 뒤에야 SQL을 돌렸다. **순서가 반대였으면 고치려던 증상이 그대로 재현된다.**

### 결과

```
카탈로그   71,860  →  34,800   (판매 중인 것만)
완주       24일    →  약 12일
상품       23종    →  ACTIVE 12종 (전부 마스코트 스킨) + RETIRED 11종
```

**하루 상한 3,000은 안 건드렸다.** 오늘만 스킨이 8종 → 12종으로 늘었고, 반나절 만에 상한을 두 번 바꾸면 계약 이력이 흔들린다. **살 것이 늘어나는 속도가 상한을 따라잡는 쪽에 걸었다.**

---

## 10. ★★ 오늘 잡은 것 중 넷은 "에러가 안 나는" 결함이었다

이게 오늘의 관통 주제다.

| | 증상 | 왜 안 걸렸나 |
|---|---|---|
| `ti-bubble` | 아이콘 자리에 **빈 네모** | 없는 클래스명이라 CSS가 그냥 아무것도 안 한다 |
| `keyframes` 문자열 보간 | shake가 **아예 안 움직임** | `lint`·`build` 통과. 콘솔 경고만 |
| `findCatalog`에 `status` 없음 | 내린 상품이 **목록에 뜸** | 구매 차단은 되니 "막혀 있다"고 착각 |
| UPSERT `code` 충돌 | 기존 상품이 **조용히 사라짐** | 행 수가 맞아서 검증 쿼리가 통과 |

**네 개 다 빌드가 통과하고 예외도 안 난다.** 화면을 열거나 값을 직접 재야만 보인다.

### `ti-bubble` — 42종을 CDN 4,962종과 대조했다

`#250`이 쓴 `ti-user-minus`가 진짜 있는지 확인하려고 CDN CSS를 받아 대조했는데, **정작 걸린 건 다른 것**이었다.

```
ti-user-minus   content:"\eb4a"   있다
ti-bubble       CSS 전체 0건       ← bubble 계열 자체가 없다
```

`RoomList.jsx:269`의 **"사라진 방"** 상태에서만 쓰인다. 모든 멤버가 나간 드문 경로라 아무도 못 봤다. **형제 아이콘(`ti-x`·`ti-clock`)이 중립 계열이라 같은 톤의 `ti-circle-off`로 맞췄다.**

> **확인하러 갔다가 다른 걸 찾았다.** 하나를 검증할 때 **같은 종류 전체를 한 번에 대조하는 게 싸다** — 42종을 세는 것과 1종을 세는 것의 비용이 같았다.

### `#259` shake — 로그인 게이트 밖이라 재현할 수 있었다

lami가 `#252` 작업 중 콘솔 경고로 발견해 별도 이슈로 분리했다. **`/signup`은 게이트 밖이라 직접 띄워서 확인했고, 이슈에 적힌 것보다 나빴다.**

```css
/* $shake={true}일 때 emotion이 생성한 규칙 */
.css-1rzw0zn { width:100%; height:54px; ... cursor:text; }
                                        ↑ animation 선언이 통째로 없다
```

- `animation-name`이 빈 게 아니라 **선언 자체가 사라진다**
- `shakeInput` keyframes는 **문서에 등록조차 안 된다**
- **빨간 테두리가 keyframes 안에 들어 있어서** 흔들림뿐 아니라 색 피드백까지 없다

**같은 파일 안에 대조군이 있었다.** `rise`·`popIn`·`fadeIn`·`modalUp`은 직접 보간이라 멀쩡하고, **화살표 함수가 문자열을 반환하는 두 줄만** 터졌다.

```js
animation: ${rise} 0.6s ...                                    // 정상
animation: ${(p) => (p.$shake ? `${shakeInput} 0.4s ease` : 'none')};  // 터진다
```

### 검증 방법에도 사각지대가 있다

`#254`(약관 체크박스 키보드) 리뷰에서 짚은 것이다. lami가 **합성 `KeyboardEvent`를 dispatch해서** 검증했는데, 그 이벤트는 `isTrusted: false`라 **브라우저 기본 동작을 아예 발생시키지 않는다.**

- "Space가 스크롤을 안 했다"가 `preventDefault` 덕인지 합성 이벤트라 원래 안 한 건지 **구분되지 않는다**
- 내가 우려한 회귀(`TermsToggle` 버튼의 클릭 생성이 막히는 것)도 **있어도 통과한다**

> **검증 도구가 못 보는 자리에 결함이 있으면 통과가 무증거가 된다.** 나도 실제 키 입력으로 재현하려다 환경 문제로 못 했고, **"확인된 결함"이 아니라 "명세상 그렇게 되는 자리"로 적어 보냈다.** 리더 판단으로 그대로 머지됐다.

---

## 11. 사고 하나 — 중단된 명령 뒤에 실행된 명령

```
git checkout main       → "Please commit your changes" 로 중단
git pull origin main    → 그대로 실행됨 (옛 브랜치에서!)
git checkout -b 새브랜치 → 그 위에서 생성
```

`&&`로 묶었는데 `git checkout`이 **실패 종료코드를 안 냈다.** 그래서 새 브랜치에 병합 커밋 3개가 딸려 붙었다.

`git diff origin/main HEAD`가 **비어 있어서** 내용은 같다는 걸 확인하고 `git reset --mixed origin/main`으로 포인터만 되돌렸다. 작업 트리는 안 건드렸다.

> **`git status`가 깨끗하지 않으면 `checkout`이 중단될 수 있다.** 체인 앞에 `git status --short`를 확인하거나, 브랜치를 만든 뒤 `git log origin/main..HEAD`가 비었는지 본다.

같은 계열로 `git add -A`가 **작업 중이던 새 스킨 폴더를 커밋에 끌어들인** 일도 있었다. `git reset --soft HEAD~1` + `restore --staged`로 뺐다. **AGENTS.md의 "Keep unrelated local changes out of the PR"이 이 자리다.**

---

## 12. 오늘 끝난 것 · 남은 것

**닫힌 이슈 14건** — clov-web 10(#237·#238·#239·#240·#247·#248·#251·#252·#253·#259) · clov-api 2(#122·#126) · web-design 2(#51·#16)

**알림 도메인이 세 겹 다 맞았다.** 하루 안에 계약·백엔드·프론트가 같은 값으로 정렬된 건 오늘이 처음이다.

```
계약     §13에 MEMBER_LEFT 신설 + 합류·수락을 FRIEND로 이동   #50 · #52
백엔드   MEMBER_LEFT 발송(#124) + type을 FRIEND로(#128)
프론트   FRIEND_SUBTYPE_META에 3종 문구·아이콘(#250)
배포     web 3회차 · api 1회차 — 셋이 같은 시각에 살아 있다
```

**인증 화면도 하루 만에 정리됐다.** lami2342가 이슈 4건을 직접 발굴해 전부 PR까지 냈다.

```
#238 JoinRoom 폰트 → #251 OAuthRedirect 이식 → #252 Enter 네비 → #253 체크박스 키보드 → #259 shake
```

**계약은 7건이 들어갔다** — `MEMBER_LEFT` 신설 · 합류/수락 이동 · §15-4 골드 개정 · §12 교감 캡 정합 + `earnedGold` · §15-1 총액 실측 교체 + `code` 충돌 경고 · 진단 정정.

### 남은 PR — 전부 chacha1650a 것이다

| PR | 상태 |
|---|---|
| **clov-api #92** 골드 지급 | 요청 6건. **오늘 유일한 실질 블로커** — 이게 풀려야 골드가 처음 돈다 |
| clov-web #264 View Transitions | 저녁에 새로 올라왔다(#263) |
| clov-web #178 골드 배지 즉시 갱신 | 머지 충돌. `#92` 뒤에 온다 |
| clov-web #165 사용자설정 카드형 | 머지 충돌 (07-30부터) |

> ⚠️ **chacha1650a가 논 게 아니다.** `#246`·`#127`을 오늘 머지했고 저녁엔 `#264`를 새로 올렸다. **멈춰 있는 건 사람이 아니라 오래된 PR이다.**

### 새로 연 이슈 11건 — 배정까지 끝냈다

| | |
|---|---|
| **lami2342** | `#251`·`#252`·`#253`·`#259` — **넷 다 같은 날 PR까지 갔다** |
| **kimgyubi1234** | `#258` 폰트 — 착수 전에 `unicode-range` 분할·`--heading` 결정·굵기 확인 셋을 코멘트로 붙였다 |
| 미배정 | `#262`(shake 0.4s 연속 실패) · `#263`(전체 애니메이션 — `#264`로 진행 중) |
| 닫힘 | `#247`(`clov-api#127`로 해결, 레포가 달라 자동 종료 안 됨) · `web-design#16`(§15-4로 해결) |

### 상태

```
clov-web    main 8bcbd4e   미배포 0
clov-api    main d24f6f2   미배포 0
shop_items  ACTIVE 17종(52,000) + RETIRED 11종
            └ COSTUME 13종 + BACKGROUND 4종 · 완주 17일
```

### 미결

- **약속(D-day) 생성 골드** — 계약에 사유가 없다. 지금 설계는 생성이 아니라 완주에 값을 매기는데 **확정한 적이 없다**(§8)
- ~~**하루 상한 3,000 대 완주 12일**~~ — **저녁에 풀렸다.** 상품이 5종 늘어 52,000/17일이 됐다(§13-3). 낮에 "줄어든 쪽이 일시적"이라고 보고 상한을 안 건드린 판단이 같은 날 맞았다. **상한은 계속 3,000이다**
- **빈 카테고리 두 개** — `SKIN`·`EVENT`는 여전히 0종이다. 배경은 재활용이 아니라 **`BACKGROUND`를 새로 만들어** 해결했다(§13-1). **이 둘을 탭에서 뺄지는 아직 안 정했다** — 지금 상점에 빈 탭이 둘 보인다
- **`#254` 가드** — 리더 판단으로 그대로 머지됐다. `TermsToggle`에 포커스를 두고 Enter를 누르면 패널이 안 열리고 동의가 토글될 수 있다(§10)

---

## 13. ★★ 저녁 — 배경을 상점에 올렸고, 한 시간 전 판단이 그날 안에 시험받았다

사계절 배경 4종이 다 만들어져서 상점 상품으로 올렸다. 값을 정하는 일인 줄 알았는데, **실제로 갈린 건 "무엇으로 아이템을 지목하느냐"였다.**

### 13-1. 값보다 먼저 정할 게 셋 있었다

| | 정한 것 | 왜 |
|---|---|---|
| 값 | RARE **2,800** × 4 | 기존 사다리 칸을 그대로 쓴다. 새 가격대를 안 만든다 |
| 카테고리 | **`BACKGROUND` 신설** | 비어 있던 `SKIN` 탭을 안 썼다 |
| 기존 5종 | **무료로 남긴다** | `shop_items`에 행 자체가 없다 |

**넷을 차등하지 않았다.** 같은 날 같은 규격으로 만든 한 세트고 봄이 여름보다 비쌀 근거가 없다. **없는 등급 차이를 지어내면 그게 다음 사람의 판단을 흐린다.**

배경 하나가 스프라이트 9장짜리 마스코트 스킨보다 비싼 건 알고 넣었다 — 배경은 모든 화면을 항상 덮어 화면 점유가 제일 크다. **값을 노력이 아니라 체감으로 매긴 자리라, 그 근거가 안 통하면 내린다고 SQL에 적어뒀다.**

> **★ 무료였던 걸 유료로 바꾸는 건 뺏는 것이고, 되돌릴 수 없다.** 반대 방향(유료 → 무료)은 언제든 선물이지만 이 방향은 아니다. 그래서 기존 5종은 `RETIRED`가 아니라 **애초에 `shop_items`에 넣지 않았다** — "판매 종료된 유료 상품"과 "상점 밖 기본 제공"은 다른 상태다.

**`SKIN` 탭을 재활용하는 게 당장은 공짜로 보였다.** 오전에 자리표시 상품을 내려서 `SKIN`·`EVENT`가 둘 다 비어 있었으니까. 그런데 그러면 **코드는 '스킨'이라 부르고 실물은 배경**인 상태가 되고, 나중에 진짜 마스코트 스킨을 `SKIN`으로 넣는 순간 한 탭에서 섞인다. **옮길 기존 행이 하나도 없는 그때가 이름을 바로잡는 가장 싼 시점이었다.**

`category`가 `ENUM`이 아니라 `VARCHAR(20)`이고 `normalize()`가 값을 그대로 통과시켜서 **DDL도 서비스 코드도 0줄 바뀌었다.**

### 13-2. ★★★ `imageUrl`로 대조하려다 접었고, 한 시간 뒤 그게 갈랐다

사용자설정이 "이 배경을 보유했나"를 물어야 하는데, `ShopItemResponse`에 **`code`가 없었다.** 계약서가 일부러 비노출로 선언해둔 필드였다 — *"클라이언트가 판단에 쓸 일이 없어 노출하지 않는다."*

우회가 있었다. 배경 상품의 `image_url`은 프론트가 쓰는 `bg.thumb`과 **같은 문자열**이라, 그걸로 대조하면 백엔드를 안 건드려도 됐다. `Mascot.jsx`가 이미 경로 매칭을 쓰고 있어서 선례도 있었다.

**접었다.** 이유는 하나였다 — 그때 **겨울 배경 id 변경이 아직 열려 있었다.** 썸네일 경로가 바뀔 수 있는 필드였다.

```
id 는 환경마다 다른 auto-increment PK 라 프론트에 상수로 못 박는다  → code 가 필요하다
image_url 은 표현용이라 언제든 바뀐다                              → 신원 확인에 쓰면 안 된다
```

`ShopItemResponse`에 `code`를 넣고 계약의 비노출 선언을 뒤집었다.

**한 시간 뒤에 시험받았다.** 상점 카드가 클로버 아이콘 썸네일을 보여주고 있었고 — 배경을 사는 사람이 보고 싶은 건 클로버가 아니라 그 배경이다 — `image_url`을 실제 그림으로 바꾸는 `UPDATE`를 돌렸다.

```sql
UPDATE shop_items SET image_url = CONCAT('/shop/backgrounds/', ...) WHERE category = 'BACKGROUND';
-- 4 row(s) affected  Rows matched: 4  Changed: 4
```

**`imageUrl` 우회를 썼으면 이 한 줄로 배경 4종이 전부 잠겼다.** 17종을 다 보유한 계정에서 바로 드러났을 것이다.

> ### ★★★ 표현용 필드를 신원 확인에 쓰지 않는다
>
> 백엔드를 안 건드리는 우회가 있고, 선례도 있고, 지금은 두 값이 실제로 같았다. **그래도 접은 근거는 "지금 같다"가 아니라 "저건 바뀌는 값이다"였다.**
>
> 하루도 안 지나서 실제로 바뀌었다. **바뀔 이유가 있는 필드는 언젠가가 아니라 곧 바뀐다.**

### 13-3. 상한을 또 안 건드렸다 — 낮의 예측이 저녁에 맞았다

```
34,800 (스킨 12종)  →  52,000 (스킨 13종 + 배경 4종)   실측
12일                →  17일
```

낮에 카탈로그가 반이 됐을 때(71,860 → 34,800) 완주가 24일 → 12일이 됐는데 **상한 3,000을 그대로 뒀다.** 근거 중 첫째가 *"스킨이 늘고 있으니 줄어든 쪽이 일시적이다"*였다.

**같은 날 저녁에 상품이 5종 들어와 17일이 됐다.** 그때 12일에 맞춰 내렸으면 지금 되돌려야 했다.

> **★ 움직이는 값에 맞춰 상수를 조이지 않는다.** 계약서가 스스로 걸어둔 *"총액이 늘고 있는데 상한을 내리면 두 번 잘못 조인다"*가 반나절 만에 값을 지켰다. 그 절 제목도 바꿨다 — **숫자 하나를 설명하는 절이 아니라, 값이 움직이는 동안 상한을 안 건드린 기록이다.**

등급 분포로 교차 검증됐다. 판매 중 17종이 `3·5·5·3·1`이고 단가를 곱하면 정확히 52,000이다. **총액과 분포가 서로를 확인해준다** — §9에서 "쪼개서 재라"고 배운 것의 다음 판이다.

### 13-4. 갈 곳을 알려주고 데려가지 않는 버튼

배경은 **사는 곳과 고르는 곳이 다른 유일한 상품**이다. 상점에서 사고 사용자설정에서 고른다. 그래서 두 화면이 서로를 가리켜야 하는데, 한쪽만 이어져 있었다.

| 방향 | 상태 |
|---|---|
| 사용자설정 → 상점 (미보유 배경 클릭) | 됐다 — `navigate` |
| 상점 → 사용자설정 (보유 배경 클릭) | **`disabled` 버튼이었다** |

`설정에서 적용`이라고 써놓고 눌리지 않았다. 사용자가 헤더 메뉴를 다시 뒤져야 했다.

설정 모달 상태가 `Header`의 `useState`였고 **`Shop`은 `Header`의 형제라 닿을 수 없었다.** `settingsStore`(zustand)로 올렸다. `persist`는 안 붙였다 — **새로고침했더니 설정 모달이 떠 있으면 그건 버그로 보인다.**

> ⚠️ 상세 모달 쪽은 `onClose()`를 먼저 부른다. 안 그러면 **설정 모달 뒤에 상세 모달이 겹쳐 남는다.**

### 13-5. 사고 — 배포 전 파일을 보고 "없다"고 두 번 말했다

배포 검증에서 번들과 CSS에 새 문자열이 **전부 MISSING**으로 나왔다. 배포가 안 됐다고 볼 뻔했다.

원인은 파일명이 **콘텐츠 해시**라는 것이었다. 배포되면 이전 이름이 404가 되는데, `curl`이 받아온 **151바이트짜리 404 HTML**을 grep해서 "문자열 없음"으로 읽었다.

```
curl -s .../index-HxADl3fj.js | grep ...     → MISSING (실은 404 본문 151바이트)
curl -s -o f -w "%{http_code} %{size_download}"  → HTTP 404  151B
```

**잡은 방법은 반대 대조였다.** 원래 있던 문자열(`네온 클로버 시티`·`ps-bg-swatch`)까지 MISSING으로 나오는 걸 보고 **"배포가 아니라 내 요청이 틀렸다"**를 알았다.

> ### ★★ 없다는 결과는 "찾았는데 없음"과 "안 찾아짐"을 구분하지 못한다
>
> §10의 "도구가 못 보는 곳"과 같은 계열인데 한 단계 더 나쁘다. **거기선 도구가 통과를 줬고, 여기선 도구가 실패를 줬는데 그 실패가 가짜였다.**
>
> **앞으로 `curl` 검증에는 `-w "%{http_code} %{size_download}"`를 붙인다.** 그리고 **있어야 할 것 하나를 같이 찾는다** — 그게 안 나오면 검증 자체가 틀린 것이다.

### 13-6. 디자인 산출물 — 오닉스 9동작과 사계절 배경 최종안

상점·계약 연동과 별개로, 오늘 확정한 실제 아트 산출물도 기록한다.

#### 개발 팀장 오닉스 (EPIC)

- 경로: `../clov-web/public/shop/skins/onyx/development-team-lead-epic/`
- 상태: `default`·`smile`·`angry`·`dizzy`·`find`·`pencil`·`pulled`·`scared`·`sleepy` **9장**
- 방향: 보석세공사 원형은 유지하되 **IT 개발 팀장**으로 읽히도록 헤드셋·노트북·사원증 계열 소품을 사용했다
- 최종 수정: 신발을 없애고 **새 발을 그대로 노출**했다. 오닉스가 사람형 개발자로 보이지 않게 종족 실루엣을 되돌린 결정이다

#### 사계절 배경 4종

| 계절 | 표시 이름 | 최종 아트 방향 | 파일 |
|---|---|---|---|
| 봄 | 봄비 뒤 벚꽃 운하 | 비가 갠 도심 운하·벚꽃·맑은 반사광 | `spring-rain-city.webp` |
| 여름 | 한여름 비밀 만 | 절벽이 감싼 비밀 해변·청록빛 바다 | `midsummer-cove.webp` |
| 가을 | 단풍빛 돌길 | 붉은 단풍과 노란 은행나무 사이의 젖은 돌길, 수채화 | `autumn-watercolor-path.webp` |
| 겨울 | 토렐로의 겨울 골목 | 크뢰위에르의 1890년작 골목에서 받은 원근·빛의 인상을 겨울 유화 골목으로 재해석 | `winter-moonlit-forest.webp` |

배경 원본은 `../clov-web/public/backgrounds/`에 **1920×1080 WebP**, 썸네일은 `../clov-web/public/bg-thumbs/`에 **512×512 PNG**로 통일했다. 생성은 내장 ImageGen을 사용했고, 최종 산출물은 전부 프로젝트 안으로 옮겼다.

겨울은 처음 만든 **푸른 달빛 침엽수 숲 수채화**를 최종 채택하지 않았다. 좁은 골목의 강한 원근, 햇빛 받은 아이보리 벽, 푸른 눈 그림자가 더 분명한 두 번째 유화 시안으로 교체했다. 다만 이미 상점 상품과 소유 판정에 연결된 값이어서 다음은 유지했다.

```text
id       winter-moonlit-forest
itemCode BACKGROUND_WINTER_MOONLIT_FOREST
path     /backgrounds/winter-moonlit-forest.webp
name     토렐로의 겨울 골목  ← 표시 이름만 변경
```

**표시와 신원을 분리한 덕분에 그림과 이름을 바꿔도 기존 구매·선택값은 깨지지 않는다.** §13-2에서 `imageUrl`을 상품 신원으로 쓰지 않기로 한 판단과 같은 결론이다.

최종 교체 뒤 `clov-web`에서 이미지 규격을 직접 확인했고, `npm run lint`와 `npm run build`가 모두 통과했다. Vite의 500kB 초과 청크 경고는 기존 경고이며 이번 이미지 교체의 블로커는 아니다.
### 13-7. ★ 2주간 아무도 안 쓴 필드를 찾았다 — 그런데 화면에는 이름이 붙어 있었다

사용자설정에 `내 초대코드 CLV-V9E38B`가 보인다는 질문에서 시작했다. **쓰는 방법이 없었다.**

```
생성·중복확인   AuthService — 가입할 때 'CLV-' + 6자리       ✅ 완성
API 노출        UserResponse · UserProfileResponse            ✅ 완성
화면            사용자설정 readOnly                            ✅ 완성
──────────────────────────────────────────────────────────
받는 API        없음
받는 화면       없음
계약 규정       없음 — 응답 예시 두 줄에만 있었다
```

**`2026-07-20` 인증 API 첫 구현부터 있었다.** 유일한 쿼리 `existsByPersonalInviteCode`는 **코드를 만들 때 중복을 확인하는 용도**였다 — 자기가 만든 값을 자기가 확인하는 닫힌 고리였다.

> **★ "구현이 덜 됐다"가 아니라 "쓰는 쪽이 설계된 적이 없다"였다.** 이 구분이 중요했다 — 전자면 이어서 만들면 되지만, 후자면 **무엇에 쓸 물건인지 먼저 정해야** 만들 게 생긴다.

### 13-8. 지운 근거는 "안 쓴다"가 아니었다

안 쓰는 필드는 그냥 두면 조용하다. **이건 조용하지 않았다.**

| | 개인 | 방 |
|---|---|---|
| 형식 | `CLV-XXXXXX` | `CLV-JOIN-XXXXXX` |
| 위치 | `users.personal_invite_code` | `invites.invite_code` |
| 쓰임 | **없었다** | `requestJoin` |

**화면에 `내 초대코드`라고 이름이 붙어 있으면 사용자는 그걸 방 입장 칸에 넣어본다.** 테이블도 접두사도 달라서 실패한다. **죽은 필드가 아니라 오답을 유도하는 필드였다.**

'만들어서 쓸까'도 검토했는데 접었다. 근거 셋이었다.

- **개인 코드가 푸는 문제가 실은 안 풀린다** — 방 코드든 개인 코드든 **앱 밖에서 주고받아야 하는 건 똑같다.** 누가 타이핑하느냐만 바뀌고 단계는 오히려 하나(수락) 늘어난다
- **Clov엔 방 밖의 '친구' 개념이 없다** — 테이블 24개가 전부 방 단위다(`friendship_rooms`·`room_members`·`room_invites`·`room_join_requests`). **없는 그래프의 씨앗이었다**
- **후회 비용이 한쪽으로 기울어 있다** — 지웠다 다시 필요하면 생성 로직 10줄 + 마이그레이션이고 **아무도 쓴 적 없는 난수라 바뀌는 사람이 없다.** 반대로 두고 안 만들면 "이거 뭐예요?"가 계속 나온다

### 13-9. ★★ 순서를 틀리면 두 방향 다 서비스가 죽는 삭제였다

컬럼이 `NOT NULL UNIQUE`라, 지우는 순서가 하나뿐이었다.

```
생성만 먼저 지우면  →  가입이 깨진다
                       NOT NULL 이고 기본값이 없어서 INSERT 가 통째로 실패한다

컬럼을 먼저 DROP    →  로그인까지 깨진다
                       UserMapper.xml 이 SELECT 절에 컬럼명을 명시한다.
                       사용자 조회가 전부 죽는다 — 로그인, /users/me, 전부

  1) NULL 허용  →  2) 코드 배포  →  3) DROP COLUMN
```

> **★ 3단계를 "머지했으니 됐겠지"로 돌리면 안 된다.** 머지는 배포가 아니다(§7). step2 SQL에 **사전점검을 쿼리로 넣었다** — step1 이후 가입자의 행이 `NULL`이면 배포된 코드가 이미 컬럼을 안 쓴다는 뜻이다. **사람 기억 대신 데이터가 답하게 했다.**

### 13-10. 사고 — 전수 확인을 `head`로 잘라서 4개를 놓쳤다

테스트 참조를 찾을 때 `grep ... | head`를 써서 **3개만 보고 다 봤다고 생각했다.** 실제로는 7개였다.

컴파일은 통과했을 것이다 — 놓친 4개는 전부 `jdbcTemplate.update("INSERT INTO users (... personal_invite_code ...)")`라 **문자열 안에 있어서 자바 컴파일러가 안 본다.** 테스트를 돌렸을 때 런타임에 터졌을 것이다.

**잡은 방법은 마지막에 조건 없는 전수 grep을 한 번 더 돌린 것이다.**

> ### ★★ 제거 작업의 완료 판정은 "고쳤다"가 아니라 "레포에 0건"이다
>
> 고칠 목록을 만들 때는 `head`로 훑어도 되지만, **끝났다고 판정할 때는 자르지 않은 전수 검색을 다시 돌린다.** §10의 "도구가 못 보는 곳"과 같은 계열인데, 여기선 **도구가 아니라 내가 출력을 잘랐다.**


---

## 14. 다음에 또 쓸 것

- **★★★ 제거 작업의 완료 판정은 "고쳤다"가 아니라 "레포에 0건"이다** — 테스트 참조를 `grep | head`로 훑고 3개를 고쳤는데 실제로는 7개였다(§13-10). 놓친 4개는 전부 **SQL 문자열 안**이라 자바 컴파일러가 안 본다. **목록을 만들 때는 잘라도 되지만, 끝났다고 판정할 때는 자르지 않은 전수 검색을 다시 돌린다**
- **★★ 죽은 필드와 오답을 유도하는 필드는 다르다** — 개인 초대코드는 2주간 아무도 안 썼는데, **화면에 `내 초대코드`라고 이름이 붙어 있어서** 사용자가 방 입장 칸에 넣어보게 만들었다(§13-8). 안 쓰는 걸 지우는 근거는 보통 약한데, **이름이 붙어 있으면 강해진다**
- **★★ "구현이 덜 됐다"와 "쓰는 쪽이 설계된 적 없다"를 구분한다** — 전자면 이어서 만들면 되고, 후자면 **무엇에 쓸 물건인지 먼저 정해야 만들 게 생긴다**(§13-7). 개인 초대코드는 생성·저장·노출이 완성돼 있어서 전자로 보였지만, 계약서에도 화면명세서에도 이슈에도 "이걸로 무엇을 한다"가 한 줄도 없었다
- **★★★ 표현용 필드를 신원 확인에 쓰지 않는다** — `image_url`로 소유를 대조하는 우회가 있었고 선례도 있었고 그때 두 값은 실제로 같았다. 접은 근거는 "지금 같다"가 아니라 **"저건 바뀌는 값이다"**였고, **한 시간 뒤에 실제로 바뀌었다**(§13-2). 바뀔 이유가 있는 필드는 언젠가가 아니라 곧 바뀐다
- **★★★ 없다는 결과는 "찾았는데 없음"과 "안 찾아짐"을 구분하지 못한다** — 배포 검증에서 새 문자열이 전부 MISSING으로 나와 배포 실패로 볼 뻔했는데, **151바이트짜리 404 본문을 grep한 것**이었다(§13-5). 잡은 건 **원래 있던 문자열까지 같이 찾아본 것**이다. `curl` 검증에는 `-w "%{http_code} %{size_download}"`를 붙이고, **있어야 할 것 하나를 대조군으로 같이 찾는다**
- **★★ 움직이는 값에 맞춰 상수를 조이지 않는다** — 카탈로그가 반이 돼 완주가 12일이 됐을 때 상한을 그대로 뒀고, **같은 날 저녁에 17일이 됐다**(§13-3). 그때 조였으면 되돌려야 했다. 값이 움직이는 중이면 **몇 번 재는지가 아니라 언제 멈추는지를 본다**
- **★★ 이름을 바로잡는 가장 싼 시점은 옮길 데이터가 없을 때다** — 비어 있던 `SKIN` 탭을 배경으로 재활용하면 공짜였지만, **코드는 '스킨'이라 부르고 실물은 배경**이 된다(§13-1). 마이그레이션할 행이 0인 그 순간이 지나면 값이 오른다
- **★ 무료였던 걸 유료로 바꾸는 건 되돌릴 수 없다** — 반대 방향은 언제든 선물이다. 그래서 기존 배경 5종은 `RETIRED`가 아니라 **애초에 `shop_items`에 넣지 않았다**(§13-1). "판매 종료된 유료 상품"과 "상점 밖 기본 제공"은 다른 상태다
- **★★★ 에러가 안 나는 결함은 빌드로 못 잡는다 — 화면을 열거나 값을 직접 재야 한다** — 오늘 넷을 잡았고 넷 다 `lint`·`build`가 통과했다(§10). 아이콘 이름, emotion keyframes, 목록 쿼리 조건, UPSERT 코드 충돌. **"통과했으니 됐다"가 성립하지 않는 종류가 있다**
- **★★ 문서에 적힌 숫자는 계산으로 갱신하지 않는다** — 카탈로그 총액을 하루에 세 번 틀렸는데 전부 "앞 값에 더해서"였다(§9). **재는 쿼리가 문서에 적혀 있으면 그건 돌리라는 뜻이다.** 그리고 잰 값에는 "쟀다"고 적어둔다
- **★★ 합계가 어긋나면 쪼개서 잰다** — 총액 하나만 봤으면 "5,200이 어디로 갔나"를 못 짚었다. **시드/스킨으로 나눠 재니 "스킨은 맞고 시드가 모자라다"가 나왔고 거기서 원인이 바로 걸렸다**
- **★★ 검증 쿼리가 못 보는 자리가 있다** — UPSERT에서 "4행 반환"은 **4개를 넣었을 때와 1개를 덮고 3개를 넣었을 때가 똑같다**(§9). 합성 `KeyboardEvent`는 **브라우저 기본 동작을 아예 안 만들어서** `preventDefault` 효과를 확인할 수 없다(§10). **도구가 못 보는 곳에 결함이 있으면 통과가 무증거다**
- **★ 하나를 확인하러 갈 때 같은 종류 전체를 대조한다** — `ti-user-minus` 하나를 보러 갔다가 42종을 CDN 4,962종과 맞췄고, **찾던 것은 멀쩡했고 다른 게 걸렸다.** 비용이 같았다
- **★ 지우는 게 아니라 내리는 수단이 있는지 먼저 본다** — `DELETE`는 FK가 막았고 실제로 6명이 갖고 있었다. `RETIRED`는 계약이 이미 정의해둔 정식 수단이었는데 **정작 그게 동작을 안 하고 있었다**(§9). 되돌릴 수 있는 쪽을 고르면 판단이 싸진다
- **★★ "안 되는 기능"과 "만든 적 없는 기능"을 먼저 가른다** — "골드가 지급이 안 된다"를 버그로 읽으면 `MemoryService`를 뒤진다. `git grep EARN_ origin/main`이 **0건**이면 고칠 코드가 없는 것이다. **증상을 재현하기 전에 그 기능이 존재하는지 본다**
- **★★ `BLOCKED`의 뜻은 추측하지 말고 룰셋을 연다** — 같은 문자열을 하루에 두 번 다르게 추측했고 **둘 다 틀렸다**(충돌 → 승인 대기 → 실제로는 아무것도 안 막고 있었다). `gh api repos/OWNER/REPO/rules/branches/main` 한 줄이면 끝난다. 열린 PR은 **건수가 아니라 막힌 이유로** 센다 — 그런데 그 이유를 지어내면 세는 의미가 없다
- **★ 틀린 진단으로 사과하면 그 진단이 사실로 굳는다** — chacha1650a에게 "승인이 없어서 5일 걸렸다"고 사과했는데, 사과가 진단을 더 그럴듯하게 만든다. 정정 코멘트를 안 달았으면 다음 사람이 이 레포를 "승인 게이트가 있는 레포"로 읽는다
- **★ 보상 금액은 노력 순서를 따른다** — 클릭 한 번이 100인데 약속 완주가 50이면, 서비스가 뭘 핵심 흐름이라 부르든 사용자는 클릭만 한다. **새 사유를 넣을 때 노력 순서표에 자기 자리부터 정한다**
- **★ 계약의 경고는 검증 가능한 형태로 적는다** — §15-4가 "사유 이름을 나열하지 말라"고 경고했는데 SQL이 그대로 어겼다. 자바 주석은 맞게 적혀 있어서 **주석만 보면 통과한다.** 경고에 `reason LIKE 'EARN\_%'`를 같이 박아야 대조가 된다
- **문서에 박힌 총액·일수는 데이터가 늘면 조용히 낡는다** — 카탈로그 `42,260`은 스킨이 없던 값이라 실제(71,660)와 70% 벌어져 있었다. **숫자를 적을 때 세는 쿼리를 같이 적는다**
- **머지 즉시 배포가 손해일 수 있다** — 헬스체크 없는 레포는 배포마다 4분 30초 502다. `#124`를 오전에 머지하고 저녁까지 참아 `#127`·`#128`과 묶었다. **세 번 쐈으면 13분 30초였다**
- **★ 판정 기준을 "결함이 있느냐"가 아니라 "그걸 보는 쪽이 있느냐"에 건다** — 같은 잔여물을 한 번은 지우고 한 번은 안 지웠는데 둘 다 맞았다. 기준이 `ALPHA_THRESHOLD=8`이었기 때문이다
- **★ 알림·카테고리는 주제축이 아니라 행동축으로 먼저 가른다** — "처리할 게 있나"가 "무엇에 관한 것인가"보다 먼저다. 처리함에 정보성 항목이 섞이면 뱃지가 0이 되지 않고, 그때부터 뱃지가 무시된다
- **★ 자기 권고를 코드로 검증한다** — 대칭성만 보고 JOIN이 맞다고 했는데, `Notifications.jsx` 네 줄을 읽고 전제가 틀렸음을 알았다. **판단 근거가 "그럴듯함"이면 코드를 열어본다**
- **자동 검출기가 오탐하면 버리고 손으로 돌아간다** — 크로마키 검출기가 오닉스에서 100건 넘게 잡길래 버리고 면별 분해로 갔다. 그때 케이블(정상)과 코너 잔여물(결함)이 갈렸다
- **한 PR에 원인 수정과 전역 정책 변경을 같이 넣지 않는다** — 나중에 문제가 생겼을 때 어느 쪽 탓인지 못 가린다
- **틀려진 주석·문서는 코드처럼 지운다** — `#241`이 "일부러 지정하지 않는다"는 옛 주석을 같이 지웠고, `#52`가 "탭이 갈리는 것은 알고 내린 결정"을 지웠다. 남겨두면 다음 사람이 "의도된 것"으로 읽는다
- **계약 문서의 부정확한 문장은 판단을 왜곡한다** — "행운편지로 뜬다"가 실제보다 심각하게 적혀 있었다. 순서 압박이 과장되면 배포 계획이 필요 이상으로 조여진다
- **`&&` 체인 앞의 명령이 중단돼도 뒤가 실행될 수 있다** — `git checkout`이 실패 종료코드를 안 냈다. 브랜치를 만들면 `git log origin/main..HEAD`로 확인한다
- **배포 반영은 인증 없이 증명된다 — 방법이 둘이다** — ① 로컬 `dist/assets/`의 해시 파일명이 배포 도메인에서 200 ② **배포된 번들을 받아 바뀐 문자열을 직접 찾기.** ②는 로컬 빌드가 필요 없고 "그 파일 안에 오늘 코드가 있다"까지 본다
