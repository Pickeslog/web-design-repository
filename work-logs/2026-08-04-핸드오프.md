# 핸드오프 — 2026-08-04 (다음 세션 이어받기용)

> 자체완결 요약. 오늘의 한 줄: **PR 47건이 들어가 상점이 17종으로 커졌고, 밤에 외부에 보여주려다 8일 묵은 운영 버그(사진 업로드 전멸)를 찾아 고쳤다. 골드는 여전히 한 푼도 안 돈다.**
> 앞선 맥락: [`2026-08-03-핸드오프.md`](2026-08-03-핸드오프.md) · [`2026-08-03-작업일지.md`](2026-08-03-작업일지.md)
> 오늘 있었던 일: [`2026-08-04-작업일지.md`](2026-08-04-작업일지.md)

---

## 0. 내일 아침 이것부터

1. **clov-web 배포** — 미배포 4커밋이 있다. **동기들 피드백 세션이 끝났으면 바로 배포한다**(§1)
2. **열린 PR 셋 머지** — `clov-web#280`(피드백 템플릿) · `clov-api#134`(골드 지급 스크립트) · `web-design#61`(`ADMIN_GRANT` 계약). **셋 다 실행/적용은 이미 끝났고 코드만 안 들어갔다**
3. **`clov-api#92`** — 요청 6건. **이게 유일한 실질 블로커다.** 지금 `EARN_*` 지급 코드가 main에 없어서 상점이 잠겨 있다(§2)
   > ⚠️ 머지할 때 **하루 상한 합산이 `reason LIKE 'EARN_%'` 접두사인지** 반드시 볼 것. 사유 나열식이면 `ADMIN_GRANT`(밤에 신설)가 상한에 잡혀 **지급받은 날 정상 획득이 통째로 막힌다.**
4. **동기들 피드백 수거** — 이슈에 `피드백` 라벨로 들어온다. 세 레포 다 확인할 것
5. **`personal_invite_code` step2(DROP COLUMN)** — `db/manual-migrations/2026-08-04-drop-personal-invite-code-step2-drop.sql`. **step1과 코드 배포는 08-04에 끝났다.** 파일 안 사전점검을 먼저 돌릴 것 — step1 이후 가입자 행이 `NULL`이면 배포된 코드가 이미 컬럼을 안 쓴다는 뜻이다. **안 급하다** — 지금은 NULL 허용 컬럼이 남아 있을 뿐 아무 문제 없다
6. **빈 탭 둘** — 상점에 `SKIN`·`EVENT`가 0종인 채로 탭만 보인다. 뺄지 채울지 안 정했다

> ✅ **밤에 끝난 것** — 사진 업로드 R2 CORS(8일 묵은 운영 버그, §7) · 동기 60,000 지급 · 외부 피드백 템플릿·라벨. `#264`도 머지됐다.

---

## 1. 현재 상태

| 저장소 | main | 배포본 | 오늘 머지 |
|---|---|---|---|
| `clov-web` | `f940ed1` | **4커밋 뒤** ⬜ | 26건 |
| `clov-api` | `3a38b89` | **동일** ✅ | 10건 |
| `web-design` | (아래 PR 참고) | — | 11건 |

**clov-web에 미배포 4커밋** — `#279`(폰트) · `#264`(View Transitions) · `#274`(Signup 리팩터) · `#282`(약속 티켓 UI 개편).

> ⚠️ **일부러 안 배포했다.** 학원 동기들이 배포본으로 피드백 중이었다. 화면이 도중에 바뀌면 **"아까 본 것"과 "지금 보는 것"이 달라져 피드백이 섞인다** — 특히 `#282`는 UI 개편이다. **세션이 끝난 뒤에 배포할 것.**

오늘 clov-web 9회 · clov-api 4회 배포했고 **열세 번 다 인증 없이 응답으로 확인했다**(§5).

```
shop_items   ACTIVE 17종 (52,000) + RETIRED 11종 (자리표시)
             └ COSTUME 13종 + BACKGROUND 4종 · 완주 17일 · 등급 3·5·5·3·1
users        personal_invite_code 는 NULL 허용 상태 (step1만 끝났다)
             └ step2 DROP COLUMN 이 남았다. 안 급하다 — §0 참고
```

> **08-04 저녁에 상점이 두 배 가까이 커졌다.** 배경 4종(`BACKGROUND` 신설)과 개발 팀장 오닉스가 들어가 34,800 → 52,000, 완주 12일 → 17일이 됐다. **하루 상한 3,000은 안 건드렸다** — 근거는 작업일지 §13-3.

> **`ShopItemResponse`에 `code`가 생겼다.** 계약의 비노출 선언을 뒤집은 것이다 — 화면이 특정 아이템을 지목해야 하는데 `id`는 환경마다 다른 PK다. ⚠️ **`imageUrl`로 대조하는 우회는 쓰지 말 것**(썸네일 경로를 바꾸면 이미 산 사람의 소유가 풀린다). 실제로 한 시간 뒤에 `image_url`을 바꿨다.

### 열린 PR — 전부 chacha1650a 것이다

| PR | 상태 |
|---|---|
| **clov-api #92** 골드 지급 | **요청 6건.** §2가 이 PR 이야기다 |
| clov-web #264 View Transitions | 저녁에 새로 올라왔다(`#263`) — 아직 리뷰 안 함 |
| clov-web #178 골드 배지 즉시 갱신 | 머지 충돌. **`#92` 뒤에 온다** |
| clov-web #165 사용자설정 카드형 | 머지 충돌 (07-30부터) |

> ⚠️ **`#92`를 막고 있던 것은 아무것도 없었다.** 5일 내내 머지 가능한 상태였다 — `build`는 07-30에 pass였고 미해결 리뷰 스레드도 0개다. `BLOCKED`을 충돌로, 다음엔 승인 대기로 읽었는데 **둘 다 추측이었고 둘 다 틀렸다.**
>
> **★ 이 레포에서 PR이 실제로 막히는 조건은 둘뿐이다.**
>
> | | |
> |---|---|
> | `build` 체크 | 실패하거나 **아직 도는 중**이면 `BLOCKED` |
> | 미해결 리뷰 스레드 | `required_review_thread_resolution: true` — **인라인 코멘트 스레드를 남겨두면 막힌다** |
>
> **승인은 0건 필요하다**(`required_approving_review_count: 0`). 리뷰는 팀 규율이지 GitHub 게이트가 아니다.
>
> ```bash
> gh api repos/Pickeslog/clov-api/rules/branches/main    # 추측하지 말고 이걸 연다
> ```

---

## 2. ★★ 골드 — 계약이 앞서 있고 구현이 없다

**다음 세션에서 가장 먼저 알아야 할 것이다.**

### 지금 main의 실제 동작

```
$ git grep -n "EARN_" origin/main -- src/main
(0건)
```

`WalletTransaction`의 사유는 **`PURCHASE`와 `SIGNUP_GRANT` 둘뿐**이다. 행동 지점 여섯 곳(`MemoryService` 2 · `PlanService` 2 · `MascotService` 1 · 이미지 보너스 1)이 전부 `expService.grant(...)`만 부른다 — **XP는 주고 골드는 안 준다.**

| | |
|---|---|
| 들어오는 길 | 가입 시 1,000 — **평생 한 번** |
| 나가는 길 | `PURCHASE` |
| 버는 길 | **없다** |

**카탈로그가 34,800인데 평생 예산이 1,000이다.** 상점은 사실상 잠겨 있다 — 900짜리 COMMON 하나 사면 끝이다.

### 계약 §15-4는 오늘 개정을 끝냈다 (`web-design#53`, 머지됨)

| 사유 | 금액 | 자체 제한 | 하루 최대 |
|---|---:|---|---:|
| `EARN_MASCOT` | 200 | 하루 10회 | 2,000 |
| `EARN_MEMORY` | 300 | 약속 연결 추억만(약속당 1개라 자연 상한) | — |
| `EARN_MEMORY_FREE` | 200 | 하루 3회 | 600 |

**유저 단위 하루 총 상한 `3,000`.**

> ⚠️ **이 상한은 카탈로그 71,860(완주 24일)을 기준으로 정했는데, 같은 날 자리표시 11종을 내리면서 카탈로그가 34,800(약 12일)이 됐다**(§4). **상한은 안 건드렸다** — 반나절 만에 두 번 바꾸면 계약 이력이 흔들리고, 오늘만 스킨이 8종 → 12종으로 늘었다. **살 것이 늘어나는 속도가 상한을 따라잡는 쪽에 걸었다.** 며칠 지켜보고 다시 정할 것.

> **금액 순서가 노력 순서를 따른다** — 클릭 200 < 자유 글 200 < 약속 완주 300. 개정 전에는 **클릭 100 > 약속 완주 50**으로 뒤집혀 있었다. `CLAUDE.md`가 핵심 흐름을 "약속 완료 후 추억 작성"이라 못박아 뒀는데 골드가 반대쪽을 밀고 있었다.
> **새 획득 사유를 넣을 때 §15-4의 노력 순서표에 자기 자리부터 정한다.**

### `#92`에 요청한 5건

| | 내용 | 급함 |
|---|---|---|
| ① | `sumEarnedToday`의 `reason IN ('EARN_MASCOT','EARN_MEMORY')` → **`reason LIKE 'EARN\_%'`** | **필수·최우선** |
| ② | 상수를 계약값으로 (100→200 · 3→10 · 50→300 · 500→3000) | 필수 |
| ③ | `EARN_MEMORY_FREE` 신설(200 · 하루 3회) + `createFree`의 틀린 주석 갱신 | 필수 |
| ④ | 캡 판정이 **행 잠금 밖**에 있다 — `findWalletForUpdate`를 캡 읽기 앞으로 | 권장 |
| ⑤ | 캡 경계 테스트(하루 실질 최대 3,300이 **의도된 초과**임을 못박기) | 가능하면 |
| **⑥** | **`MascotService.DAILY_INTERACTION_LIMIT` 3 → 10** | **필수 — ②와 세트다** |

> **★ ⑥이 ②와 세트인 이유** — 교감 API가 `DAILY_INTERACTION_LIMIT`에서 `429`를 던진다. **`DAILY_GOLD_LIMIT`만 10으로 올리면 4번째 호출이 골드 로직에 도달하지 못해** 하루 2,000이 방 4개 이상 있어야 닿는 죽은 값이 된다. 계약 §12를 10으로 맞춰뒀다(`web-design#55`).
>
> **스코프는 다르게 유지할 것** — 교감 캡은 **방 단위**(`countMascotInteractionsToday(roomId, userId, ...)`), 골드 캡은 **유저 단위**. 방 3개면 교감 30번·골드 10번이 정상이고, 11번째부터 `429`가 아니라 **정상 응답에 `earnedGold: 0`** 이 나가야 한다.

> **★ ①이 제일 급한 이유** — 계약 §15-4가 **이 실수를 이름으로 지목해서 금지**하고 있는데 SQL이 그대로 어겼다. 그리고 ③을 얹는 순간 **자유 추억 골드가 통째로 총 상한 밖에서 무한히 쌓인다.**
> 자바 Javadoc은 *"`EARN_*` 사유 합계만 센다"* 로 **맞게** 적혀 있다 — **주석만 보면 통과한다.** 대조는 SQL까지 내려가야 한다.

**설계 자체는 좋다.** `earnGold`가 실지급액을 반환해 캡에 걸려도 화면이 거짓말을 안 하고, revive 분기가 `earnGold` 앞에서 early return해서 **삭제 후 재작성으로 다시 받는 경로가 없고**, `ClovTime.startOfTodayUtc()`가 KST 자정을 UTC로 환산한다(UTC 자정으로 잘랐으면 한국 사용자는 **오전 9시에 하루가 바뀐다**).

### 순서

```
1. chacha가 #92 요청 6건 처리
2. #92 머지
3. clov-api 배포          ← 여기서 처음으로 골드가 돈다
4. clov-web #178          ← 배지 즉시 갱신. rebase 필요
```

> **★ GitHub은 `#92`를 한 번도 막은 적이 없다**(§1). 요청만 처리되면 바로 머지된다.

### 미결 하나

- **약속(D-day) 생성에는 골드 사유가 없다.** 계약에 `EARN_PLAN`이 없고 `#92`도 `PlanService`를 안 건드린다. 지금 설계는 **생성이 아니라 완주**(약속 → 완료 → 추억 300)에 값을 매긴다 — 생성에 붙이면 만들고 지우기로 벌 수 있어 캡이 하나 더 필요하다. **확정한 적이 없으니 정하고 넘어갈 것.**

**계약 §12의 `earnedGold`는 넣었다**(`web-design#55`). 실지급액이고 총 상한에 걸리면 `0`이 오며 **`0`이 정상 응답**이라는 것까지 적혀 있다.

---

## 3. 알림 — 오늘 세 겹이 다 맞았다

```
계약     §13 MEMBER_LEFT 신설(#50) + 합류·수락을 FRIEND로 이동(#52)
백엔드   MEMBER_LEFT 발송(#124) + type을 FRIEND로(#128)
프론트   FRIEND_SUBTYPE_META에 3종 문구·아이콘(#250)
배포     web 3회차 · api 1회차 — 셋이 같은 시각에 살아 있다
```

### ★ 구조를 반드시 알고 손댈 것

| | |
|---|---|
| `type` | **어느 탭에 보일지.** `NOTICE` / `FRIEND` / `JOIN` 세 값 고정 |
| `subType` | **무슨 일이 있었는지** |

**`JOIN` 탭은 알림 테이블을 조회하지 않는다.** `Notifications.jsx`의 `enabled: activeTab !== 'JOIN'`이 그 자리고, 대신 가입신청 리소스를 부른다. 라벨도 `'가입 신청'`이다 — **알림 피드가 아니라 승인/거절하는 처리함이다.**

> **그래서 `type='JOIN'`으로 만든 알림은 화면 어디에도 안 뜬다.** `MEMBER_JOINED`·`JOIN_ACCEPTED`가 그 상태였고 오늘 `FRIEND`로 옮겼다.

**분류 기준** — 알림은 주제축("멤버에 관한 것끼리")이 아니라 **행동축("처리할 게 있나")으로 먼저 가른다.** 처리함은 비워야 하는 통이라 안 없어지는 정보성 항목이 섞이면 뱃지가 0이 되지 않고, 그때부터 사용자가 뱃지를 무시한다.

**`TYPE_JOIN` 상수는 남아 있고 그게 맞다** — 계약 §13에 `JOIN` 탭과 `JOIN_REQUEST` 정의가 살아 있다. 미사용이라고 지우지 말 것.

**현재 `FRIEND`에 있는 subType** — `MEMBER_JOINED` · `JOIN_ACCEPTED` · `MEMBER_LEFT` (+ 기존 추억·약속·레벨업)

### ★ 아이콘 이름은 넣기 전에 CDN에서 확인할 것

`#250`이 쓴 `ti-user-minus`를 검증하면서 **앱 전체 정적 아이콘 42종을 CDN 4,962종과 대조**했다.

```
ti-user-minus   content:"\eb4a"   있다 ✅
ti-bubble       CSS 전체 0건       ← bubble 계열 자체가 없다 (RoomList "사라진 방")
```

**찾던 건 멀쩡했고 다른 게 걸렸다.** `#255`로 `ti-circle-off`로 바꿨다.

> Tabler 웹폰트는 CDN(`index.html:15`)이라 **없는 이름이면 에러 없이 빈 네모**가 된다. 대조는 이 한 줄이다.
>
> ```bash
> curl -s "https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@2.47.0/tabler-icons.min.css" | grep -c "\.ti-<이름>:"
> ```

---

## 4. 스킨·마스코트 현황

| | 아침 | 저녁 |
|---|---|---|
| 판매 중인 상품 | 자리표시 12종 + 스킨 5종 | **스킨 12종만** (34,800) |
| 스킨을 가진 마스코트 | 4/6 | **5/6** (버거노인만 없다) |
| 마스코트가 보이는 화면 | 1곳 | **4곳** (우정공간·추억피드·행운편지·일정계획) |

```
COMMON     900  옐로우 크루 · 수박껍질모자 · 마법학교 학생
UNCOMMON 1,500  철골 작업반 · 블랙스타 공방 · 신입 개발자 · 탐험가 · 개발 경력자
RARE     2,800  ROV 조종사
EPIC     6,000  마지막 신호병 · 오션 구조대
LEGEND   9,800  아케이드 보스
```

**등급도 같고 내용(상태 9종)도 같으면 값도 같게 둔다** — 이 규칙 하나로 12종이 다 정해졌다. **'신입 개발자'(김철수)와 '개발 경력자'(크로비)가 같은 1,500인 것도 의도다.** 이름은 경력 차이를 말하지만 **값을 가르는 축은 등급과 내용이지 세계관이 아니다.**

### ⚠️ 자리표시 11종을 판매 종료했다 — 되돌릴 수 있다

07-29 시드 12종은 에셋 없이 이름과 가격만 있던 자리표시였다. `RETIRED`로 내렸고 **`DELETE`가 아니다.**

```
user_inventory_items.item_id        FK RESTRICT   ← 실제로 6명이 갖고 있었다
user_preferences.equipped_item_id   FK RESTRICT
```

**산 사람은 그대로 갖고 장착도 된다**(`equip`은 `status`를 안 본다 — 의도된 것). 되돌리려면 한 줄이다.

```sql
UPDATE shop_items SET status = 'ACTIVE' WHERE code IN ( ... );
```

**같이 고친 것** — `ShopMapper.findCatalog`에 `status='ACTIVE'` 조건이 없어서 내린 상품이 목록에 그대로 뜨고 누르면 실패하는 상태였다(`clov-api#130`). **테스트 셋업에 `TEST_RETIRED_SKIN`이 이미 있었는데 구매 차단만 검증하고 목록 쪽은 비어 있어서** 이 버그가 통과해 왔다.

> ⚠️ **`SKIN`·`EVENT` 카테고리가 0종이 됐다.** 상점 탭 3개 중 2개가 빈 화면이다. **배경 스킨으로 `SKIN`을 채우는 방향으로 논의 중**(§6).

### ★ 새 상품을 넣기 전에 반드시 — `code` 충돌 확인

등록 SQL은 `ON DUPLICATE KEY UPDATE`라 **코드가 겹치면 기존 상품이 통째로 덮이고 에러가 안 난다.** 08-04에 실제로 `COSTUME_ROB_EXPLORER`가 시드 상품(EPIC 5,200)을 덮어써 상품 하나가 사라졌다(리더 판단으로 그대로 뒀다).

```sql
SELECT code, name, rarity, price FROM shop_items WHERE code IN ( ...넣을 코드들... );
```

**0행이어야 새로 넣는 것이다.** 등록 SQL의 검증 쿼리로는 못 잡는다 — **"4행 반환"은 4개를 넣었을 때와 1개를 덮고 3개를 넣었을 때가 똑같다.**

### 새 스킨 넣을 때 (이 순서를 지킬 것)

```
1. public/shop/skins/<mascot-id>/<skin-id>/  9장 + metadata.json
2. Mascot.jsx의 EQUIPPED_SKIN_STATE_SPRITES에 경로 한 줄
3. shop_items 행 (category='COSTUME' — SKIN이면 구매는 되고 장착이 막힌다)
4. 전수 검사 쿼리
```

**조용히 깨지는 값 둘** — `category`가 `SKIN`이면 `ITEM_NOT_EQUIPPABLE`, `image_url` 오타면 `endsWith` 매칭이 실패해 **에러 없이 상태 전환만 사라진다.**

```sql
SELECT code, rarity, price, category, status,
       IF(category = 'COSTUME' AND image_url LIKE '/shop/skins/%', 'OK', '★ 확인') AS chk
FROM shop_items WHERE image_url LIKE '/shop/skins/%' ORDER BY sort_order;
```

### ⚠️ 에셋 내보내기 — 결함 하나는 멈췄고 하나는 6종 연속이다

| | 상태 |
|---|---|
| `metadata.json` `states` 절대 경로 | **멈췄다** — `senior-developer`부터 파일명 형식으로 나온다 |
| 캔버스 코너·최하단 **고립 픽셀** | **6종 연속** (옐로우 크루 · 수박껍질모자 · 오늘 4종) |

**판정은 "잔여물이 있느냐"가 아니라 "도구가 그걸 보느냐"에 건다.** `SpriteTool.cs`의 `ALPHA_THRESHOLD = 8`이 기준이다.

```
알파 ≤ 8   도구도 렌더도 못 본다   → 안 지운다 (수박껍질모자)
알파 > 8   Measure가 본다          → 지운다   (옐로우 크루 41 · 오늘 4종 9~37)
```

지우는 방법은 **`y < 12` 또는 `y >= 628` 두 띠에서 알파 ≤ 48만** 비우는 것이다. 본체는 어느 스킨도 그 띠에 안 닿는다(minY 22+, maxY ≤ 617). 지운 뒤 **bbox가 형제 범위로 돌아오는지**로 확인한다.

> **한 점이 두 값을 동시에 망친다.** `senior-developer`의 좌하단 코너(0, 639) 한 픽셀이 `minX=0`과 `maxY=639`를 같이 만들었다. **bbox가 캔버스 전체면 픽셀이 많은 게 아니라 구석에 하나 있는 것**일 때가 많다.

**경로 문제는 내보내기 쪽에서 멈췄으니 알파도 거기서 끝낼 수 있다.** 매번 손으로 잡는 건 이제 여섯 번째다.

---

## 5. ★ 배포 — 규칙 셋

### ① 머지는 배포가 아니다

```bash
git -C clov-web push origin origin/main:deploy-myeongjun
git -C clov-api push origin origin/main:deploy-myeongjun
```

상태 확인은 `git log origin/deploy-myeongjun..origin/main`.

### ② clov-api는 배포마다 4분 30초 502가 난다 — 묶어서 쏜다

헬스체크가 없다(`clov-api#108`, 08-02부터 열려 있음). 오늘은 `#124`를 오전에 머지하고 **저녁까지 참아** `#127`·`#128`과 한 번에 쐈다. 세 번 나눠 쐈으면 **13분 30초**를 죽었다.

> **헬스체크 없는 레포에서는 머지 즉시 배포가 오히려 손해다.**

### ③ 초록불은 "컨테이너가 떴다"까지다

- **clov-api** — `curl https://clovlabcalss.store/api/v1/rooms` → **401이면 정상, 502면 아직**
- **clov-web** — 워크플로에 헬스체크가 있어 초록불이 곧 정상(`#182`)

**clov-web은 인증 없이 내용까지 증명된다. 방법이 둘이다.**

| | |
|---|---|
| 해시 대조 | 로컬 `dist/assets/`의 콘텐츠 해시 파일명을 배포 도메인에 요청 → 200 |
| **문자열 검색** | 배포된 번들을 받아 **바뀐 문자열을 직접 찾는다.** 로컬 빌드가 필요 없고 "그 파일 안에 오늘 코드가 있다"까지 본다 |

```bash
curl -s https://clovlabcalss.store/ | grep -o '/assets/[^"]*\.js'
```

`REACT_ENV_deploy-myeongjun` 시크릿이 없어 레포의 `.env.production`(공용 도메인)을 쓴다는 경고가 매번 뜬다 — **리더 배포에서는 의도한 동작이다.**

---

## 6. 다음에 할 것

### 리더

1. **`#92` → clov-api 배포 → `clov-web#178`** (§2). **이게 가장 오래된 블로커다** — 07-30부터고, 이게 풀려야 상점 경제가 처음 돈다. **GitHub은 이걸 막은 적이 없다**(§1)
2. **`personal_invite_code` step2(DROP COLUMN)** — 안 급하다. §0 참고
3. **`clov-web#264` 리뷰** — 저녁에 chacha가 올렸다(View Transitions)
4. **약속(D-day) 생성 골드를 정한다** — 지금은 사유 자체가 없다(§2)
5. **빈 탭 둘을 어떻게 할지** — `SKIN`·`EVENT`가 0종인데 탭은 보인다. 배경은 재활용이 아니라 `BACKGROUND`를 새로 만들어 해결했다
6. ~~**하루 상한 3,000 재검토**~~ — **08-04 저녁에 풀렸다.** 상품이 5종 늘어 52,000/17일이 됐다. **상한은 그대로 3,000이다** — 낮에 12일이 됐을 때 안 건드린 판단이 같은 날 맞았다(작업일지 §13-3)
7. ~~**배경 스킨**~~ — **끝났다.** 사계절 4종을 `BACKGROUND` 카테고리 RARE 2,800으로 올렸고, 기존 5종은 무료로 남겼다. 사용자설정 잠금·상점 카드 그림·양방향 이동까지 배포 완료

### ★ 배경 스킨 — 이미 있는 기능에 잠금만 붙이면 된다

리더가 "스킨 = 배경"으로 읽었고, **그게 사용자 직관과 맞다.** 그리고 배경 기능은 **이미 다 만들어져 있다.**

```js
// src/lib/appBackground.js — 6종 하드코딩
APP_BACKGROUNDS  →  applyAppBackground(id)  →  :root에 --clov-app-bg 세팅
저장             →  localStorage('clov_appBgTheme')   ← 서버 아님
```

**빠진 건 "소유 판정"뿐이다.** `Settings.jsx:255`가 목록을 전부 그냥 그린다. 여기서 `GET /shop/inventory`를 읽어 **안 산 건 잠그면** 끝이다.

| | |
|---|---|
| 백엔드 | **없음** — 소유 판정은 `user_inventory_items`가 이미 한다 |
| 계약 | **없음** — `equip`을 안 쓴다. 배경 선택은 `equipped_item_id`가 아니라 클라이언트 설정이라 §15-1 (3)("장착은 COSTUME만")이 그대로 유효하다 |
| DB | `shop_items`에 `SKIN` 행 추가 (`code`를 배경 `id`와 맞춘다) |
| 프론트 | `appBackground.js`에 항목 추가 + `Settings.jsx`에서 소유 여부로 잠금 |

**에셋 규격**(기존 실측)

```
배경    1920 × 1080   WebP   67KB ~ 424KB     public/backgrounds/<id>.webp
썸네일   512 × 512     PNG    26KB ~ 155KB     public/bg-thumbs/<id>.png
```

> ⚠️ **기존 6종은 무료로 그대로 둘 것.** 이미 배포돼서 다들 쓰고 있다 — 값을 붙이면 **쓰던 걸 뺏는 것**이다. **새로 만드는 것부터 판다.**

**한 종당 그림 2장이면 된다.** 마스코트 스킨(9장 + 알파 검사 + 배선)보다 훨씬 싸서 **카탈로그를 늘리는 가장 빠른 수단**이다.

### 팀원

| 담당 | |
|---|---|
| **lami2342** | **오늘 10건 — 팀에서 가장 많이 움직였다.** 알림 도메인 세 겹을 끝내고 저녁엔 **인증 화면 이슈 4건(#251·#252·#253·#259)을 직접 발굴해 전부 PR까지** 냈다. 대기열이 비었다 — **배경 스킨이 이 사람 자리다** |
| **chacha1650a** | `clov-api#92` 요청 6건 → `clov-web#178` → `#165`. **열린 PR 4건이 전부 이 사람 것이다.** 다만 `#246`·`#127`을 오늘 머지했고 저녁엔 `#264`를 새로 올렸다 — **멈춰 있는 건 사람이 아니라 오래된 PR이다** |
| **kimgyubi1234** | `#258` 폰트 진행 중. **착수 전에 세 가지를 코멘트로 붙여뒀다** — `unicode-range` 분할(몽토리체가 3.03MB로 JS 번들의 4.6배다) · `--sans`만인지 `--heading`도인지 · `dashboard.proto.css`의 `font-weight` 41건이 전부 600↑이라 디스플레이 서체를 얹으면 위계가 사라진다 |

### 열린 이슈 (이월)

- **clov-api #108** 배포 헬스체크 부재 — 오늘 두 번 겪었는데 **한 번은 17초, 한 번은 4분 30초**였다. 편차가 크니 매번 `curl`로 확인할 것
- **clov-api #89** 골드 지급(=`#92`의 원 이슈) · **#94** 팀원별 CI/CD · **#72** 개인 최종 배포
- **web-design #42** DB 스펙 DDL 주석에 `MEMBER_JOINED` 누락
- **clov-web #177** 상점 골드 렌더링(=`#178`의 원 이슈)
- **clov-web #262** Signup shake가 0.4초 안에 연속 실패하면 재시작 안 됨 — **미배정.** 제출 버튼을 400ms 안에 두 번 눌러야 재현되는 기존 한계다
- **clov-web #263** 사이트 전체 애니메이션 — `#264`로 진행 중

### ⬜ 리더가 오늘 넘긴 것

- **`clov-web#254` 가드를 안 넣고 머지했다.** `TermsToggle`(약관 보기 버튼)이 `TermsRow` 안에 있는데 `keydown`에 `stopPropagation`이 없다. 버튼에 포커스를 두고 Enter를 누르면 행 핸들러의 `preventDefault()`가 **버튼의 클릭 생성을 막아** 패널이 안 열리고 대신 동의가 토글될 수 있다. **`if (event.target !== event.currentTarget) return` 한 줄이면 막힌다.** 실제 키 입력으로 재현은 못 했다 — **명세상 그렇게 되는 자리**로 남겨둔다
- **`clov-web#243`의 `<ScrollRestoration />`** — 오전에 같은 방식으로 머지됐다. 앱 전역 정책이라 나중에 스크롤이 이상하면 여기를 먼저 볼 것

---

## 7. ⚠️ 미결·주의

### ★★★ R2 버킷 CORS — 배포할 때 같이 안 따라오는 설정이다

`2026-08-04` 밤에 **사진 업로드가 07-27 배포 이후 8일간 죽어 있었던 것**을 찾았다. 대표 사진·프로필 사진·추억 사진·인생4컷 **전부**였다.

```
presign 호출  →  clovlabcalss.store API   →  ✅ 백엔드 CORS 통과
R2 로 PUT     →  r2.cloudflarestorage.com →  ❌ 버킷이 localhost:5173 만 허용
```

`07-27` 배포 때 `APP_CORS_ALLOWED_ORIGINS`를 배포 도메인으로 바꿨다. **그건 백엔드(Spring) CORS다.** R2 버킷 CORS는 **Cloudflare 대시보드 쪽 별개 설정**이라 같이 안 따라갔다.

> **★ 왜 8일간 아무도 몰랐나 — 로컬에서는 멀쩡했다.** 버킷이 허용한 오리진이 정확히 개발 환경(`localhost:5173`)이라, **개발자가 자기 화면에서 확인하는 한 영원히 안 보인다.**

현재 값(수정 완료):

```json
[{ "AllowedOrigins": ["https://clovlabcalss.store", "http://localhost:5173"],
   "AllowedMethods": ["PUT", "GET"], "AllowedHeaders": ["Content-Type"] }]
```

위치: `Cloudflare → R2 → clov-media → Settings → CORS Policy`. **코드 배포 없이 저장 즉시 반영된다**(8일 묵은 버그의 수정이 2분이었다).

⚠️ **새 도메인으로 배포하면 여기에 그 오리진을 반드시 추가한다.** `localhost:5173`은 빼지 말 것 — 팀 로컬 개발이 막힌다.

⚠️ **증상은 `Failed to fetch`다.** 이건 `fetch`가 던진 것이지 서버 응답이 아니다 — `!res.ok` 경로면 `이미지 업로드에 실패했어요 (403)`으로 뜬다. **문구로 범위가 갈린다.**

---
### 검증 제약 (안 지키면 헤맴)

- **Claude는 로그인 뒤 화면을 못 본다.** 마스코트·상점·알림이 전부 대시보드 안이라 코드 대조까지가 한계다
  - **우회법 셋** — ① lami2342가 `#228`에서 쓴 **axios 어댑터 스텁**(가장 좋다) ② **정적 자원은 로그인 없이 `curl`로 검증된다** ③ **배포 번들에서 문자열 찾기**(§5-③)
- **★ Browser pane이 안 보이면 이미지 디코드도 안 된다.** `new Image()`가 `onerror`로 떨어지고 `naturalWidth`가 0인데 **`fetch`는 200에 정상 바이트를 준다** — "경로는 맞는데 이미지만 깨진 것"으로 오진하기 쉽다
- **Docker 미설치 → Testcontainers 로컬 실행 불가.** `compileJava`/`compileTestJava`까지만
- **`gh`가 PATH에 없다** — `C:\Program Files\GitHub CLI\gh.exe` 전체 경로. **PR 머지·`deploy-*` push는 사용자가 직접 실행한다**(분류기 차단)
- **JDK가 PATH에 없다** — `$env:JAVA_HOME` 먼저 세팅. clov-api는 **Java 21** 요구

### 이월

- **버거노인 8종** — 유일하게 상태 머신이 안 붙는 캐릭터(1/9)
- **`crobi/dizzy.png` 오프모델** — 검정 모자·흰 셔츠·회색 바지라 **어지러울 때 옷을 갈아입는다.** 프로토타입 원본이 그렇다
- **롭 나머지 5종 원본 점검** — `angry`·`find`·`idle`·`pencil`·`sleep`은 07-20 원본 그대로라 대조한 적이 없다
- **`_bak_user_wallets_20260802`** 스냅샷 테이블 — 데모 끝나면 `DROP TABLE`
- **테스트 골드** — `grant-test-gold.sql`로 지급한 잔액이 남아 있다. **`reason`은 `SIGNUP_GRANT`를 쓸 것** — `EARN_` 계열을 쓰면 하루 상한 합산에 잡혀 그날 정상 획득이 막힌다
- **certbot 갱신** — 인증서 10/26 만료
- **`StoragePresigner`에 삭제 메서드 없음** — `deleteImage`가 DB 행만 지우고 R2 객체는 남는다
- **`findCatalog`에 `status` 조건이 없다** — INACTIVE 아이템이 목록에 뜨고 사면 실패한다("보이는데 못 사는" 상태). **아직 이슈로 안 만들었다**
- **모서리 실험은 버렸다**(08-03 리더 판단). 측정 결과만 `2026-08-03-핸드오프.md` §7에 남아 있다. **찾지 말 것**

---

## 8. 집에서 시작할 때 첫 명령

```bash
git -C clov-api checkout main && git -C clov-api pull
git -C clov-web checkout main && git -C clov-web pull
git -C web-design-repository checkout main && git -C web-design-repository pull
```

**이 문서의 번호를 믿지 말고 `gh pr list`·`gh issue list`로 확인할 것.** 그리고 **배포 상태는 `git log origin/deploy-myeongjun..origin/main`으로 확인한다** — main 머지는 배포가 아니다.
