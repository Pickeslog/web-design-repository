# 핸드오프 — 2026-07-27 (다음 세션 이어받기용)

> 자체완결 요약. 이 세션은 **#111 상점을 뒤로 미루고**(팀원이 아직 구상 전) 리더 무소유 레인 작업을 진행했다.
> 앞선 맥락: `2026-07-26-핸드오프.md`.

---

## 1. 현재 상태

- **clov-web main = `966fa73`** · **clov-api main = `84659da`** · **web-design main = `6f7f5c6`**
- **열린 PR 1건** — clov-web **#111 상점 UI**(chacha1650a). **팀원이 아직 구상 전이라 리뷰 보류**(리더 판단 2026-07-27). 재촉하지 말 것.
- **열린 이슈 3건** — clov-web `#96`(상점, → #111) · `#114`(겹침 카드, **P1만 끝나서 열어둠**) · `#127`(약속 여정 모달, **착수 승인됨**)
- **오늘 올린 것은 전부 머지됐다.** 리더 무소유 레인 백로그도 비었다(§7).
- ⚠️ **로컬 백엔드 재기동 필요** — `cd clov-api && ./gradlew.bat clean bootRun`. **#70(카드 기본 테마)이 재기동 전에는 반영 안 된다.**

---

## 2. 오늘 머지된 것

| PR | 저장소 | 내용 | 머지 커밋 |
|---|---|---|---|
| #130 | clov-web | 겹침 카드(coverflow) 정본 3D 재이식 — **P1만** (#114) | `05596c6` |
| #71 | clov-api | 증거 카드 기본 테마를 명세 정본(겹침 카드)으로 (#70) | `84659da` |
| #131 | clov-web | 글쓰기 버튼 호버 색 + 한 줄 메시지 등록 실패 시 입력 소실 | `0398a2c` |
| #132 | clov-web | 다가오는 D-day 남는 칸을 지난 약속으로 채움(정본 top3 이식) | `8e78c92` |
| #133 | clov-web | 일기장 테마 이미지 3종 WebP — 1634KB → 84KB(−94.9%) | `966fa73` |

문서: web-design `6f7f5c6`(계약 §5 `memoryCardTheme` 허용값·기본값 표 신설)

**#132에서 부수적으로 해소된 것**: 대시보드만 `['plans', roomId, { status: 'SCHEDULED' }]`라는 자기 키를 써서 Schedule·Feed(`['plans', roomId]`)와 캐시를 못 나누고 **같은 방 plans를 두 번 받고 있었다.** status 필터를 떼면서 키를 통일해 요청이 하나 줄었다. 기존 무효화는 전부 `['plans', roomId]` 접두라 그대로 맞는다.

**#132 상태별 규칙**(프로토타입엔 상태 개념이 없어 프로덕션 계약에 맞춰 정한 것): 다가오는 칸 = `SCHEDULED`만(기존 동작 그대로) / 지난 칸 = `CANCELED` 제외, 최근순(완료한 약속 + 완료 처리 안 한 채 지나간 약속 둘 다). `ddayUrgency`의 `is-past` 회색 배지가 이제 실제로 렌더된다.

---

## 3. #114 겹침 카드 — P1만 끝났다 (이슈 열어둠)

`Closes` 대신 **`Refs #114`** 로 머지했다. 남은 것:

- **P2 방향성 슬라이드 전환** — `.cline-cards`에 `slide-past`/`slide-current` 토글, keyframes 10개(`cf-deck-*` 2 + `cf-current-*` 4 + `cf-past-*` 4). 정본 `space.js:1255`의 `evidenceSlideDirection`이 그 역할. `prefers-reduced-motion` 대응 필수.
- **P3 3D 마우스 틸트** — `--rotateX`/`--rotateY` + `is-3d-hovering`(최대 ±18°/±15°). 정본 `space.js:1330-1360`.

P1에서 함께 처리한 것: 빈 슬롯 클래스 `isDiary` → `isFanned`(정본 `space.js:1191`), 좁은 화면 ±3 슬롯 숨김(`@media max-width:900px` — 정본은 desktop viewType에서만 렌더).

**리더 눈 확인이 아직 안 됐다면 main에서 한 번 봐야 한다** — 설정 → 증거 카드 테마 "겹침 카드". 양옆 카드가 안쪽으로 비스듬히 눕고, 호버 시 크기는 그대로 밝기만 오르면 정상.

---

## 4. ★ 카드 기본 테마 드리프트 — 원인과 교훈 (#70)

명세가 정한 기본 테마(**겹침 카드**)가 **아무에게도 적용되지 않고 있었다.** 신규 가입자는 전원 빨랫줄로 시작했다.

`UserService.preferences()`는 설정 row가 없으면 **최초 조회 때 lazy insert**를 하는데, 그 insert가 `'clothesline'`을 하드코딩하고 있었다. **row가 한번 생기면 서버가 항상 값을 채워 주므로 프론트 fallback(`'stack'`)은 영영 도달하지 않는다.**

4곳이 전부 달랐다:

| 위치 | 값 |
|---|---|
| 명세(정본) `09-component-inventory.md:100` | 겹침 카드(coverflow, 기본) |
| 프로토타입(정본) `clov-profile-modal.js:847` | `'coverflow'` |
| 계약 예시 · 백엔드 insert | `clothesline` |
| 프론트 fallback | `stack` (도달 불가) |

**교훈 2개**

- **"프론트에 fallback이 있으니 기본값은 거기서 정해진다"가 틀릴 때가 있다.** 서버가 값을 채워 주는 필드는 프론트 fallback이 죽은 코드다. **기본값을 바꾸려면 서버를 고쳐야 한다.**
- **계약에 허용값 정의가 없으면 드리프트가 조용히 생긴다.** `memoryCardTheme`은 계약에 예시 한 줄만 있고 허용값·기본값 정의가 없었다. 이번에 표를 신설했다(`API-CONTRACT.md` §5). **enum성 필드는 예시가 아니라 표로 박을 것.**

**기존 row는 건드리지 않았다** — 자동 배정과 실제 선택을 구분할 방법이 없어 일괄 UPDATE하면 진짜로 빨랫줄을 고른 사람의 선택까지 지운다. 기존 사용자는 설정에서 직접 바꿔야 한다. (덮어쓰기가 필요하면 별도 SQL)

---

## 5. #127 약속 여정 모달 — 착수 승인됨

리더 확정: **인생4컷 극장과는 다른 기능.** [이슈 코멘트](https://github.com/Pickeslog/clov-web/issues/127#issuecomment-5086221085)로 전달 완료, kimgyubi 착수 가능.

- **극장(입장하기)** = 일정계획에서 방의 4컷을 풀스크린 전체 상영 → 07-25 결정대로 **안 가져옴**
- **약속 여정(#127)** = 추억 상세에서 **그 추억에 연결된 약속 하나**의 4컷 진행 상황 → 진행

스코프 주의를 코멘트에 함께 남겼다(극장 연출로 번지면 착수 전 재확인).

---

## 6. ★ 경험치 배너 — 손대지 말 것 (리더 지시 2026-07-27)

**팀원 작업을 먼저 보고, 그 뒤에 부족한 게 보이면 그때 한다.** 이슈도 만들지 않았다(팀원 작업과 겹쳐 보일 수 있어서). 아래는 **조사 결과 기록일 뿐 착수 대상이 아니다.**

**이미 동작하는 것**: 백엔드 `ExpService` 단일 통로(적립 5종·레벨당 100·연속 레벨업·만렙 777·`FOR UPDATE`·LEVEL_UP 알림) / 프론트 `lv-pill` 게이지·티어 배지·XP 펄스·히스토리 모달(오늘 필터·사진 보너스 묶음)·티어업(111)/만렙 파티클·생일 이벤트·XP 지점 5곳 쿼리 무효화.

**정본과 어긋난 것 2건** (이슈 없음, 판단 보류):

| | 정본 `styles/space.css` | 현재 |
|---|---|---|
| 게이지 색 | 계절별 **그라디언트** + 우측 26px **shine**(`lv-pill-bg::after`) | 계절별 **단색**, shine 없음. 봄 색 계열도 다름(정본 연두 `rgba(104,204,104)` vs 현재 분홍 `rgb(198,72,120)`) |
| 만렙 배지 | `+777` (`space.js:1489`) | `Lv.777` (`Dashboard.jsx`) |

게이지 색은 **"승격(의도된 편차)이냐 복원이냐"가 미정.** 만렙 표기는 정본이 명확해 한 줄 수정.

**명세에 있으나 MVP에서 뺀 것**(`이벤트-기능정의서.md` §9, 백엔드 구현 흔적 0): §9.2 XP 가속도(누적 게시글 30/50/70/100 → ×1.2~×3.0) · §9.3 기억의 샘(일일 패시브) · 편지 XP · §9.4 마스코트 상태 이벤트(어지러움·졸음·혼잣말·드래그·공포/화남 — `Mascot.jsx` 주석에 "범위 밖" 명시).

---

## 7. 미결 — 이슈로 안 만든 것들

**07-26에서 이월된 것은 오늘 전부 해소됐다.** 리더 무소유 레인 백로그는 비어 있다.

| 미결 | 상태 |
|---|---|
| ~~#109 기본값 무효~~ | ✅ 이슈 #70 → PR #71 머지 |
| ~~hover 색 · 메시지 입력 소실~~ | ✅ PR #131 머지 |
| ~~#112 남는 칸~~ | ✅ PR #132 머지 (정본 top3 이식, (가)안) |
| ~~diary PNG 1.6MB~~ | ✅ PR #133 머지 (WebP, −94.9%) |
| **#105 게이지 계절색** · **만렙 배지 표기** | §6으로 이동 — 경험치 레인이라 **보류**(리더 지시) |

### 다음에 잡을 것 (무소유 레인이 비었으므로)

- **#114 P2·P3** — 겹침 카드 방향성 슬라이드 전환 / 3D 마우스 틸트(§3에 상세)
- **미이식 3화면**(아래) — 다만 **배너 시간대 테마는 경험치 배너와 같은 영역**이라 §6 보류 지시에 걸리는지 먼저 확인할 것
- 팀원 쪽이 풀리면 **#111 상점 리뷰**·**#127 착수 지원**

### 프로토타입 미이식 잔여

배너 **시간대 테마**(아침/낮/저녁/밤) · **auth Login·Signup** · **JoinRoom** — 이 셋만 `.proto.css` 없이 styled-components로 남아 있다. 배너 시간대 테마는 경험치 배너와 같은 영역이라 §6 지시와 겹치는지 먼저 확인할 것.

---

## 8. 오늘 나온 교훈

- **기본값은 "누가 마지막에 쓰는가"로 결정된다.** 프론트 fallback이 있어도 서버가 lazy insert로 값을 채우면 fallback은 죽은 코드다. 기본값 버그를 볼 때 **쓰기 경로를 끝까지 따라갈 것.**
- **단계로 나뉜 이슈는 P1을 머지해도 `Closes`를 쓰지 않는다.** 자동으로 닫히면 P2·P3가 통째로 유실된다. `Refs #N`으로 두고 PR 본문에 남은 단계를 적는다.
- **이월 항목은 "다음 PR에 얹기"로 두면 또 넘어간다.** hover 색은 #128 → #129를 그냥 통과했다. 이번엔 별도 PR(#131)로 뺐다. **얹지 말고 분리할 것.**
- **CSS 특이성 동률은 주입 순서로 갈린다.** `.clov-btn--action:hover`(0,3,0)와 `.proto-*` 기본 색(0,3,0)이 동률이면 나중에 주입되는 `Button.css`가 이긴다. **프로토타입 스코프에서 공용 컴포넌트를 쓸 땐 호버 상태의 색까지 다시 못박아야 한다.**
- **같은 데이터를 화면마다 다른 쿼리 키로 받으면 중복 요청이 된다.** 대시보드만 `status` 필터를 키에 넣어 Schedule·Feed와 캐시를 못 나누고 있었다. **필터를 바꿀 일이 생기면 다른 화면이 같은 리소스를 어떤 키로 받는지부터 볼 것.**
- **투명 PNG 최적화는 "알파를 실제로 쓰는지"부터 확인한다.** `diary-memo.png`는 RGBA로 저장돼 있었지만 **투명 픽셀이 0개**였다. 인코더가 알파 평면을 뺀 걸 버그로 오해할 뻔했는데, 원본을 전수 검사하니 정상 최적화였다. 파일 이름·색 타입이 아니라 **픽셀을 봐야 한다.**
- **이미지 변환 품질은 "보이는 픽셀"만 재야 한다.** 전체 픽셀로 색차를 재면 알파=0 영역의 무의미한 RGB 때문에 최대 색차가 255로 나와 겁먹게 된다. **렌더 크기로 축소한 뒤 알파가 있는 픽셀만** 집계하면 실제 체감(평균 2/255)이 나온다.
- **Vite dev 서버는 없는 경로에 index.html을 200으로 준다.** 파일이 지워졌는지를 HTTP 상태코드로 확인하면 안 된다 — 지운 PNG 경로가 200을 반환해 잠깐 헷갈렸다. **`Content-Type`을 볼 것**(`text/html`이면 fallback, `image/webp`면 진짜 파일).
