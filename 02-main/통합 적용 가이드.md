# growth-detail 리디자인 — 통합 적용 가이드

포토부스 영수증 스타일(확정안 **1e / 2a**) + STATUS 제거(**5b**) + 생성 모달 단계 넣기(**5d**).
아래 순서대로 3개 파일만 수정하면 끝납니다.

| 파일 | 무엇을 |
|---|---|
| `02-main/index.html` | ① 폰트 링크 ② 툴바에 "단계 넣기" 버튼 |
| `02-main/css/desktop.css` | ③ `growth-detail.css` 내용으로 교체 ④ 버튼 스타일 |
| `02-main/js/desktop.js` | ⑤ 도장색 변수 ⑥ 상세 마크업 교체 ⑦ 함수 추가 |

---

## ① index.html — 폰트 (`<head>`에 없으면 추가)

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Gothic+A1:wght@400;500;700;800;900&display=swap" rel="stylesheet">
```

## ② index.html — 툴바 "단계 넣기" 버튼

`.rich-editor-toolbar`(≈545행) 안, **구분선(hr) 버튼 바로 다음**에 추가.
오른쪽 끝으로 밀고 싶으면 사이에 `<div style="flex:1"></div>` 하나 넣기.

```html
<div class="rich-divider"></div>
<button type="button" class="rich-btn rich-btn-steps"
        onclick="insertScheduleSteps('dt-input-schedule-body')"
        title="약속 4단계(제안·일정·확정·만남) 한 번에 넣기">
    ＋ 단계 넣기
</button>
```

---

## ③ desktop.css — 영수증 스타일 교체

기존 규칙 **`.growth-detail`, `.growth-detail::after`, `.growth-detail .spotlight-note`,
`.mobile-window .growth-detail`, 그리고 `.spotlight-*` 블록(≈5906–6010행)** 을 삭제하고,
같은 폴더의 **`growth-detail.css` 전체 내용**을 그 자리에 붙여넣기.
(라이트/다크/모바일/빈 상태/서식까지 전부 포함. `.spotlight-*`는 새 마크업이 `.receipt-*`를 쓰므로 남겨둬도 무해)

## ④ desktop.css — 단계 넣기 버튼 스타일 (아무 곳에나 추가)

```css
.rich-btn-steps {
    width: auto;
    padding: 0 10px;
    font-size: 12px;
    font-weight: 800;
    color: #fff;
    background: var(--btn-primary-bg, #357a58);
    border-radius: 6px;
    white-space: nowrap;
}
.rich-btn-steps:hover { filter: brightness(1.08); }
```

---

## ⑤ desktop.js — 도장색 변수

이미 있는 `const ddayPhrase = ...` **바로 아래**에 한 줄 추가:

```js
const stampColor = diffDays < 0 ? '#2e5233' : '#c0392b';  // 지난 약속=초록, 그 외=빨강
```

## ⑥ desktop.js — 상세 카드 마크업 교체

`zone.innerHTML` 안의 기존 `<div class="growth-detail" ...> ... </div>` 블록
(수정/삭제 버튼 ~ `spotlight-note`까지)을 통째로 아래로 교체.
나머지(`.growth-hero`, `.growth-density`, `.growth-card-rail`)는 그대로.
메모 `id`/`onblur`는 기존 저장 로직 그대로 유지됨.

```js
                    <div class="growth-detail receipt" style="--stamp:${stampColor};">
                        <div class="receipt-paper">
                            <div class="receipt-zigzag"></div>
                            <div class="receipt-head">
                                <div class="receipt-brand">CLOV. MEMORIES</div>
                                <div class="receipt-sub">★  약 속 메 모  ★</div>
                            </div>
                            <div class="receipt-stamp-wrap">
                                <div class="receipt-stamp">
                                    <span class="receipt-stamp-label">${escapeHtml(ddayPhrase)}</span>
                                    <span class="receipt-stamp-dday">${escapeHtml(ddayText)}</span>
                                </div>
                            </div>
                            <div class="receipt-title">${escapeHtml(selectedSchedule.title)}</div>
                            <div class="receipt-meta">
                                <div><span>DATE</span><span>${escapeHtml(friendlyDate)}</span></div>
                                <div><span>D-DAY</span><span>${escapeHtml(ddayText)}</span></div>
                            </div>
                            <div class="receipt-memo-label">— MEMO ————————————</div>
                            <div class="receipt-memo" contenteditable="true"
                                 id="${prefix}-schedule-content-${selectedSchedule.id}"
                                 onblur="saveScheduleContent('${viewType}', ${selectedSchedule.id})"
                                 placeholder="✎ 메모를 추가해 보세요">${selectedSchedule.content || ''}</div>
                            <div class="receipt-barcode"></div>
                            <div class="receipt-actions">
                                <button onclick="openScheduleModal('${viewType}', ${selectedSchedule.id})">수정</button>
                                <button class="danger" onclick="deleteSchedule(${selectedSchedule.id})">삭제</button>
                            </div>
                        </div>
                    </div>
```

## ⑦ desktop.js — 단계 넣기 함수 (전역에 추가)

```js
function insertScheduleSteps(editorId) {
    const editor = document.getElementById(editorId);
    if (!editor) return;
    editor.focus();
    const sel = window.getSelection();
    const range = document.createRange();
    range.selectNodeContents(editor);
    range.collapse(false);            // 커서를 에디터 끝으로
    sel.removeAllRanges();
    sel.addRange(range);

    const scaffold =
        '<h3>약속 준비 4단계</h3>' +
        '<ul>' +
        '<li><b>제안하기</b> — </li>' +
        '<li><b>일정 맞추기</b> — </li>' +
        '<li><b>약속 확정</b> — </li>' +
        '<li><b>만남</b> — </li>' +
        '</ul><p><br></p>';
    document.execCommand('insertHTML', false, scaffold);
}
```

---

## 확정된 사양 요약

- **비주얼**: 짙은 초록 글로시 → 세이지 앨범 + 크림 영수증 (4컷 레일과 통일)
- **구성**: CLOV. MEMORIES → D-day 고무도장 → 제목 → DATE·D-DAY → 메모 → 바코드 → 수정/삭제
- **도장색**: 지난 약속 초록 / 그 외 빨강 (자동)
- **폭 600px · 다크모드 지원 · 클로버 워터마크 없음 · STATUS 행 없음**
- **MEMO**: 자유 편집. 비면 `✎ 메모를 추가해 보세요` 한 줄, 내용 있으면 서식(H/B/리스트/hr) 렌더
- **단계 넣기**: 강제 X. 생성 모달에서 원하는 사람만 원탭 삽입

미리보기: `growth-detail-redesign.dc.html` (REF → 탐색 1 → 확정 2 → 폭·다크 3 → 메모 4 → 결정 5)
```
