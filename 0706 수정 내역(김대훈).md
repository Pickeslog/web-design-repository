# 0706 수정 내역 (김대훈)

오늘 진행한 작업을 영역별로 정리했다. 롭(rob) 마스코트 관련 상세 내역은 `02-main/롭 마스코트 적용 내역.md`에 별도로 정리되어 있어 여기서는 목록만 남긴다.

## 1. 롭(rob) 마스코트
- 크로비/로봇(롭) 캐릭터 선택 UI, 코드 에디터 스타일 말풍선, 타이핑 효과, 클릭 리액션, 머리 잡고 들어올리는 드래그 인터랙션(높이 기반 scared/angry 전환), 커서 파괴 연출 등 추가
- 상세 내용은 `02-main/롭 마스코트 적용 내역.md` 참고

## 2. 로그인 화면 (`01-auth/login.html`)
- 이메일/비밀번호 입력 시 화면이 꿀렁거리던 레이아웃 시프트 버그 수정 (힌트를 `opacity`/`transform`만 애니메이션하도록 변경)
- 힌트 문구 위치를 입력창 아래 → 각 라벨(이메일/비밀번호) 바로 옆으로 이동, 작은 gap으로 배치
- 좌측 소개 패널의 "추억 카드"를 친구들끼리의 채팅 말풍선 형태로 교체, 문구도 일상 대화체로 변경
- 배지 문구를 "우정이 자라는 공간, Clov!"로 변경, 타이틀/설명 줄바꿈이 단어 중간에서 끊기던 문제 수정
- **로그인 성공 애니메이션 추가**: 체크마크 draw 애니메이션 + 랜덤 환영 문구 + 이스터에그 힌트 한 줄 → 줌아웃 후 확대되며 사라지는(빨려 들어가는) 연출 → `02-main/index.html`로 이동
  - 공통 컴포넌트로 분리: `02-main/js/success-overlay.js`, `02-main/css/success-overlay.css`
  - 워프 단계에서 배경이 페이드되며 로그인 화면이 잠깐 다시 보이던 버그 발견 후 수정 (배경은 opacity:1 유지, 콘텐츠만 확대/페이드)

## 3. 로그인 유지 (accessToken 목업)
- `02-main/js/auth.js` 신규 추가 — `ClovAuth.setAccessToken(remember)` / `getAccessToken()` / `clearAccessToken()`
- "로그인 유지" 체크 시 `localStorage`, 체크 안 하면 `sessionStorage`에 저장 (탭/창 종료 시 자동 삭제)
- accessToken이 있으면 `login.html` 진입 시 즉시 `02-main/index.html`로 리다이렉트
- 헤더 드롭다운 "로그아웃" 클릭 시 토큰 즉시 삭제 후 로그인 화면으로 이동 (`02-main/index.html`, `03-rooms/makerooms.html`에 `auth.js` 로드)

## 4. 회원가입 화면 (`01-auth/signup.html`)
- 전화번호 입력 필드 완전 삭제 (`checkPhone`, `formatPhoneInput` 등 관련 코드 정리)
- 닉네임/생년월일 카드의 힌트 UI를 알약 모양 칩에서 단순 텍스트로 정리, 생년월일 카드 제목·설명 텍스트 정렬 오류 수정
- 비밀번호 힌트도 로그인 화면과 동일하게 라벨 옆 배치로 변경
- 이용약관 체크박스 미체크 상태에서도 체크 표시가 남아있던 시각 버그 수정
- 약관 "보기" 클릭 시 실제 약관 내용을 확인할 수 있는 모달 추가
- confetti 이모지 줄 삭제

### 회원가입 정책 현업 트렌드 반영 재구성
- **약관 동의**: 필수 항목까지 포함해 전부 기본 미체크로 변경 (다크패턴 규제 대응, 사용자가 직접 체크해야 진행 가능)
- **비밀번호 정책 강화**: "8자 이상" → "8~20자, 영문/숫자/특수문자 중 2가지 이상 조합"으로 변경, 실시간 충족 여부 체크리스트 UI 추가
- **생년월일 완전 선택화**: 사실상 필수처럼 막던 흐름을 개인정보 최소수집 원칙에 맞춰 완전 선택 항목으로 전환, "선택" 뱃지와 "나중에 입력할게요" 스킵 버튼 추가